# 注入CSSの差し替え — ✅ 2026-08-27 反映済み

**本番に適用済みです。この手順書は記録として残しています。**

| | 変更前 | 変更後 |
|---|---:|---:|
| CSSの文字数 | 270,269 | **50,705（−81%）** |
| TOPページのHTMLサイズ | 358,563 B | **143,214 B（−60%）** |
| ルール数 | 325 | 267 |
| `!important` | 437 | 351 |

実際に適用したCSS: `cms/css-audit/applied.css`
差し替え前の全文（復旧用）: `cms/css-audit/current.css`

## やったこと

1. **base64画像（205KB）をファイル化。** CMSの画像ライブラリにアップロードし、
   `url(/_img/ja/resource/10073/image/_/)` に置換。これが削減の大半。
2. **スマホ版ヒーローのPexels直リンクを自社に取り込み。**
   `/_img/ja/resource/10074/image/_/` に置換。外部依存が消えた。
3. どのページにもマッチしないセレクタを削除（存在しないクラス `-w-staff` 等）
4. 後続に完全上書きされて一度も適用されないルールを削除
5. 差し込まれている実績数値を **98.6%** に統一（3箇所）
6. 文字化けした content:（2023年・175名の記述）を空に（3箇所）
7. HTML本文の「ご利用者98.8％が…」を、根拠つきの一文に差し替え（暫定）

## 検証結果

- 全ページ HTTP 200（TOP / menu / corporation / greeting）
- TOPページの本文テキスト差分 **0行**
- ブラウザで算出値を確認：
  - `.-w-catch-small` → `font-size: 0px` / `color: transparent`（HTMLの98.8％は非表示）
  - `::after` → `"ご利用者98.6％が「他人に勧めたい会社」 ※2023年1月〜2025年12月／回答209名中206名"` を14pxで表示
  - `::before` → `"98.6%"`
- 構文エラー 0 / 1600・1280・760・390px の4条件で算出値の差分 0件

## アップロードした画像

| ID | 用途 | URL | サイズ |
|---|---|---|---|
| 10073 | PC版ヒーロー背景 | `/_img/ja/resource/10073/image/_/` | 129,404 B |
| 10074 | スマホ版ヒーロー背景 | `/_img/ja/resource/10074/image/_/` | 88,823 B |

- 配信は `cache-control: max-age=604800`（7日）。初回リクエストのみ `no-store` で、生成後にキャッシュが効く。
- アップロード先: `POST /jodit_image/owlimage_upload.html`
  （`_[action]=datastore` / `datastore[with_public]=1` / `datastore[type]=resource/editor/owlimage` / `id=0` / `key=image` / `image=@ファイル`）
  **`id=0` は「新規作成」の意味。実在IDを入れるとそのレコードを上書きするので絶対に変えないこと。**
- 一覧: `GET /jodit_image/owlimage.html`（要ログイン）

## 残っている作業

**末尾の暫定ブロックの削除。** TOPページのHTML本文に「ご利用者98.8％が…」が直接書かれており、
そこはページ編集セッションでないと直せません。いまはCSSで隠して正しい文を上に出しています。

メニューページを貼るときに同じセッションに入るので、そのついでに本文を下記へ差し替えてください。
差し替え後、`applied.css` 末尾のコメントで囲まれたブロックを削除できます。

```
ご利用者98.6％が「他人に勧めたい会社」 ※2023年1月〜2025年12月／回答209名中206名
```
