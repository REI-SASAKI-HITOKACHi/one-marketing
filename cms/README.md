# 公式サイト（one-hitter.jp）の改修作業

## 分かったこと

- CMS は独自PHP製（管理UIは Material Components、リッチテキストは Jodit）。管理画面は `/_sys/`。
- **料金表は `/menu/` ではなく TOPページに描画されている。**
  `/menu/` は「SERVICE サービス詳細」への導線だけで、価格は1円も載っていない。
- 料金を生成しているのは 管理画面 → コンテンツ → アーティクル → **メニュー（アーティクルID 804）** の11レコード。
  - 編集URL: `/_sys/contents/article/804/<記事ID>/`
  - 記事ID: 1299, 1300, 1301, 1302, 1303, 1304, 1353, 1354, 1412, 1500, 1683
  - フィールド: `page[name]` / `input[h_ttl]` / `input[price]`(Joditリッチテキスト) / `input[text]` / `input[image]` / `display` / `pbd` / `ped`
  - 保存は POST に `_[action]=datastore` と `datastore[type]=article`
- **アーティクル単位の下書きは存在しない。保存＝即公開。**
  可視性は `display=0`（非公開）と公開日時の未来指定のみ。
- サイト全体の「ページ編集セッション」という下書き機構はあるが、
  **未公開セッションが87件（2022〜2026-05-31）滞留**しており、
  新しくセッションを開始すると他者の未公開作業を壊す恐れがある。使用は保留。

## このディレクトリの中身

| ファイル | 内容 |
|---|---|
| `menu-page/proposed-menu.html` | パンフレット準拠のメニューページ提案版（単体で開ける） |
| `menu-page/proposed-menu-body.html` | CMS貼付用の本体HTML |
| `menu-page/proposed-menu-style.css` | 同上のCSS（`.oh-menu-root` 配下にスコープ） |
| `menu-page/cms-field-map.json` | 既存11記事の価格欄「現在値 → 新値 → 変更理由」の対応表 |
| `menu-page/current-articles-backup.json` | 変更前の全11記事の値（バックアップ） |

公開前プレビュー（非公開Artifact）:
https://claude.ai/code/artifact/124243eb-b1c6-4f86-9e52-b5db686c6515

## 現状

### ✅ 2026-08-27 TOPページの料金表を修正・反映済み（オーナー承認のうえ実施）

アーティクル804の10記事の `input[price]` を差し替え。全件、保存後に再取得して一致を確認。
公開TOPページを差分検証し、**変更は料金ブロックのみ・他は一切変化なし**であることを確認済み。

| 記事ID | メニュー | 変更内容 |
|---|---|---|
| 1299 | エアコン（ノーマル） | 繁忙期表記を一律+¥3,300へ／税込明記 |
| 1683 | エアコン（自動洗浄機能） | 90分→**120分**／繁忙期統一／税込明記 |
| 1300 | 洗濯機 | **¥16,800→¥15,800**／90分→**120分**／繁忙期統一／税込明記 |
| 1302 | 換気扇（レンジフード） | **プロペラ式¥9,800を追記**／繁忙期統一／税込明記 |
| 1303 | キッチン | 90分→**120分**／繁忙期統一／税込明記 |
| 1301 | 浴室 | 90分→**120分**／繁忙期統一／税込明記 |
| 1304 | 追焚配管 | **欠落していた繁忙期注記を追加**／税込明記 |
| 1353 | トイレ | 繁忙期の「/90分」**誤記を解消**／統一／税込明記 |
| 1354 | 洗面台 | 繁忙期統一／税込明記 |
| 1412 | お掃除時短コーティング | 45分→**60分**／繁忙期注記を追加／税込明記 |

差し替え前の値は `menu-page/current-articles-backup.json` にある。戻す場合はこれを使う。

### 未実施

- `/menu/` ページの作り替え（提案版は `menu-page/proposed-menu.html` に待機中）。
  今回はTOPの料金修正のみを実施する判断。
- 滞留している未公開セッション87件の破棄。オーナーの承認は得ているが、
  今回は直接編集で完結したため破棄の必要がなく、不可逆な操作のため保留している。

## 決定事項（2026-08-27）

- TOPページの料金修正 → **実施**
- 反映方式 → 既存記事の価格欄を直接差し替え（即時反映）
- 繁忙期+¥3,300の適用範囲 → **本メニューのみ**（オプション・セット・法人には適用しない）
- 「表示価格はすべて税込」→ **明記してよい**。各価格に（税込）を付記済み
- レンジフードの「カバー光沢仕上げ」→ **掲載しない**（提案版から削除済み）
- 滞留セッション87件 → 破棄してよい（未実施、上記参照）

## 残っている確認事項

- キッチンのオプション「レンジフード」の価格。単体¥16,800か換気扇セット¥13,800かが不明で、
  提案版では「要お問い合わせ」表示のまま。

## 決定済み

- アンケート実績は **98.6%**（209名中206名／2023年1月〜2025年12月）。
  パンフレットとサイトの「98.8%」は根拠と一致しないため使わない。提案版は修正済み。

## 注意

- **認証情報は一切このリポジトリに置かないこと。**

---

## `/menu/` ページの編集経路（2026-08-27 調査）

### 結論：`/menu/` の本文は、こちらから直接差し替えられない

- 公開 `/menu/` は **ページID 6374 / レイアウト102**。TOP（6372）とは別ページ。
- 本文は「ページビルダー」で組まれた**ライブラリインスタンスの積み重ね**。
  - `data-instance="42946"`（lib 29712）= 見出し＋説明文のリッチテキストブロック ← 差し替え対象
  - `data-instance="43148"`（lib 37154）= 「SERVICE サービス詳細」の動的一覧（`/feature/` 配下を出力）
- **`/_sys/` 側にブロックを直接編集する datastore フォームが存在しない。**
  `/_sys/contents/page/` はダッシュボードのみ。`/_sys/contents/page/6374/` も `/_cms/page/`（GET）も 404。
- 編集する唯一の正規手段は**フロント側のページ編集セッション**（`POST /_cms/page/` に
  `_[action]=cms` / `mode=on`）。開始すると別タブでフロントが開き、その上に編集UIが被さる。

### 分かったこと：サイト全体にCSSを流し込む口がある

- 管理画面「リソース → フォント」（リソース1018）の `input_ci[common_css_url]` は plain textarea で、
  **`<style>…</style>` をそのまま受け取り全ページに出力する。**
- ここには既に **約267KB の `<style id="codex-brand-override">`** が入っており、
  公開 `/menu/` にも出力されている。**過去に別のAIエージェント（codex系）がCSSを注入した形跡。**
- 保存は `POST /_sys/contents/resource/1018/`（`_[action]=datastore` / `datastore[type]=resource` / `group=1018`）。
  **datastore は送った値で全置換するため、既存値を取得→追記して送らないと267KBが消える。**

### リッチテキスト欄には自由HTMLが通る（実証済み）

- Jodit にソースモードあり。`cleanHTML`/`allowTags` の上書きなし。
- サーバ側は実質サニタイズしていない。実証：ブログ記事 `/_sys/contents/article/561/1549/` に
  `<span style="font-size: 14px;">` や `<strong style="background-color: rgb(255, 255, 0);">` が原文のまま保存されている。
- `class` も保存される（`table[class="-x-on-richtext"]` が公開CSSに存在）。
- **ただし `<style>` と `<script>` は Jodit 側で除去され得る。** CSSは必ずリソース1018へ。

### 滞留セッション（現在86件）

- 存在確認: `GET /_sys/cms_session/check.html` ／ 一覧: `GET /_sys/cms_session/list.html`
- 最古 2022-09-06（#112192）〜 最新 2026-05-31（#179503）。
  **うち約50件が 2026-05-31 に集中**し、コメントは "codex" "inspect" "test" "hero" など。
  上記のCSS注入作業の取りこぼしと見られる。
- 破棄は1件ずつ: `POST` に `_[action]=datastore` / `datastore[mode]=cancel_by_id` / `datastore[session_key_id]=<ID>`
  一括破棄APIはない。

### 危険箇所

1. リソース1018のCSSは **site-wide**。`/menu/` 限定にするならセレクタを厳密に絞ること。
2. ページ編集セッションの開始・再開・破棄は、滞留86件の未公開編集を失わせる恐れがある。
3. `/menu/` の SERVICE一覧（instance 43148）は `/feature/` の動的出力。触ると詳細ページに波及する。
4. アーティクル804（TOPの料金）は修正済み。`/menu/`（6374）とは別物。
