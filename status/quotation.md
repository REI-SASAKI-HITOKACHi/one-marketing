# 見積アプリ担当 の現況

更新: 2026-09-18 07:02

## 一言でいうと

**改修版は完成しています。本番に貼られていないだけです。**
ブランチ `claude/quotation-app-performance-discount-7gr1e8`、HEAD **`e9fe133`**。テスト全緑。

## 出来ているもの

| 件 | 状態 |
|---|---|
| 速さ（①の宿題） | **済**。マスタは1リクエスト1回＋キャッシュ。入力中の再計算はブラウザ内で0往復。旧版は画面を開くたびマスタに追記していて、割引繁忙期マスタが1071行まで増えていた。新版は読み込み中に1件も書かない |
| 変則割引（②の宿題） | **済**。値引き・割増を名前つきで最大5つ。名前はそのままPDFに出る。「税込合計をこの金額にする」も入れた |
| 受注経路タブ（通常／WEB経由） | **済**。オーナー指示どおり**通常見積は一切変えていない**。WEB側だけ予約フォームと同じ料金（同時施工価格・ネット申込特典）。予約フォームとの金額一致テスト **9/9通り** |
| 請求書 | **済**。見積から起こす。過去の請求書は上書きしない（`invoice_id` があれば既存を返す） |
| 書類番号 | **済**。見積書・請求書とも `YYYYMMDD-nn` に統一 |
| 外部API（doPost） | **済**（`43047f9`〜`e9fe133`）。8アクション。**合言葉が未設定なら全リクエストを拒否**（fail closed）。定数時間比較。失敗理由はレスポンスに書かない。仕様は `apps/estimate-app/docs/external-api.md` |
| 合言葉の置き場所 | **済**。スクリプトプロパティ `API_TOKEN`。値はリポジトリにも掲示板にも無い（`apps/estimate-app/docs/認証情報.md` は置き場所だけ） |
| テスト | 計算101／保存復元93／請求書78／実マスタ55／外部API38／予約フォーム一致9／参照チェック。`bash apps/estimate-app/tools/run-all.sh` で全部走る |

## 止まっている理由（1つだけ）

**貼り付けとデプロイ。** `apps/estimate-app/deploy-paste/` の4ファイルを貼り、`adminDeployAll()` と `adminSetApiToken('…')` を実行し、新バージョンを公開する。
ここだけ**オーナー（またはブラウザ担当）の操作**が要ります。手順は `apps/estimate-app/docs/README.md` の手順0。
**貼る前に「デプロイを管理」で現バージョン番号を控えてください**（戻せるように）。

これが終わるまで、改修版も外部APIも1回も動きません。週次の数字もゼロが並びます。

## cmo の判断待ち（`20260918-01-cmo`）

**Webアプリの公開範囲が、前回と今回で逆になっています。** 9/15 は「『全員』で公開してよい」、今回は「『全員』を前提にしないで」。
技術的には「全員」以外だと呼ぶ側に Google のログインが要り、外からは叩けません。
ただし**外に口を開ける話（CLAUDE.md の「外に出す」）なので、私は公開設定を触りません。** cmo で決めてオーナーへ。
合言葉が未設定のうちは、公開だけ先に済ませても全部拒否されるので実害は出ません。

## 掲示板

- `20260912-04-quotation`（週次）… 初回を返信。**毎週月曜 08:00 JST の定期実行を作った**（`trig_01Ti2WXk8qVret2WJHstuEn2`）
- `20260912-05-quotation`（再発防止）… 「済」を返信。見積アプリは Google のドメインなので点検対象外。deploy-paste に入る外部URLは高速料金検索サイトの1本だけ（設定マスタの値）

## 他スレッドへのお願い（未対応）

`docs/price-master.md` の業務用エアコンを **¥29,800（1台）／2台目以降 −¥5,000/台** に直しました（私のブランチ側）。
**`claude/lp-outline-presentation-8fahl3` 側の正本がまだ `¥27,280〜（2台以上）` のままです。** lp が直さないと、こちらの修正が上書きされます。

## 終わった話（掘り返さない）

- `=TODAY()` の作業日 … **復元不要**とオーナー確認済み（2026-09-16）。もう追いかけません
- 繁忙期加算 3,300円 … **税込**。通常・WEB の両タブとも税抜換算して加算する（オーナー 2026-09-16 の訂正どおり）
