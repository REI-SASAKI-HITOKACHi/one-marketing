# 見積アプリ担当 の現況

更新: 2026-09-10 21:25

## いまの状態

見積アプリの改修は**コードとしては完成**。デプロイだけオーナー待ちです。
ブランチ `claude/quotation-app-performance-discount-7gr1e8`（push済み）

## 詰まっていること

**GASへの貼り付けとデプロイ**（依頼 20260910-05-cmo）
コピペ4回＋関数実行1回、約10分。インストール不要。
私からApps Scriptへ書き込む手段がこのセッションに無いため、ここだけ手作業です。

## 入れたもの

- **速度**：画面ロードのシートAPI往復 107回→10回（2回目以降キャッシュで0回）。
  入力中の再計算はブラウザ内で完結しサーバー通信ゼロ。見積作成は2フェーズにして
  保存直後に見積IDを返す。
- **本番で割引が1円も効いていなかった不具合を修正**。割引繁忙期マスタの列ずれで
  繁忙期・早期予約・複数台のすべてが不成立になっていた。マスタは開くたびに16行
  増え続けて1,071行まで膨らんでいたので、その増殖も止めた。
- **変則割引**：調整行（名称がPDFに出る）／明細ごとの値引き／合計を指定額に合わせる。
- **請求書機能一式**：INV採番・請求DB・PDF・Gmail下書き・見積へのinvoice_id書き戻し・
  二重請求防止。請求日と支払期限は提出先マスタの締め日／支払サイトから自動。
- 担当者マスタ、二重登録防止、所要時間ログ、見積プレビュー。

## 検証

`bash apps/estimate-app/tools/run-all.sh` で一括。全通過。
- 計算エンジン46 / 見積の保存→復元56 / 請求書66 / 本番マスタ実データ50
- 静的チェック（未定義関数・孤立ハンドラ・存在しないid）
- 旧コードとのシートAPI呼び出し回数比較

**未検証**：PDFの実際の見た目、Gmail下書き、Drive保存、実測秒数、スマホ操作感。
いずれもデプロイしないと確認できません。

## 共有

**予約フォームとの金額不一致を9通り全部で確認**（依頼 20260910-06-cmo）。
判断3件を出しています。決まるまで見積アプリ側は現状維持です。

自動割引は `auto_discount_enabled=FALSE` で納品しています。
コードは直っていますが、TRUEにするまで金額は今までどおりです。
