# 見積アプリ担当 の現況

更新: 2026-09-18 23:59

## 一言でいうと

**改修版は完成しています。本番に貼られていないだけです。**
ブランチ `claude/quotation-app-performance-discount-7gr1e8`、HEAD **`b2ec6e2`**。テスト全緑。

## 9/18 にやったこと

**再送で見積・請求が2件できる穴を2つ見つけて、両方ふさぎました**（`b2ec6e2`）。
`apiSaveEstimate` / `apiSaveInvoice` を本物のまま動かすテストを新しく書いたら出てきたものです。

| 穴 | 何が起きるか | 直し方 |
|---|---|---|
| ① 見積と請求でキャッシュの名前空間が同じ | 同じ `requestId` を見積→請求で使うと、**請求番号に見積番号が返り、請求行が1行も書かれないまま ok:true** | `kind` で名前空間を分けた |
| ② キャッシュ（30分）が切れたあとの再送 | **同じ見積・同じ請求がもう1件できる**。通信が切れて何時間か後に再試行されると起きる | シートの `request_id` 列を見る（`findRowByRequestId_`）。守りが3段に |

採番 `YYYYMMDD-nn` は問題ありませんでした。日またぎ・時計戻り・同時実行20件・旧接頭辞や枝番の除外・見積と請求の独立、すべてテストで固定しました。**採番が必ず ScriptLock の中で呼ばれていることも見張っています。**

**直す前のコードに新テストを当てると13ケース落ちます。**空振りではありません。

## 出来ているもの

| 件 | 状態 |
|---|---|
| 速さ（①の宿題） | **済**。マスタは1リクエスト1回＋キャッシュ。入力中の再計算はブラウザ内で0往復 |
| 変則割引（②の宿題） | **済**。値引き・割増を名前つきで最大5つ。「税込合計をこの金額にする」も |
| 受注経路タブ（通常／WEB経由） | **済**。**通常見積は一切変えていない**。WEB側だけ予約フォームと同じ料金。金額一致 9/9通り |
| 請求書 | **済**。様式は正規仕様のまま。**変更しない**（オーナー 2026-09-18 訂正） |
| 書類番号 | **済**。見積書・請求書とも `YYYYMMDD-nn`。日またぎ・同時実行をテスト済み |
| 外部API（doPost） | **済**。8アクション。合言葉未設定なら全拒否（fail closed）。定数時間比較。失敗理由は返さない |
| 二重発行の防止 | **済**。3段（キャッシュ／シートの `request_id`／見積の `invoice_id`） |
| テスト | 計算101／保存復元93／請求書78／実マスタ55／外部API38／**採番・二重作成48**／フォーム一致9。`bash apps/estimate-app/tools/run-all.sh` |

## 止まっている理由（1つだけ）

**貼り付けとデプロイ。** `deploy-paste/` の4ファイル → `adminDeployAll()` → `adminSetApiToken('…')` → 新バージョンを **「全員（匿名ユーザーを含む）」** で公開 → `ping` で疎通確認。
ここだけ**オーナー（またはブラウザ担当）の操作**です。手順は `apps/estimate-app/docs/README.md` の手順0。
**貼る前に「デプロイを管理」で現バージョン番号を控えてください**（戻せるように）。

公開範囲の食い違いは cmo が「全員」で確定させました。**私は公開設定を触りません。**

## 貼ったあとにやること

**PDF出力の実物確認**（文字化け・欠落・はみ出し・改ページ）。デプロイ前は本物のPDFが出せないので、そこからです。
**レイアウトの変更ではありません。**崩れているなら直す、気に入らないから作り替える、はしません。

## 触らないもの（確定）

- **請求書の様式**。正規仕様。変更も改善提案もしない（オーナー 2026-09-18）
- **通常見積の料金**。改修前のまま（オーナー 2026-09-16）
- `=TODAY()` の作業日 … **復元不要**で確認済み。もう追いかけない

## 他スレッドへのお願い（未対応）

`docs/price-master.md` の業務用エアコンを **¥29,800（1台）／2台目以降 −¥5,000/台** に直しました（私のブランチ側）。
**`claude/lp-outline-presentation-8fahl3` 側の正本がまだ `¥27,280〜（2台以上）` のままです。** lp が直さないと、こちらの修正が上書きされます。

## 掲示板

- `20260915-02-quotation` … 上の①②③を返信
- `20260912-04-quotation`（週次）… 毎週月曜 08:00 JST の定期実行あり（`trig_01Ti2WXk8qVret2WJHstuEn2`）
- `20260912-05-quotation`（再発防止）… 「済」
