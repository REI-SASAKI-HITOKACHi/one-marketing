# 見積アプリ担当 の現況

更新: 2026-09-23 21:04

## 一言でいうと

**本番で動いています（v6・9/19公開）。エラーは1件も出ていません。ただし実務でまだ使われていません。**
ブランチ HEAD `53aac74`。テスト全緑。

## 9/23 の確認：採番は**まだ確認できず**（3回目）

操作ログ **94行 → 95行**。増えたのは `2026/09/23 初期表示` の1行だけで、**`見積作成` は増えていません。**
最後の `見積作成` は **2026/08/07** のままです。

### わかったこと：**オーナーは開いています。見積を作っていないだけです**

```
2026/09/11  初期表示   ← 公開前
2026/09/19  初期表示   ← 公開当日
2026/09/23  初期表示   ← 今日
```

3行とも `case.foot.kid@gmail.com`（オーナー）。**エラーは1行も出ていません。**
開いてマスタを読み込んで正常に表示されている＝**動作の問題ではなく、実務でまだ使われていないだけ**です。
和真さんのアカウントでの利用は記録にありません。

### 判定方法を直しました

9/20 は見積データシートを数えて「0件」と言いましたが、**あのシートは Drive 経由だと途中で切れます**（1行約200列）。言い切れる読み方ではありませんでした（9/21 訂正済み）。

いまは**操作ログで判定**しています。ログが切れていないことも確認済み：**9/17=93行 → 9/20=94行 → 9/23=95行**。増えたぶんだけ増えている＝末尾まで取れている。**「作られたのに見逃す」ことはありません。**

### 3日おきの再確認はやめます

3回続けて「未使用のため確認できず」でした。**月曜の週次に統合します**（週次はもともと操作ログを読んでいます）。
見積が作られれば週次の「見積作成 ○件」に必ず出るので、そこで採番の形式を確認します。火曜に作られて月曜まで気づかなくても実害はありません。
**9/27 はSA鍵の差し替え日**でもあるので、CMO の作業と重ねない意味もあります。

## 小さな引っかかり（急ぎません・直していません）

**`初期表示` の3行だけ、時刻が表示されず日付のみ**です。旧アプリが書いた行（`2026/09/04 23:41:52` など）には時刻が入っています。原因は特定できていません。
動作に支障はありませんが、**ログを見ても何時の操作か分からず**、タイムゾーンの検証もやりにくくなります。

## これまでの経緯（直近）

| いつ | 何を |
|---|---|
| 9/18 | **再送で見積・請求が2件できる穴を2つ塞いだ**（`b2ec6e2`）。テスト48ケース追加 |
| 9/19 | `run-all.sh` の Windows 誤検知を修正（`297bfba`）。**貼り直し不要** |
| 9/19 | ブラウザ担当が **v6 公開**＋`adminSetMasterTimezone()` 実行 |
| 9/20 | **業務用エアコンの価格は食い違っていませんでした**（`53aac74`）。¥29,800＝定価／¥24,800＝2〜10台の1台あたり／¥27,280＝その税込。**同じ価格表の別の顔**。自分の記録の誤り2つも訂正。**lp への訂正依頼は取り下げ** |
| 9/20 | SA鍵の棚卸し。**このスレッドは鍵を持っていません。9/27 の差し替えで止まるものなし** |
| 9/21 | 週次提出。**「見積データは8行」を訂正**（実際は28行前後。Drive経由の読み取りが切れていた） |

## 止めているもの（オーナー指示と衝突）

**「見積・請求の様式を進めて」は実装していません**（`20260920-05-cmo`）。
オーナーの 9/18 の直接指示（「元の仕様が正規レイアウトだよ。勝手に変更しようとしないで」）と、CMO自身の同日の撤回と正面から反対するためです。記録ちがいだと言われれば従います。

## 次

| いつ | 何を | 誰 |
|---|---|---|
| **毎週月曜** | 週次＋**採番の確認（見積が作られていれば）** | 私 |
| 着手可 | **PDF出力の実物確認**（文字化け・欠落・はみ出し・改ページ）。**レイアウト変更ではない** | 私 |
| CMO段取り中 | `API_TOKEN` の設定。打つまで外部APIは全拒否 | オーナー |
| 保留 | 宛先メール3社。**社内のどこにも無いとCMOが確認済み。空のまま** | — |

**確認のためだけにテスト見積を作らないでください。**本番データに消せない行が残り、その日の `-01` を使ってしまいます。

## 触らないもの（確定）

請求書の様式（オーナー 9/18）／通常見積の料金（オーナー 9/16）／`=TODAY()` の作業日（復元不要）
