# 見積アプリ担当 の現況

更新: 2026-09-15 09:26

## いまの状態（2026-09-15）

**判断待ちゼロ。デプロイ待ちのみ。** コミット **`54f76c4`**、全テスト通過、`deploy-paste/` 最新。
貼るのはブラウザ担当。オーナーのPC作業リストでは3番目（①Google広告の本人確認 ②GA4 ③GASデプロイ）。

### できているもの

**受注経路タブ**（通常見積 / WEB経由見積。既定は通常見積）

- 両タブ共通：繁忙期加算 ¥3,300 は税込扱い（明細には税抜3,000で載る）
- WEB経由のみ：同時施工価格／ネット申込特典 −¥2,200（2箇所以上かつ同時施工割引なしのときだけ自動）／
  早期予約割引は1箇所限定／予約フォーム提示額との照合欄
- 納品時の設定のままWEB経由タブが予約フォームと9通り一致
- 設定は経路ごとに分離：`auto_discount_enabled`＝FALSE（通常）／`auto_discount_enabled_web`＝TRUE

**書類番号**：見積書・請求書とも `YYYYMMDD-nn`（例 `20260915-01`）。採番は `generateDocumentId_` 1本。
旧 `EST-` / `INV-` 付きの既存行と混在しても壊れない。過去の請求書は振り直さない。

受注経路・フォーム提示額・フォーム差額は見積レコードに残り、過去見積検索で絞り込める。

### デプロイ担当への申し送り（必ず伝える）

**貼る前に「デプロイを管理」で現バージョン番号を控えること。** 30秒。
手順書にあったGASバックアップのリンクは実在しないファイルを指していたため削除済み（Drive APIで404を確認）。
バージョン履歴が唯一確実な戻し方。マスタ・帳票のシートバックアップは実在する。

### 進行中：`=TODAY()` の件（過去行は凍結）

シート内の日付145件中143件が取得日と同一で、揮発は確実。復元経路を4つ当たった結果：

- 版履歴 … 私からは不可（コネクタに版履歴APIなし）。かつ元が静的だった場合しか役に立たない
- **貼られたJPGの日付 … いちばん見込みあり。** ファイル22.8MBに対しテキストは0.49MB。
  差の約22MBは画像とみてよく、印刷時の日付が焼き込まれているはず。取り出しは私には不可
- 顧客管理台帳の施工日 … 請求日ではないので代用にしかならない（入れると誤った日付で固まる）
- Driveのファイル作成日時 … 行き止まり（`請求書` フォルダに過去のPDFは1件も無い・確認済み）

オーナーのシート確認（1分）待ち。空振りなら「復元不能」と記録して永久凍結。
**アプリ経由で出すぶんは最初から固定値**なので、デプロイ後に発行するものは再発しない。

### 昨夜の請求書（完了）

株式会社プルクリトゥード様／請求番号 `20260915-01`／合計 16,280円（税込）。
PDFはオーナーへ直接、HTMLは Drive の `請求書` フォルダへ。CMO側で仕上げ済み。

### 残る宿題

- `docs/price-master.md` の業務用エアコンは ¥24,800〜 だがオーナー確認では ¥29,800 が正
- デプロイしないと確認できないもの：PDFの見た目 / Gmail下書き / Drive保存 / 実測の速度 / スマホUX
