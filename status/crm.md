# 顧客接点担当（SMS・公式LINE） の現況

更新: 2026-09-13 12:00

## いま何をしているか

掲示板6件のうち5件を処理しました（`857a803`）。**KDDIの回答で費用の前提が崩れています。**

### ✅ 内部テスト送信2通、完了（`20260913-01-crm`）

`LINE_ログ` から嶺さん・和真さんのIDを取得。今月の消化 0 → 2。**見え方の確認の返事待ち。**

> ⚠️ `LINE_ログ` は**内部用アカウントのwebhookが書いたもので、公式アカウントとはチャネルが別**。
> LINEのユーザーIDはプロバイダー単位で一意なので、そのまま送らず
> `/v2/bot/profile/{userId}` で有効かを確かめました（両方200・表示名も一致）。

道具を2つ足しました。`tools/line_internal_ids.py`（検証してから控える）と
`line_client.py push --who 嶺`（**IDをコマンドラインに書かずに済む**。表示も末尾4文字）。

### 🚨 KDDI：費用が想定の6倍（`20260913-02-crm` に回答済み）

**1通=70文字で分割課金。「1人1通＝11円」ではありません。**

| | 文字数（中央値） | 1人あたり | 費用 |
|---|---:|---:|---:|
| 本舗の確定文面 | 451文字 | **7通** | **77円** |
| 自社の確定文面 | 282文字 | 5通 | 55円 |

**対象の線引き：KDDI経由は本舗399名だけ。** 送信済み95件のうち92件が自社で、
未送信415件のうち399件が本舗。自社の残り16名は手送りで足ります（返信も届く）。

| 案 | 通数 | 費用 |
|---|---:|---:|
| 現状の確定文面 | 2,736通 | **30,096円** |
| **3通案（推奨）** | 1,197通 | **13,167円** |
| 2通案 | 798通 | 8,778円 |

### 🚨 返信導線に実害（同上）

**送信元が固定番号03なので、お客様はSMSで返信できません。**
**自社の配信停止は「その旨ご返信ください」しか書いていません。**
特定電子メール法4条は「受信拒否の通知を**受けるための**連絡先」の表示を求めるので、
**届かない返信先だけでは表示義務を満たさないおそれ**があります。
自社はKDDIに回さないので当面は問題になりませんが、**回すなら直す必要があります。**

### ✅ セーフブラウジング点検（`20260912-03/04-crm`）

**これから送るぶんに無料ドメインは0件。** netlify が入っているのは73件すべて送信済み。
未送信でURLがあるのは自社16名だけで、全部 `yoyaku.onehitter.jp`（「安全」判定）。

### ✅ 週次サマリーの定期実行（`20260912-02-crm`）

`trig_013c9rgtnNUcFaYv2N8RFgEu`／毎週月曜 08:00 JST。初回 9/14。

## 詰まっていること

なし。

## 判断待ち（CMOへ）

1. **本舗399名の文面を3通案に差し替えるか**（−16,929円）。オーナー支給の文面なのでCMO経由で
2. **返信導線の直し**（3か所）。1を通せば同時に片付きます
3. **無料トライアル（2か月3,000通）が今回の申込に付くか** — 先方に確認できますか
4. テスト行（シート6行目）を消すか（9/12から継続）
5. 返信メモの書き方を「予約／断り／質問／その他」の4択にするか（週次の分類に要る）

## 待ち

**待ち：嶺さん・和真さんからのクーポンの見え方の返事**（5項目）。
返ってきたら配布の承認依頼をCMOへ出します。

## 次にやること

1. アンケートのSMS後追い（S1）を、KDDIの70文字前提で設計し直す
2. 受付の一言（「どなたのご紹介ですか」）を和真さんの運用に入れる依頼をCMOへ
