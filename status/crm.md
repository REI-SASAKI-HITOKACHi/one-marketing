# 顧客接点担当（SMS・公式LINE） の現況

更新: 2026-09-11 16:33

## いま何をしているか

CMOの回答（判断待ち4件）と measurement の指摘を反映しました（`dfde19e`）。
掲示板の3件（web-inflow の共有／CMOの回答／measurement の単価指摘）は返信・完了済みです。

| 件 | やったこと |
|---|---|
| 紹介の制度 | **「次回1,000円割引」で受付運用を固めた**（`docs/line-referral-coupon.md` §3）。振込先を受け取る仕組みは外した |
| リピート割引 | 冬季SMSでは使わない。併用ルールはLINEのクーポン設計の中で扱う |
| 内部テスト | 手順を §3.5 に。**表示名をもらえればこちらで完結する** |
| 禁止語 | `ONE-HITTER`／`受付番号`／`netlify` を追加。自己診断の試験も2件増やした |
| **KDDIの単価** | **こちらの試算が間違っていた。11.0円（税込）に訂正** |

## 自分の間違いを2つ直しました

1. **KDDIの単価。** 公式サイトの「9.35円（税込）〜」をそのまま使っていましたが、
   これは最下段（月50,001通〜）の単価で当社には適用されません。
   先方提示は税抜で、当社の帯は**税抜10.0円＝税込11.0円**。measurement の指摘で判明しました。
2. **「KDDIに差し込み機能がある」と書いていたのは誤り。**
   APIプランには差し込みも短縮URLもありません（先方回答）。
   ただし実務上は問題なし。API経由は1通ずつ本文を渡すので、差し込みは送る側で作れます。

## 詰まっていること

なし。手は動かせています。

## 判断待ち（CMOへ・掲示板 `20260911-03-crm` に返信済み）

### 1【急ぎ】稼働中の紹介クーポンが、確定した制度と中身が違う

`01JWGRWJ9XP59WS2T1GY1944Y7` は `reward.type = cashBack`（口座振込）で、
説明文に「紹介先割引￥1,000/回」まで書いてあります。
**制度は「次回1,000円割引」で確定したので、このまま配ると違うことを伝えることになります。**

APIに獲得数・利用数を返す項目が無いので、**既に何人が持っているかは分かりません。**

案：①新しく `discount` ¥1,000 を作って配る ②旧は配るのをやめるが**終了させない**
（獲得済みの方が使えなくなるため）③提示されたら約束どおり応じる（応じ方はオーナー判断）

### 2 内部3人のLINE表示名

`GET /v2/bot/followers/ids` は使えます（実機でHTTP 200）。
**ただし返るのはIDだけで、表示名が分からないと誰のIDか突き合わせられません。**
429人全員の表示名を引くのは個人情報の取りすぎなのでやりません。
**表示名を教えてもらえれば、ID特定→テスト用クーポン→3通のテスト送信まで通します。**

### 3 撤退ラインを超えた（お金の話）

税込11.0円は撤退ライン（税込10円）超え。切り替えの判断をお願いしています。
**こちらの意見はKDDI継続。** 473名で差は473円、NTT CPaaSの10円が税込か税抜か未確認、
無償の「SMS結果通知API」の価値のほうが大きいと見ています。

## 保留していること

**本文の文字数は確定させません。** 1通の文字数上限と超過時の課金単位がKDDIに照会中で、
分割分が別課金なら費用が倍になります（measurement）。回答が来たら `UNIT_CHARS` を直します。

## 次にやること

1. CMOの判断1・2が出たら、テスト用クーポン → 内部3人 → 本番クーポン と進める
2. 受付の一言（「どなたのご紹介ですか」）を和真さんの運用に入れる依頼をCMOへ出す
3. 紹介者のLINEユーザーIDを掴む手順（受付で名前 → LINEで本人に名乗ってもらう）を詰める
