# 顧客接点担当（SMS・公式LINE） の現況

更新: 2026-09-23 02:13

## 2026-09-23 予約_Web の★列を埋める道具ができた（`20260913-03-crm` / `20260923-01-crm` 完了）

- `tools/yoyaku-match.py`（`claude/crm-sms-line` de2904d）。**既定は dry-run、書くのは `--kaku` のときだけ。**
- **参照は氏名＋電話の両方一致だけ。** 片方だけは `★照合の手がかり` に残して参照は空（別人の売上を
  広告のCVとして送らないため）。売上との照合（ご希望日±14日）も同じ規則。
- **`★台帳の名義` だけは片方一致でも採る。** 参照は緩いと事故、名義は厳しいと事故（9/11 の型）。
  引けないときは「不明」。本舗と不明の行に色を付ける。
- **台帳・◯月_売上/顧客 には1文字も書かない。** `kensan_hani()` が書き込み先を全セル検査して、
  `予約_Web` の機械の列以外なら例外で止める。注文ID(AB)・gclid(AC) も触らない。
- 検算は `tools/test-yoyaku-match.py`（偽データ27件・シートに触らない）。
  `予約_Web` は0行のままで、**今回シートへの書き込みはゼロ**。
- タイミングは**週次（月曜08:30）＋月初のCVを回す直前にもう1回**を提案して返信済み。

### 引き継ぎ

- **台帳のTEL空欄152名（957名中15.9%）には構造的に参照が付かない。** 厳しくした以上そうなる。
  手がかり欄は残るので人が見て判断できる。
- 取りこぼしが「翌月に回るだけ」で済むかは `ads-offline-cv.py` の作り次第。計測担当に確認依頼済み。

### 変わっていないもの

- 本舗SMS 394名は GO 待ちのまま（この件とは混ぜていない）。夜間なので SMS・LINE は出していない。
