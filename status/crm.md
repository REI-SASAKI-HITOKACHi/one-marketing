# 顧客接点担当（SMS・公式LINE） の現況

更新: 2026-09-11 17:49

## いま何をしているか

**オーナーの判断が2件出て、片方は手順書を渡したところまで進みました。**
差分は `claude/crm-sms-line` の `2785c65`。報告は `20260911-16-cmo`。

| | 結論 |
|---|---|
| 内部テストの宛先ID | **オーナーが「画面で控える」を選択。手順書を渡した** |
| 旧クーポンを提示されたら | **書いてあるとおり口座振込で応じる**（「万が一持ってる人がいたら振り込むよ」） |

### 手順書を渡しました

`docs/LINEユーザーIDの取り方-オーナー用.md`
`manager.line.biz` →「チャット」→ お二人のトークを開いて、**アドレス欄のURLを丸ごとコピー**。
IDだけ抜き出す必要はない、と伝えました。

**画面の作りが変わっていた場合に備えて「無理に探さず、スクショを送ってください」**
という逃げ道も書きました。こちらはブラウザで実機を見られないので、
URLに `U` から始まる文字列が出ることを**断定はしていません。**

### 旧クーポンについて、事実を1つ残しました

オーナーは「振込なんてクーポン無いと思う／使った人はいない」との認識でしたが、
**クーポン自体は実機に確かに存在します**（`cashBack`／説明文に「ご指定の口座へのお振込」と明記／
2025-06-01 から `RUNNING`）。

**「無い」ではなく「あるが使われていない」**という形で記録しました。
配布はもう止めているので、新たに誰かの手に渡ることはありません。

## 詰まっていること

### 🛑 オーナーのID待ち。ここだけで止まっています

IDが2つ来れば、**その日のうちに** テスト送信2通 → 見え方の確認 → 配布の承認依頼 まで進みます。
本番のクーポンはもう作ってあるので、テスト用を別に作る必要もありません。

## 判断待ち

なし。オーナーの回答で2件とも片付きました。

## ひとつ反省

オーナーの回答を**掲示板を経由せず直接もらいました。** このスレッドの画面でちょうど
やり取りできる状態だったので受け取りましたが、決まりとしては本来CMO経由です。以後気をつけます。
（CMOへも `20260911-16-cmo` で報告済み）

## 保留していること

**SMS本文の文字数は確定させません。** 1通の文字数上限と超過時の課金単位がKDDIに照会中で、
分割分が別課金なら費用が倍になります（measurement）。回答が来たら `UNIT_CHARS` を直します。

KDDIの資料一式（API仕様書・API利用ガイド・申込書）は
`claude/measurement-ga4-calltracking` の `docs/vendor/kddi-message-cast/`。実装時はここから取ります。
