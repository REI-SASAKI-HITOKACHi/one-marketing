# 顧客接点担当（SMS・公式LINE） の現況

更新: 2026-09-21 16:20

## 🔴 今日いちばん重い2件（2026-09-21）

### 株式会社ドアが止まった理由が、台帳に書いてあった
> 「天カセ4台 ¥118,008 ←**故障させてしまったため請求できず**」（2025年4月・菊川レジデンス）

**そして提携タブは、この ¥118,008 を売上に計上している。** この1社は営業ではなく**お詫びが先**。
金額の扱いはこちらの持ち物ではないので触っていない。

### おそうじ本舗 船堀店は「1年以上止まっている」に当たらない
**実際の最終発注は 2025-09-27（359日前）。提携タブに2件・¥37,500 が未転記。**

## 楽ラクーン除外の穴（`20260920-04-crm`・実害1名）

- **「1回でも楽ラクーン経由なら除外」に直した**（`tools/rakuraccoon-jogai.py`）。
  送信済み **1名**（CMOの特定と一致）／**155行目以降は0名 → 和真さんの手は止めなくてよい**
- 新たに除外 5名（全員もともと保留かSMS不可。番号が埋まったとき送れてしまうのを防ぐため）
- ★**途中で盛大に外した。**「行のどのセルでも楽ラクーンがあれば該当」にしたら送信済み5名・155行目以降2名と出て、
  あやうく和真さんの送信を止めさせるところだった。**原因は `カウント用`（T列）が
  プルダウンの選択肢を置いてあるだけの列**で、どの月次タブでも3〜6行目に
  One Hitter／本舗／楽ラクーン／下請 が並んでいること。**「どこかに書いてあれば」は照合ではない**

## 🔴 見出しの空白で、2023年の金額が静かに0になっていた

**2023年の月次タブの見出しは `' 売上（税込） '`（前後に空白）。** 見出し名で引くと空になる。
`derive-soushin-keitou.py` を含め `strip()` してから引く形に直した。
**列の位置だけでなく、見出しの文字も年で違う。**

## 名義判定の同日ルール（`20260920-03-crm`）

**同じ日に自社と本舗の両方があれば本舗**（和真さん指示）。**判定が変わる顧客は0件。**
★0件は偶然。以前は `max()` で「先に読んだ行」を採っており、対象4名ともたまたま本舗が先だっただけ。
**行の並びが変われば自社にひっくり返る状態だった。**

## 本舗SMS：**394名・1,182通・¥13,002。GO待ち。1通も送っていない**

## 待っているもの

| | |
|---|---|
| **送信のGO** | CMO |
| **9/27の鍵差し替えは日曜23:00 UTC の週次トリガーと重なる** | CMO（9/28 09:00 JST 以降を提案済み） |
| ドアの ¥118,008 の扱い（請求できなかったものが売上に入っている） | CMO・佐々木さん |
| 船堀店の2件未転記／2025-09-22 の金額空欄 | CMO |
| 5社の電話番号（ドア・山本組・キノビト・はるか・船堀店。台帳に無い） | CMO |
| `提携先_休眠度` タブの作り直し | CMO |
| 管理画面から発言のない友だちをブロックできるか | ブラウザ担当 |

## 申し送り

- **`sheets_client.call()` は API エラーで `sys.exit()` する。`except Exception` では捕まらない**
  （`except (Exception, SystemExit)`）。「1件失敗しても続ける」ツールは全員ここで踏む
- **控えは git（`tools/hikae.py`）。`duplicateSheet` を呼ぶツールは0本**
- **`カウント用` 列は選択肢の置き場。データではない**
- **月次タブの見出しは年で違う。位置も文字も。必ず `strip()` して名前で引く**

## 次にやること

1. `20260913-03-crm`（予約_Web ↔ 台帳の紐づけ。案A＝予約_Web側に成果元を持たせる、で決定済み）
2. 毎週月曜 08:30 JST の週次サマリー（第2回は 9/21 に提出済み）
