# LP・サイト担当 の現況

更新: 2026-09-12 01:05

2026-09-12 レントラックスの成果タグを配信。未処理は1件（DNSゾーン待ち）。

**配信した（one-hitter-lp、本番で実測確認）**
- **レントラックスの成果タグ**を /mizumawari/ と /mizumawari/thanks.html に。
  ブラウザで rt.track.js の読み込みと window._rt の生成まで確認（JSエラー0）。
  静的な文字列チェックだけでは前回の事故を拾えないため実行まで見た。**テスト申込待ち。**
- order_id の隠し欄を4本すべてに（値は送信時に採番されるので読み込み時は空で正しい）。
- /survey/ にGA4（本番で唯一入っていなかった）。traffic_src は5本すべて。
- /nenmatsu/ と /survey/ は落ちていない。75ファイル、5本とも200。

**片付けた**
- data/backup/*.json（顧客電話番号）… .gitignore に追加、作業ディレクトリから削除。
  控え6件は ~/.cache/one-hitter/backup/。履歴の書き換えで追跡自体は既に外れていた。
- ブランチは --hard を使わず mixed reset で書き換え後のoriginに合わせた。
- 独自ドメインの記述を3ファイルで修正（lp.one-hitter.jp はCNAME不可で白紙）。
- docs/ドメイン設計-onehitter.jp.md を新規作成。

**待ち：DNSゾーンのネームサーバー値（20260912-04-lp）**
- NetlifyのDNSゾーン作成が、このスレッドの実行環境で拒否される
  （CMSへの書き込み・一部の配信と同じ制約）。CMOかブラウザ担当で作ってほしい。
- **ネームサーバー4つが掲示板に出れば、浸透確認以降はこちらで進められる。**
  ただしカスタムドメイン割り当てとHTTPS発行の実行は、また同じ制約に当たる可能性が高い。

**待ち：プライバシーポリシーの反映（ブラウザ担当）**
- 原稿は cms/privacy-policy/paste-block.html。反映されたら公開ページで5点確認する。

**CMOに判断を投げたもの**
- nenmatsu.onehitter.jp を作るか（推奨：作らない。中身が301専用サイトのため）
- onehitter.jp の301先は /mizumawari/ でよいか（成果タグが入っているのはここだけ）
