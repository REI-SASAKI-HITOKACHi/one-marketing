# LP・サイト担当 の現況

更新: 2026-09-20 14:35

2026-09-20 06:xx commit 3f78e2d。配信・公開はしていない。

【20260920-01-lp 完了】広告グループID（?ag=）をLP2本の隠し欄に写した。
- CMO提案は location.search から素で取る形だったが、src・cid と同じ clean()
  （英数と _- のみ・20文字まで）を通した。値の素性が不明な点が同じため
- 別ホストへの引き継ぎ（hikitsugu）にも ag を追加。予約が yoyaku.onehitter.jp 側で
  入るとLPの隠し欄は使われず、B群の区別が付かなくなるため。要否はCMOの判断
- 実測：gads_mizumawari&ag=987654321 → ag="987654321"／QR・直接は ag=""（無影響）
  ／不正な値は無害化／25桁は20文字で切られる／別ホストのリンクにも ag が付く
- check-public-page（配信物6本）／check-tracking／test-ads-tracking／haishin-kakunin 全通過

【20260919-03-lp 調査完了】6ページの会社情報ブロックはバイト単位で同一
（sha e17bc94055ce・1438バイト）。＝共通の1か所から出ており、1回直せば6ページとも直る。
PC用(-w-pc)とスマホ用(-w-sp)で番号が2回出ていて、スマホ用だけが tel: リンク。両方直す。
探す先はリソース5（Webサイト基本情報）が第一候補。
onclick の ga(...) は typeof ga === "function" を実測。生きているので触らない
（UA終了済みなので死んでいると推測したが、実測したら動いていた）。
反映後の確認はクエリを付けずに curl して grep -c が 0 になること。

【手が離れたもの】GA4のsurvey_complete件数はCMOが貼る（Data API有効化待ち）。追わない。
Apps Scriptはオーナーからの直接依頼として成果物を残してある。

【次】未処理の残りに着手。
