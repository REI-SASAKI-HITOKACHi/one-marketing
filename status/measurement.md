# 計測担当（GA4・広告タグ・電話CV） の現況

更新: 2026-09-22 23:05

# 計測担当（GA4・広告タグ・電話CV） の現況

## ✅ 本番の計測：**全項目PASS**（2026-09-21 月曜の週次で実測）

```
python3 tools/measure-production.py  → すべて通りました
```

| | 本番で撃たれる送信先 |
|---|---|
| 申込（メイン） | `AW-18450975194/e-nVCPqV2PccENqTjt5E` |
| 電話タップ（サブ） | `AW-18450975194/2qAcCOiw3fccENqTjt5E` |
| LINEタップ（サブ） | `AW-18450975194/AryNCK7n7PkcENqTjt5E` |

`gclid` の保存・`order_id` の採番・`traffic_src`・レントラックスのタグ（`/mizumawari/` のみ）も○。
広告の着地URL2本（`?src=gads_*` ＋ `cid` ＋ `ag`）も 200。

**先週止めた「LINEのCVが本番に無い」は解消しました。**

## いま止まっているもの

| | 誰 |
|---|---|
| **アフィリエイト：案件公開のお願いメール** | **★オーナー**（`docs/原稿/レントラックス-案件公開のお願い.txt` を貼って2分） |
| **GA4の数字**（週次・アンケート欠損・`purchase`） | **CMO**（鍵はそちら。Data API は 9/20 に有効化済み） |
| GA4のキーイベント3つの登録 | ブラウザ担当（`20260920-01-browser`） |
| `?ag=` の台帳への受け渡し | LP担当（`20260920-04-lp`。hidden欄1つ＋取り込み1行＋列1つ） |
| テスト成果2件の拒否 | ブラウザ担当（`20260918-02-browser`。**10/30 に自動承認**） |

## 直近で見つけたもの（2026-09-20）

### アンケートの `survey_complete` が0件だった件 → **実装は壊れていない**

**本番のページをブラウザでウィザードごと最後まで通しました。**
`survey_step → survey_start → survey_step ×3 → survey_complete`。設計どおり。
→ **原因はページの外。** GA4の実データで3つに区別できる（`docs/measurement-audit.md`）。

**`survey_submit` を足した**（`f607e61`）。送信を押した時点。次の配信で入る。
**「送信は届いたが完了画面が出なかった」と「そもそも送信していない」を区別する材料が無かった。**

### `?src=` が GA4 とフォームで違う値になっていた → 直した（`efccbe9`・未配信）

規約（英数字と `_` `-` だけ／20文字）が**フォームの hidden 欄にしか掛かっていなかった。**
**広告で使う `gads_*` は規約に収まるので出稿には影響しない。**

## ⚠️ 触ってはいけないもの（全部、理由つきで文書に残してある）

1. **`teltap` と `form_complete`** … 公式サイトの計測。LPとは**別のページを別の名前で**数えている
2. **公式サイトの `click`** … UA時代の変換が**1回のタップで2つ撃つ**ので、キーイベントにすると1タップ2件
3. **Google広告の「連絡先 — メインのCV 0 — 設定が不適切」の警告** … 電話とLINEをサブにした設計どおり。
   **メインに昇格させると、タップだけする人を集めるほうへ最適化される**
4. **GA4のキーイベントをGoogle広告にインポート** … 直接タグ方式なので**二重計上**になる

## サービスアカウント鍵（9/27 差し替え）

**このスレッドは鍵を持っていません**（1 いいえ／2 いいえ）。**差し替えで止まる定例は1つもありません。**
ただし**週次のGA4数字はもともとCMO側で走らせる形**なので、**9/28 の週次が差し替え直後**に当たります。

## 直近でやったこと（2026-09-22）

### gclid のオフラインインポートを設計し、道具まで作った（`2844fc0`）

「決まっていないこと」の1つ目が片づきました。

| | |
|---|---|
| 設計 | `docs/広告-オフラインコンバージョンの運用.md` |
| 道具 | `tools/ads-offline-cv.py`（**既定は下見。アップロードはしない**） |
| 検算 | `tools/test-ads-offline-cv.py`（**個人情報が混ざらないことも毎回見る**） |

**月1回・月初／突き合わせは `order_id` のみ／値は実際の売上・日付は施工日／
「受注（オフライン）」を新設してタグの「申込」とは別にする。**

> **⛔ 「申込」と同じコンバージョンアクションに入れない。**
> 同じ申込が2回数えられ、CPAが半分に見えて入札が暴走する。

**確認待ち：台帳に `order_id` 列があるか。** 無ければ足すところから（`crm` / `quotation`）。
**氏名・電話での突き合わせは、別人の売上を別人のクリックに結ぶので採らない。**

### GA4の道具に `--param`（絞り込み）と `--by`＋`--daily` を足した（`1b07de9` `da169d0`）

`?src=ig` のように**カスタムディメンションで絞って日別に出せる**ようになった。
素の名前は `customEvent:` に自動で解決する。**送っているリクエストの形まで見るテスト付き。**

**0件の読み方も出すようにした。** カスタムディメンションは登録から24〜48時間出ないので、
**「0件」と「まだ記録が始まっていない」は別物。**

## 決まっていないこと

- **コンバージョンの「値」**（`lead_value` は0のまま）。入札の目標を決めるときに要る
- `gift_zip_submit` の計測仕様（`20260918-01-measurement`。急ぎません）

## 自動で回るもの

- **毎週月曜 08:00 JST**（`trig_01M3k5W9pr2HoNduxCD2B2HV`）… 週次の数字＋本番の実測＋承認期限の点検＋現況の更新
- **10/23（金）単発**（`trig_01MFE1ydXfrvFubMqziXUdMD`）… テスト成果2件の拒否が済んでいるかの確認（10/30 期限の予備）
