# 計測担当（GA4・広告タグ・電話CV） の現況

更新: 2026-09-19 01:51

# 計測担当（GA4・広告タグ・電話CV） の現況

## ✅ 広告の計測は、配信してよい状態です（2026-09-18 夜・実測）

LP担当の配信後、こちらの道具で独立に確かめました。**全項目PASS。**

```
python3 tools/measure-production.py  → すべて通りました
```

**今朝は LINEタップが FAIL（ラベルが本番に無い）でしたが、配信で解消しました。**

| 広告のCV | 本番で撃たれる送信先 |
|---|---|
| 申込（メイン） | `AW-18450975194/e-nVCPqV2PccENqTjt5E` |
| 電話タップ（サブ） | `AW-18450975194/2qAcCOiw3fccENqTjt5E` |
| **LINEタップ（サブ）** | `AW-18450975194/AryNCK7n7PkcENqTjt5E` ← 今朝は空だった |

**`?src=` の4つ（`gads_aircon` `gads_mizumawari` `gads_brand` `gads`）は、
GA4もフォームも同じ値で入ります**（`/mizumawari/` `/aircon/` の両方で確認）。

> **⚠️ 唯一こちらで確かめられないのは「GA4のサーバーが受け取って画面に出るか」。**
> 鍵も管理画面もこちらに無いため。**CMOが `?src=test_web` で1回開けば5分で決着します。**

### 次の配信に回したもの（今回はブロックしない）

`efccbe9`（GA4側にも `?src=` の規約の削り方を入れる）が未配信。
**広告で使う4つは規約に収まるので今回の出稿には影響しません。**
規約から外れた `?src=` を手で作ったときだけ、GA4とフォームで値が割れます。

---

## 本番の状態（2026-09-15 実測。`tools/measure-production.py`）

| | |
|---|---|
| 申込CV `generate_lead` | LP4本のサンクス＋予約フォームの完了画面。**1回だけ発火** |
| 電話タップ `phone_click` | LP4本＋アンケート＋予約フォームの `tel:` 全23本 |
| LINEタップ `line_click` | LP4本のLINEリンク各2本。**GA4には入るが、広告側へは未配信** |
| Google広告 | `AW-18450975194`。申込・電話・LINEの3つ（**申込だけメイン**） |
| `gclid` / `order_id` | 入る（例 `OH-20260915-mizumawari-7DQV`） |

## 今夜やったこと（2026-09-17）

### 1. `?src=` が **GA4とフォームで違う値**になっていた → 直した（`efccbe9`）

規約（英数字と `_` `-` だけ／20文字）が**フォームの hidden 欄にしか掛かっていなかった。**

```
?src=gads mizu!  → GA4 'gads mizu!'   フォーム 'gadsmizu'
?src=日本語テスト → GA4 '日本語テスト'  フォーム 'direct'
```

**同じ訪問がGA4とNetlify Formsで違う名前になり、突き合わせられない。**
広告で使う `gads_*` は規約に収まるので**今回の出稿には影響しない**が、
**手で作ったURLが1つ混ざるだけで崩れる。**

### 2. `purchase` の出どころを、**当社のページから消し込んだ**（`efccbe9`）

DebugViewも実データも見られない（鍵はCMOの環境）ので、網羅で確かめた。

| 調べた先 | `purchase` | `form_complete` |
|---|:--:|:--:|
| 公式サイト **129ページ**（サイトマップ全部） | **0件** | **0件** |
| 当社の配信物すべて | **0件** | **0件** |

見つかったのは `teltap`（127ページ）と `form_view`（1ページ）だけ。**GTMも無し。**
→ **残る可能性は3つ。`tools/ga4-event-shirabe.py` の実数1回で区別できる**（CMOに実行を依頼）

### 3. 承認期限の点検をリハーサルし、**落ちる不具合を見つけて直した**（`d927ee7`）

**同じ日に成果が2件あると落ちていた。** テスト成果2件がまさに同じ 9/15 なので、
**直していなければ 10/26 の点検が落ちて、2件とも自動承認されていた。**

## ⚠️ 見つけて、まだ閉じていないもの

1. **`teltap` と `form_complete` を外さないこと。** 公式サイトの計測で、LPとは**別のページを別の名前で数えている**。
   外すと公式サイトの電話タップと問い合わせが数えられなくなる（`docs/measurement-audit.md`）
2. **公式サイトの `click` をキーイベントにしないこと。** UA時代の変換の仕掛けが
   **1回のタップで `click` と `teltap` の2つを撃つ**ので、両方キーイベントにすると1タップが2件になる
3. **Google広告の警告「連絡先 — メインのコンバージョンアクション 0 — 設定が不適切」を直さないこと。**
   メインに昇格させると、タップだけする人を集めるほうへ最適化される
4. **このブランチの `lp/` が、LP担当のブランチより古い。** hidden 欄（`h-src`）がこちらに無く、
   **手元のテストではフォーム側を見られない**

## 待っていること

| 相手 | 待っていること |
|---|---|
| **LP担当** | **LP4本の再配信**（`20260916-01-lp`）← 広告を止めている唯一の作業 |
| CMO | `ga4-event-shirabe.py` の実行（`purchase` の決着）／設計3つのキーイベント登録／週次の数字／`?src=test_web` のリアルタイム確認 |
| ブラウザ担当 | テスト成果2件の拒否（管理画面。10/30 に自動承認） |
| オーナー | Google広告の本人確認 |

## 決まっていないこと

- **コンバージョンの「値」**（`lead_value` は0のまま）。入札の目標を決めるときに要る
- **オフラインインポートの運用**。`gclid` は貯まるが「いつ・誰が・どう戻すか」が未設計

## 自動で回るもの

**毎週月曜 08:00 JST**（`trig_01M3k5W9pr2HoNduxCD2B2HV`。次回 9/21 08:06）
週次の数字 ＋ 本番の実測 ＋ **レントラックスの承認期限の点検** ＋ 現況の更新。
