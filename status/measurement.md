# 計測担当（GA4・広告タグ・電話CV） の現況

更新: 2026-09-10 21:32

最終更新: 2026-09-10 ／ ブランチ `claude/measurement-ga4-calltracking`

## 一言でいうと

**計測の仕組みは完成していて、測定ID1行を待って止まっています。**
いま広告を出しても、成果は1件も数えられません。

## できているもの（実装・検証ずみ）

| | 状態 |
|---|---|
| LPの計測タグ（GA4・Google広告・Meta） | 設定ファイルにID1行入れて配信すれば動く |
| イベント設計 | `docs/measurement-spec.md` が正。9種類＋アンケート用8種類 |
| フォーム成果（`generate_lead`） | 送信完了ページで計上。二重計上しない作り |
| 見積り金額の引き継ぎ | フォームの見積額を成果の金額として送る（入札の最適化用） |
| 配信前の自動確認 | `python3 tools/check-tracking.py`。設定の矛盾も拾う |
| 電話CV（0円の道筋） | Google広告の転送番号で、実着信数まで無料で測れる。手順書ずみ |
| コールトラッキング事業者の比較 | 11社。数字は全て出典URL付き（`docs/calltracking-vendors.md`） |

Chromiumで実ページを通しで操作して検証ずみです（LP・サンクス・アンケート）。

## 詰まっていること

| # | 待っているもの | 誰に | 依頼ID |
|---|---|---|---|
| 1 | **GA4の測定ID（`G-`）** | CMO経由でオーナー | `20260910-08-cmo` |
| 2 | CMSのログイン情報（プライバシーポリシー追記用） | 同上 | 同上 |
| 3 | 本番へ `src`/`cid` を配信 | CMO・LP | `20260910-10-cmo` |
| 4 | 予約フォームへの計測の入れ方（A/B/C） | CMO | `20260910-10-cmo` |

**1が最優先です。** これが来た日に、設定→検証→LP担当へ配信依頼まで一気にやります。

## 今日やったこと

- スレッド組織の決まりを取り込み、`CLAUDE.md` に明記
- LP担当の依頼に対応（`build-site.py`・`deploy-netlify.py` をLP版に差し替え、
  `lp/nenmatsu/` を参照用に取り込み、ビルドと確認が両ターゲットで通ることを確認）
- **`one-hitter-lp` への配信をやめました。** 配信が要るときはLP担当へ依頼します
- 本番を実測して穴を2つ発見（`src`/`cid` 未配信・予約フォームに計測なし）

## 判断待ち（CMOに決めてほしいこと）

1. **`one-hitter-lp` へ配信してよいブランチを1本に決めてほしい。**
   `KOTEI_URL` のガードはファイルの有無しか見ないので、
   **中身が古い状態での上書きは止められません。**同じ事故がまた起きます
2. 予約フォームへの計測の入れ方（A＝こちらで変更を作って渡す／B＝手順だけ渡す／C＝保留）
3. SMSのリンクに `cid` を付けているか（付けているなら記録する価値があります）

## 反省

**9/6の私の配信が `/nenmatsu/` と `/survey/` を落としました。**
配信前に他ブランチを取り込む手順を踏んでいませんでした。CMOに復旧いただきました。
以後、`one-hitter-lp` への配信は行いません。

## 期限のあるもの

**ドメイン `one-hitter.jp` の有効期限は 2026/09/30。残り20日です。**
失効するとサイトもメールも止まります（計測以前の話として）。
