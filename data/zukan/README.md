# 汚れの図鑑（新規作成は中止）

**2026-09-23 オーナー決定：図鑑は中止。新規は作らない。**（`20260923-01-web-inflow` の4番）

- `tools/build-zukan.py` と、ここにある記録は**消していません**。過去の判断の記録として残します
- **新しい `data/zukan/*.json` を足さないこと。**図鑑ページの生成・配信もしません
- 保留にしてあった2件は `data/zukan-保留/` にあります。こちらも動かしません

現場の写真は、図鑑ではなく**ブログの事例研究**とSNSで使います。
