# ブラウザ担当｜いまの状態と次の一手（2026-09-12 02:30 CMO 作成）

**このファイルが最新。掲示板の古い依頼文より優先する。** 二度手間を防ぐため、「済んだこと」はやり直さない。
作業の前に必ず `python3 tools/org.py 読む browser`。終わったら `python3 tools/org.py 返信 20260911-01-browser --from browser`。

## 済んだこと（やり直さない）

| 件 | 状態 | 証拠 |
|---|---|---|
| Meta（Instagram / Facebook）の API 化 | **完了 9/11** | 認証情報ドキュメント第11節。web-inflow が疎通確認済み（フォロワー365・投稿66）。**トークンの再発行・アプリの作り直しは不要** |
| GBP の管理者は誰か | **判明** | MEO用アカウント **onehitter.her@gmail.com** がオーナー権限を持つ（リアライズ回答＋オーナーのログインで確認）。**Googleへの「アクセス権のリクエスト」は不要。出さない** |
| リアライズへの依頼 | **オーナーが返信を送る**（文面はCMO作成済み） | CNAME 不可／プライバシーポリシーは自分で／GBP に case.foot.kid の権限付与を依頼中／GA4 権限は付与済み |
| onehitter.jp の Netlify DNS ゾーン | **CMO が作成済み 9/12 02:2x** | ネームサーバー：`dns1.p06.nsone.net` `dns2.p06.nsone.net` `dns3.p06.nsone.net` `dns4.p06.nsone.net` |
| 見積アプリの deploy-paste | **締まった**（quotation コミット 1cc823d） | 貼ってよい |
| info@ → Gmail 転送 | **未設定**（計測担当が「転送されていない」ことを実地で確認済み） | やる（下の4） |

## 残っていること（この順。オーナーはログインだけ）

1. **GBP**（onehitter.her@gmail.com でログイン → プロフィールを表示）
   - ユーザーとアクセス権：`case.foot.kid@gmail.com` を**オーナー**、`claude-sheets@one-hitter-sheets.iam.gserviceaccount.com` を**管理者**で追加（弾かれたら文言を返信）。リアライズのアカウントは触らない
   - 直す：ビジネス説明（web-inflow ブランチ `docs/gbp-audit.md`）／営業時間 8:00〜20:00／屋号「ワンヒッター株式会社」／電話 080-8043-8259・サイトURL の確認
   - 控える：クチコミ件数・★・**未返信の一覧（日付・★・冒頭20字）**・投稿の最終日・写真枚数・Q&A・属性・カテゴリ
2. **LINE ユーザーID 2つ**（manager.line.biz → チャット → 嶺さん／和真さんのトーク → URL 末尾の `U…` 33文字）→ 認証情報ドキュメントに見出し「LINE ユーザーID（内部テスト用）」を作って貼る → `crm` に返信
3. **onehitter.jp を取得**（ムームードメイン。ロリポップと同じ GMO ペパボ。自動更新 ON・WHOIS 代理公開 ON。支払いはオーナー）→ ムームー DNS の**ネームサーバーを上の4つに変更** → 認証情報ドキュメントに見出し「ムームードメイン（onehitter.jp）」（ログインID・取得日）→ `lp` と `cmo` に返信。取れなければ onehitter.tokyo → onehitter.net
4. **info@ 転送**（ロリポップ → メール設定 → info@one-hitter.her.jp → 転送先に `case.foot.kid@gmail.com`、**「サーバーに残す」ON**）→ テスト送信 → `measurement` に返信
5. **プライバシーポリシーの追記**（CMS `https://one-hitter.jp/_sys/`、ID/PW は認証情報ドキュメント第5節。手順書 https://app.quden.io/step-guides/page/6aa3b4de92b48226b17e4f22 。原稿は lp ブランチ `cms/privacy-policy/paste-block.html`）→ 公開ページで確認 → `lp` に返信
6. **GA4**（analytics.google.com、case.foot.kid@gmail.com）：見えるプロパティ（G-8MV0E0GZHS／G-DLJCTKGTDN／G-DZF7NP8CTG）を控え、各プロパティに `claude-sheets@…` を**編集者**で追加。G-DLJCTKGTDN にカスタムディメンション `traffic_src`（イベント範囲・パラメータ名 traffic_src）を登録 → `measurement` に返信
7. **見積アプリの GAS デプロイ**（quotation ブランチ `apps/estimate-app/docs/README.md` 4章。貼る前に現バージョン番号を控える。`deploy-paste/` 4本 → `adminDeployAll()` 1回 → 新バージョンでデプロイ）→ `quotation` に返信
8. **Business Profile API の利用申請**（1 が済んでから。プロジェクト one-hitter-sheets）→ 申請日を返信

## 守ること
- 認証情報は認証情報ドキュメントにだけ。掲示板・チャット・md に書かない
- 有料の画面が出たらオーナーに一言（ドメイン代は承認済み）
- 済んだ件を再調査しない。迷ったら cmo に掲示板で聞く（作業は止めない）
