# 検索広告 段0 の入稿手順（支払い登録が終わったらこれを上から）

作成：2026-09-14 CMO ／ 設計は `docs/広告-検索広告-段0.md`
アカウント：**795-377-7623**（onehitter.her@gmail.com 配下・エキスパートモード・日本／(GMT+09:00) 日本時間／JPY）

> ## 進め方の考え方
> **キャンペーンの「設定」は画面で作る（6項目・2分）。**
> **広告グループ・キーワード・除外・広告文は、表を1回アップロードする（41語＋18除外＋広告4本）。**
> 読み上げで数十行を手入力すると、時間がかかるうえ打ち間違いが混ざる。**入るものは表で入れる。**

---

## 段取り0（支払い登録の直後・ブラウザ担当）

- [ ] `case.foot.kid@gmail.com` を **管理者**で招待（`ツールと設定 → 設定 → アカウントアクセスと安全性 → ＋`）
      ※GA4 ↔ Google広告 のリンクに両方の編集権限が要るため。実ログイン時に2段階認証が要る

---

## 段取り1：キャンペーンを画面で作る（設定だけ。広告は入れない）

`＋ 新しいキャンペーン` → **「目標を指定せずにキャンペーンを作成する」** → **検索**

| 項目 | 入れる値 | なぜ |
|---|---|---|
| キャンペーン名 | `検索_江戸川_段0` | 表の1列目と**完全に一致させること**。違うと別キャンペーンが増える |
| 単価設定 | **クリック数の最大化**／**上限CPC 500円** | CVが30件貯まるまで自動入札を使わない。データが無い自動入札は学習に予算を食われる |
| 1日の予算 | **1,667円** | 月5万円 |
| ネットワーク | **検索パートナー オフ**／**ディスプレイ オフ** | 段0は検索だけを見る。混ざると判断できない |
| 地域 | **江戸川区・江東区・墨田区・葛飾区・浦安市・市川市** | 移動60分。遠方のクリックは払い損 |
| 地域の指定方法 | **「対象地域に所在地または定期的に訪れる場所がある人」** | 既定の「関心を示した人」だと区外から拾う |
| 言語 | **日本語** | |
| 広告のローテーション | 既定のまま | |

**キャンペーンの作成画面で広告グループや広告を求められたら、仮の名前で1つ作って進めてよい**（あとで表が上書きする）。
**予算・単価・地域を入れ終えたら、そこで保存して次へ。**

---

## 段取り2：表をアップロードする

`ツールと設定 → 一括操作 → アップロード` → **ファイルを選択** → `data/ads/段0-一括アップロード.csv`

1. **プレビューを見る。**エラー行が出たら適用せず、そのメッセージをそのまま CMO に貼ること
2. 問題なければ **適用**

入っているもの：

| | 数 |
|---|---:|
| 広告グループ | 4（`A_エアコン` `B_レンジフード` `B_浴室` `C_ハウスクリーニング`） |
| キーワード | 41（フレーズ一致39・完全一致2） |
| 除外キーワード（キャンペーン単位） | 18 |
| レスポンシブ検索広告 | 4（グループごとに1本） |

着地ページ：
- `A_エアコン` → `https://lp.onehitter.jp/aircon/?src=gads&cid={campaignid}`
- 他3グループ → `https://lp.onehitter.jp/mizumawari/?src=gads&cid={campaignid}`

> **`{campaignid}` は Google が配信時に差し替える値**で、文字どおり入れて正しい。書き換えないこと。

---

## 段取り3：配信を開始する前に、必ず確認する3つ

- [ ] **コンバージョン計測が動いているか**（`docs/広告-コンバージョン計測.md`／measurement 担当）
      予約フォーム送信 `generate_lead` と 電話タップ `phone_click` の2つ。**ここが死んでいると、広告費は全部ただの支出になる**
- [ ] **着地ページが開くか**（`https://lp.onehitter.jp/aircon/?src=gads&cid=test` を実際に踏む）
- [ ] **地域が6つだけになっているか**（既定で「日本」が残っていることがある。残っていたら消す）

---

## 判定（`docs/広告-検索広告-段0.md` 4章のとおり）

**最初の2週間（9/16〜9/30）はCPAで切らない。** 見るのは
①検索語句が想定どおりか ②CVが1件でも出るか ③電話の着信、の3つ。
**10月1日に、CPAと受注単価で継続・停止・増額を決める。**

---

## 表を作り直すとき

```
python3 tools/build-ads-upload.py
```

見出し（全角2・半角1で30まで）と説明文（90まで）を**機械で検査してから**書き出す。
1本でも超えると CSV を書かずに止まる。`98.8%` や「No.1」等の使えない表現も弾く。

**広告文に使ってよい事実**：満足度98.6%（209名中206名／2023年1月〜2025年12月）／Googleクチコミ★5.0（24件）／東京・千葉・神奈川／60分10,780円（税込）から。
**使わない**：98.8%（根拠と一致しない）、裏づけの無い最上級表現（景表法）、**追加料金のオプションを標準に含まれるかのように書くこと**（例：エプロン内部高圧洗浄は ¥8,470 の別料金）。
