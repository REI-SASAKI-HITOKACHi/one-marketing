# 顧客接点（SMS・公式LINE）スレッドのセーブデータ

**このスレッドを別の場所で再開するときは、まずこれを読むこと。**
全社の前提は `docs/cmo-savedata.md`（CMOセーブデータ）。こちらはその下位。

- 最終更新：2026-09-16（送信元が死に番号の件）
- ブランチ：`claude/crm-sms-line`（`claude/one-hitter-cmo-strategy-5qk4ux` から分岐）

---

## 🚨 2026-09-15 夜 送信元 03-5674-8758 は「誰も出ない番号」だった

オーナー本人の言葉（WHOIS の話の流れで出た）：

> 君の推奨は和真の業務用携帯、03は使ってない番号、末尾4796は僕に繋がる番号だよ

**KDDI が 080/090/070 を登録できないため固定番号で申し込んだ番号が、当社の死に番号だった。**

### ★ここがいちばん大事：**送信元より本文のほうが重い**

**3通案の本文に書いてあるのは `080-1344-3137`（本舗の受付・渡辺）で、送信元の03ではない。**
本文の導線は最初から電話一本化にしてある。死んでいるのは「SMSの送信元に折り返したとき」だけ。

**ただし `080-1344-3137` が生きているかを、まだ誰も確かめていない。**
オーナーが挙げたのは3つの番号だけで、この番号は入っていなかった。

| 番号 | 繋がる先 | 確度 |
|---|---|---|
| 080-8043-8259 | 和真さんの業務用携帯 | オーナー明言 |
| 03-5674-8758 | **誰も出ない** | オーナー明言 |
| 080-6817-4796 | オーナー本人 | オーナー明言 |
| **080-1344-3137** | 本舗の受付（渡辺） | **❗未確認** |
| **080-1755-7275** | ワンヒッター代表 | **❗未確認** |

> 本文に `080-1344-3137` が2回入っていて、**うち1つは特定電子メール法4条の配信停止の連絡先**。
> ここが死んでいたら**表示義務を満たさないおそれ**がある。送信元の死は機会損失だが、こちらは法令。
> **和真さんに1行「080-1344-3137 は今も出られますか」。KDDIより先。**

### ⚠️ 本舗の文面に `080-8043-8259` は入れられない

CMOから「本文に 080-8043-8259 を明示できるか」と依頼が来たが、**できない。**
`tools/meigi_check.py` の `NG["本舗"]` に入っていて、機械の照合で止まる（2026-09-11 の決まり）。
**足すべきは番号ではなく「返信できない」の一言。**

### 案：「このSMSには返信できません」を足す（**＋15文字。通数も費用も変わらない**）

`docs/原稿/sms-honpo-3通案-返信不可を明示.txt`。実データ396名で測定済み。

| | 変更前 | 変更後 |
|---|---:|---:|
| 文字数（最短／中央／最長） | 163／169／183 | 178／184／**198** |
| 通数・費用 | 全員3通・¥13,068 | **全員3通・¥13,068** |

試し方：`python3 tools/sms-honpo-3tsu.py --ひな形 <案のファイル>`（`--confirm WRITE` とは併用不可）

### 🚨 03-5674-8758 は、いまも公式サイトとLPに「FAX」として載っている

`lp/aircon` `lp/aircon-b` `lp/mizumawari` `lp/nenmatsu` と `deploy/` の同じ4ページ、`docs/price-master.md`。
**FAXが受信できるかは電話とは別の話。推測で消さない**（CMO）。`lp/` は触らずCMOへ報告済み。

### 確認の順番（どれも1行）

```
① 和真さん「080-1344-3137 は今も出られますか」   ← 最優先・法令に効く
② KDDI「送信元への折り返しを転送できますか」
③ lp「FAX 03-5674-8758 は受信できますか」
```

**1〜3が片付くまで第1波は出さない**（CMO）。F列・D列はいまの文面のまま。

---

## ★ 2026-09-15 CMO判断6件への対応（本舗396名／名義不一致4件／法人名列）

### 本舗は **399 → 396名**（¥13,068）。CMOの数字と一致

**線引きは「法人だから外す」ではない。**「お客様本人が会社か／会社は紹介元にすぎないか」（CMO 9/15）。
外す理由も費用ではなく、**3通案の本文が追い焚き配管まで含む住居向け**だから。

判定の順序（`tools/sms-honpo-3tsu.py` の `houjin_hantei()`／答えは `data/sms-houjin-hantei.txt`）：

```
① 顧客名に法人格（株式会社／有限会社／合同会社／(株)／(有)／㈱／㈲）★かっこの中は見ない
② 提携先タブの会社名と一致
③ data/sms-houjin-hantei.txt（CMOが個別に決めたもの）
④ ①〜③で決まらず「法人/個人」列が法人 → 本文を作らず一覧で出す（いま0件）
```

> **①でかっこの中を見ないのが肝。**「古庄(株式会社エコハウス紹介)」のかっこは紹介元の注記で、
> お客様は古庄さま個人。そのまま当てると**送るべき人を外す**。

外した3件は **F列とD列のリンクを両方消して配信区分を「除外」**に。`法人_別対応` タブへ（6件。
うち3件は今回外したぶん、3件はもともと対象外だった法人）。

### 🚨 名義が食い違う行は **1件ではなく4件**あった

CMOの指示は行583だけだったが、全行を機械で照合したら4件。**うち3件は 9/11 の事故と同じ向き**
（送信系統=自社なのに台帳=本舗）。

| 行 | 顧客名 | 送信系統 | 台帳 |
|---|---|---|---|
| 567 | R.Kプランニング | 自社 | **本舗** |
| 573 | RKプランニング | 自社 | **本舗** |
| 583 | アンカークリニック船堀 | 本舗 | **自社** |
| 717 | 高幡不動店 | 自社 | **本舗** |

4件とも**配信区分を「保留」**に。理由は書き足し（もとの理由は消さない）。
照合できない19行にも理由を1文追記（配信区分は触らない）。道具 `tools/meigi-fuitchi-hold.py`。
**567と573は表記ゆれで同じ先に見える。**

> **止め方は「ツールに任せる」ではなく「シート上でも止まっている」でなければいけない**（CMO）。
> 和真さんはスマホでシートを直接触るので、配信区分を手で戻せば道具を通らずに送れる。

### `法人名` 列（U列）を売上12タブの末尾に追加

**必ず末尾。** 途中に挿入すると `SUMIF('5月_売上/顧客'!R4:R501,…)` がずれて支出/成績と年間成績が黙って変わる。
**6月で先に試して集計が変わらないことを確認してから12タブへ。**
道具（`tools/houjinmei-column.py`）が実行の前後で集計を自動照合する。12か月すべて一致。
6月行37・8月行8 の「山口様」に `株式会社テック山口`。**氏名は1文字も触っていない。**

### planning へ直接出した（`20260915-01-planning`）

| | 件数 | 金額 |
|---|---:|---:|
| planning | 21件 | 約219万 |
| **crm 確定（A＋B）** | **21件** | **¥2,863,470** |
| crm 9/14時点 | 19件 | ¥2,622,730 |
| crm 全部拾い | 26件 | ¥2,967,485 |

行単位の表26行つき。訊いたのは①どの行か ②金額はどの列か ③**8/11 BREXA の ¥1,683,000 は入っているか**。
**答えが出るまでどの数字も確定として使わない**（CMO）。

### 覚えておくこと

- **KDDI 無料トライアル（2か月3,000通）が付けば 1,188通はまるごと無料枠。費用は未確定。**
- **`tools/org.py` の名簿に `planning` `gift` `browser` が無かった**（掲示板には前から居る）。
  こちらでは足した。**cmo ブランチの org.py にも同じものが要る**
- 9/15 09:28、`org.py 現況 --from crm --本文 ""` を動作確認のつもりで打って**現況を一度空にした**。
  書き直し済み。**`--本文` に空文字を渡すと上書きで消える。**

---

## ★ 2026-09-15 未明 本舗SMSを3通案へ／テック山口／予約_Webの照合ツール

### 本舗SMS 3通案（依頼 `20260913-02-crm`・オーナー決定 9/14 11:5x）

**399名全員が3通に収まった。名乗り照合の違反0件。¥13,167。**

| | |
|---|---:|
| 対象 | 399名（個人392・**法人7**） |
| 文字数 | 最短163／中央169／**最長184** |
| 通数 | 全員3通 |
| 費用 | 1,197通 × 11円 = **¥13,167**（元の451文字版は7通＝¥30,096） |

- ひな形：`data/sms-template-honpo.txt`（**cmoブランチのファイルをこちらに新規追加した。マージで競合する**）
- 道具：`tools/sms-honpo-3tsu.py`（既定は検算のみ。`--3通` で70文字の区切りを表示）
- 控えタブ `控え_冬季見込み客_20260914-1754`。**F列以外の差分0件**
- **1通も送っていない。** KDDI 利用開始は 9/25 ごろ

> **「本舗の未送信」は 661行ある。399名になるのは配信区分が「送信可」の行だけ**
> （SMS不可59・除外200・保留3 を除く）。ここを間違えると費用が1.7倍に見える。

> 🚨 **行583：送信系統=本舗なのに台帳の最新名義が「自社」。** いまは配信区分が送信可でないので
> 399名に入らないが、**区分が変われば自社のお客様に本舗名義で飛ぶ。** 9/11 の裏返し。CMOに報告済み。

### 株式会社テック山口（依頼 `20260914-02-crm`・オーナー指示 9/14 15:5x）

**「山口様」＝株式会社テック山口。本舗→自社への転換成功事例であり、業務提携先でもある。**
提携タブ**行19**に追加し 6/17 ¥149,000・8/2 ¥91,740 を紐づけ。総合計 **¥11,860,552**。
**`◯月_売上/顧客` の「山口様」という氏名は書き換えていない。**

これで保留は **06/13 の加藤グループ5件（¥104,015）だけ**。
確定は **21件・¥2,863,470**。**planning の「21件・約219万」と件数は合うが金額が合わない**（内訳待ち）。

### 🚨 `法人/個人` の列は絞り込みに使えない（両方向にずれている）

- 山口様は台帳でも冬季リストでも「個人」だが、実体は**株式会社**
- 本舗399名の「法人」7名には `寺西 志津` `寺田 美香` のような**個人名が混ざっている**

→ 「法人に個人向けの季節案内を送らない」を機械の条件にするなら、
**顧客名に法人格（株式会社・有限会社・合同会社 等）が含まれるかで見るほうが確か。** CMO判断待ち。

### 予約_Web の照合ツール（依頼 `20260913-03-crm`・CMO 9/14 08:5x 全面採用）

`tools/yoyaku-match.py`。`予約_Web` に **O〜AA の13列**を追加した。

```
O:src｜P:cid｜Q:★台帳の顧客ID｜R:★台帳の名義｜S:★台帳の最終施工日｜T:★照合の手がかり｜
U:★属性｜V:★売上行への参照｜W:★売上（税込）｜X:レントラックス注文番号｜
Y:成果発生日｜Z:入金確認日｜AA:★却下判定
```

- **★は機械が書く。X・Y・Z は人が入れる列で、値があれば上書きしない**
- 照合は `meigi_shiraberu()` と同じ（電話でも氏名でも引き、いちばん新しい施工を採る）
- 売上との照合は**ご希望日の±14日**。当たらなければ空のまま残す
- **条件付き書式**：台帳の名義が「本舗」の行に色。**SMS・LINEで連絡せず和真さんが電話**
- `--自己診断` で台帳の実在客6名を通し、全員照合・本舗に印が付くことを確認
- **cmo の毎時ルーティンから `--confirm WRITE` で呼んでもらう**

### ロイヤリティ二重計上 → オーナー決定「年末まで触らない」

2027年度シートから正しい構造にする。**2026 の販管費は年 ¥600,600 過大、
和真さん理論値は月 ¥22,522 過少**のまま。`docs/ロイヤリティ二重計上-検算.md` に明記済み。

### クーポン統計はブラウザ担当が画面から取得できた（CMO 9/14 15:26）

**APIには無いが、LINE公式アカウントマネージャーの画面にある。** 来週の週次から入れる。
動いているのは防カビ1本だけ（獲得40・使用3＝7.5%）。**リッチメニュー経由は PV19・獲得0**。

### SMS返信の4択分類 → まだ0件

`お詫びSMS_20260912` は 送信済み54・返信あり3（9/14 に27→57名へ進んだ）。
返信メモは3件のままで、4択の書き込みは0。いまの3件を読むと**予約見込み2・お礼1、確定予約0**。

---

## ★ 2026-09-14 週次サマリー第1回／提携タブの転記／ロイヤリティ二重計上

### 週次サマリー（掲示板 `20260912-02-crm` に返信。**完了にしないこと**）

毎週月曜 08:30 JST まで。定期実行 `trig_013c9rgtnNUcFaYv2N8RFgEu`（日曜23:00 UTC）。

初回（9/7〜9/13）に出した数字：

| | 前週 | 今週 | 差 |
|---|---:|---:|---:|
| SMS 送信先（実人数） | 0 | **74**（自社73・本舗1） | +74 |
| SMS のべ通数 | 0 | 101（本編74＋お詫び27） | +101 |
| 不達 | 0 | 3 | +3 |
| 返信（実人数） | 0 | 5 | +5 |
| 予約につながった件数 | 0 | **0** | ±0 |
| LINE 配布／消化／友だち／ブロック | 0／0／429／37 | 0／2（社内のみ）／429／37 | ±0／+2／±0／±0 |

**前回「送信済み95件」と書いたのは誤りだった。** `送信済み` 列の **`対象外` 21件を送信に数えていた**。
正しくは 74件（送信済み67・返信あり4・不通エラー3）。対象外21は送っていない。

**数字の取り方で分かったこと**

- `冬季見込み客_2026` に**送信日の列が無い**。今回は「SMSは9/9開始・お詫びは9/12」という事実で
  週を切った。**複数週にまたがると週ごとに切れない。** H列「送信日の目安」は予定であって実績ではない
  → **送信日の列を1つ足す案を出してある（返事待ち）**
- LINE の友だち数は `insight/followers?date=YYYYMMDD` で日付指定の実測が取れる。9/6・9/13とも429/37
- お詫びSMSの27名は本編74名に**全部含まれる**（重なり27／実人数は74のまま）
- 返信は 冬季4件＋お詫び3件、重なり2 → **実人数5**

### 提携タブの転記（依頼 `20260914-01-crm`）

`【毎月更新】リピート/業務提携` へ 2026年5〜8月の **19件 ¥2,622,730** を追記済み。
総合計 ¥8,997,082 → **¥11,619,812**。**既存セルは1つも書き換えていない**（控えタブと全セル比較）。

- ツール：`tools/teikei-tenki.py`（既定は計画表示のみ。書き込みは `--confirm WRITE`）
- **レジェンド様が売上22組を使い切っていた** → 売上23〜32の列を追加。
  合計→**BQ**、総合計→**BR**、平均単価→**BS**、案件発生→**BT**、施策→**BU** に移動
- 平均単価の割る数が手打ちでズレていた（レジェンド様は21件あるのに `/19`）→ COUNTA で数える式にした
- **保留7件 ¥344,755**：06/13の加藤グループ5件（個人紹介に見える）、山口様2件（提携先不明）。
  山口様は台帳では `C0878`・受注2回・流入=業務提携の**お客様**として載っている

> **planning の「21件・約219万」と合わない。** こちらの機械抽出は26件・¥2,967,485。
> 加藤グループ5件を除くとちょうど21件になるが、**金額は286万で219万にならない。**
> planning に内訳を出してもらう依頼を返信に入れてある。

### 🚨 ロイヤリティ二重計上は**クロ**（月 ¥50,050・年 ¥600,600）

`docs/ロイヤリティ二重計上-検算.md`／検算ツール `tools/roy-check.py`。**まだ直していない**（指示どおり）。

販売管理費 `=(F53+F61-F59)+K61` を展開すると `-J61` と `+J61`（L61の中）が打ち消し合い、
**HITOWA3行＋固定ロイ1行（計¥50,050）が支出明細とロイヤリティの両方に残る**。
**12か月すべてシート上の値と1円まで一致**したので読み違いではない。

- 効き先：`年間成績` 28行目の販売管理費 → 営業利益・経常利益が月¥50,050少ない。
  和真収入（理論値）も月¥22,522ほど少なく出る。1〜8月だけで **¥400,400**
- 直し方（案A推奨）：`F53: =(SUM(F2:F52))-J61-F9-F10-F11-F12`

### 🚨 Drive のバックアップが取れない

**サービスアカウントには Drive の保存容量が無い**（`403 storageQuotaExceeded`）。
Driveコネクタの `copy_file` もこの環境では無効。**タブ複製で代えた**（`控え_業務提携_20260913-2326`）。

> **スプレッドシート全体のバックアップが要る作業は、オーナーの手で Drive のコピーを取ってもらうしかない。**

### 予約_Web の設計（依頼 `20260913-03-crm`・返信済み）

- **新規客を台帳に手で足す運用は作れない。** 台帳は `◯月_売上/顧客` から生成する派生データなので、
  手で足すと再生成で消えるか二重になる。**施工後に和真さんが売上に1行入れれば自動で顧客IDが付く**
- **電話番号だけの照合では6人に1人を取り逃す**（台帳957名中 TEL空欄152名＝15.9%／同じTELの重複3通り）
  → `meigi_shiraberu()` と同じく**電話でも氏名でも引いて、いちばん新しい施工を採る**
- **台帳957名のうち523名（55%）が本舗名義。** 本舗のお客様がWebから申し込んだら、
  受注は自社でよいが**確認連絡をSMS/LINEで送らない**（施工前は最新名義がまだ本舗＝9/11と同じ事故）
- measurement の38日：`レントラックス注文番号／成果発生日／入金確認日／却下判定` の4列
- **見出しをどちらに合わせるか（いまの列名 vs cmo予定の列名）だけ返事待ち**

---

## ★ 2026-09-10 スレッド組織が始まった（ここが一番変わった）

**作業を始めるとき、いちばん最初に `python3 tools/org.py 読む crm` を実行する。**
決まりは `docs/org/README.md`、自分の職務範囲は `docs/org/roles/crm.md`。役割IDは `crm`。

- **オーナーに直接聞かない。CMOに掲示板で聞く。**
  オーナーに上がるのは「お金を使う」「外に出す」「元に戻せない」の3つだけで、それもCMOがまとめて上げる
- 区切りごとに `python3 tools/org.py 現況 --from crm` を出す

### 仕事の切り分けが変わった

| | 誰が持つか |
|---|---|
| **冬季見込み客SMSの実行** | **CMO**（`tools/build-sms-list.py`／和真さんがスプレッドシートから送信）。9/10時点で40件送信済み |
| **SMSの文面（正データ）** | **オーナー支給の確定版。** `data/sms-template.txt`（自社）／`data/sms-template-honpo.txt`（本舗） |
| KDDI Message Cast の申込 | **CMO**（`docs/KDDI申込-確認事項.md`）。こちらからは追わない |
| 公式LINE（Messaging API・紹介クーポン） | **こちら** |
| アンケートのSMS後追い設計 | **こちら** |

### 🛑 こちらのSMS準備物は廃止した（2026-09-10）

`data/sms-templates.json`／`tools/sms_build.py`／`docs/sms-copy.md` に使用禁止のマークを入れた。

> **とくに本舗ぶん（B1/B3）は絶対に流用しないこと。**
> 「12月は繁忙期料金となります」と料金に触れており、本舗の営業ルール
> （料金に触れない・URLを載せない・箇所の提案だけ）に反する。

残したのは、差し込みの安全装置（年またぎ・社内用語の言い換え）と
名乗り＋法令の機械チェック（`--check`）。CMOへ渡す材料として置いてある。

### 重複確認の結論（掲示板 `20260910-11-cmo` に提出済み）

- **こちらは1件も送っていない。送れる状態にもない**（KDDI未契約・provider設定なし・`--confirm SEND` 必須）
- こちらの自社60名は **CMOの自社208名の部分集合**。優先1〜3から抜いたもので、**楽ラクーン経由は0名**
- **二重送信のリスクはゼロ**

### CMOへ出した指摘（返事待ち）

1. **オーナー支給の確定版に配信停止の案内が無い。** 特定電子メール法4条は
   ①送信者の名称 ②受信拒否の通知先 の表示を求める。①はあるが②が無い。**40件送信済み**
2. **流入経路=本舗 なのに 送信系統=自社 の方が、60名中19名いる。**
   `build-sms-list.py` も送信系統列で振り分けるので、この19名にはワンヒッター名義が飛ぶ。台帳の確認を依頼
3. 固定電話17件・前回メニュー欠落2名・未登録タブ34名の価値（情報提供）

---

## ★ 2026-09-13 KDDIの回答で前提が変わった／内部テスト送信を実施

### ✅ 内部テスト送信 2通、完了

`LINE_ログ` タブ（2026年台帳）のD列から嶺さん・和真さんのユーザーIDを取得。

> ⚠️ **`LINE_ログ` は内部用アカウント（3人グループ）のwebhookが書いたもので、公式アカウントとは
> チャネルが別。** LINEのユーザーIDはプロバイダー単位で一意なので、別プロバイダーなら値も別物になる。
> **そのまま送らず、公式アカウントのトークンで `/v2/bot/profile/{userId}` を引いて確かめた。**
> → 両方 HTTP 200。表示名「ささき」「わたなべかずま」。**同じプロバイダーだった。**

道具を足した：
- `tools/line_internal_ids.py` … 台帳から取り、**公式アカウントで検証してから**控える
  （`~/.config/one-hitter/line-internal-ids.json`・gitの外・600）
- `line_client.py push --who 嶺` … **ユーザーIDをコマンドラインに書かずに済む。** 画面も末尾4文字だけ

今月の消化数 0 → 2。**見え方の確認（5項目）の返事待ち。**

### 🚨 KDDI：1通=70文字で分割課金。1人1通ではない

**ここが一番効く。** 「660文字まで送れる」は事実だが、**課金は70文字ごと**。

| | 文字数（中央値） | 1人あたり | 1人あたりの費用 |
|---|---:|---:|---:|
| 本舗の確定文面 | 451文字 | **7通** | **77円** |
| 自社の確定文面 | 282文字 | **5通** | 55円 |

**「1人1通＝11円」で見積もると7倍ちがう。**

### 🚨 送信元が固定番号 03-5674-8758。お客様はSMSで返信できない

080/090/070 は申し込めない。**確定文面の「ご返信ください」が、KDDI経由では機能しない。**

- 本舗：「このメッセージにご返信いただくか、080-1344-3137（渡辺）までお電話ください」
  → 電話が併記されているので導線は残るが、返信の案内は無効
- **自社：「今後のご案内が不要でしたら、その旨ご返信ください。」→ 返信しか書いていない。**
  特定電子メール法4条は「受信拒否の通知を**受けるための**連絡先」の表示を求めるので、
  **届かない返信先だけの記載は表示義務を満たさないおそれがある**

**いま和真さんが手送りしているぶん（080から）は返信が届くので、問題は起きていない。**
**KDDI経由（03から）に切り替えた瞬間に問題になる。**

### 対象の線引き（実データ）

| | 送信済み | 未送信 |
|---|---:|---:|
| 自社 | **92** | 16 |
| 本舗 | 3 | **399** |

**自社は手送りでほぼ終わっている。未送信16名も手送りで足りる（返信も届く）。**
→ **KDDI経由は本舗399名だけ。**

| 案 | 通数 | 費用（税込11円） |
|---|---:|---:|
| 現状の確定文面 | 2,736通 | **30,096円** |
| **3通案（推奨）** | **1,197通** | **13,167円** |
| 2通案 | 798通 | 8,778円 |

**3通案を推奨。** 2通に詰めると、オーナーが最重視した名乗り（直近の施工時期と内容）が痩せる。
短縮案は `docs/原稿/sms-honpo-短縮案.txt` ／ `docs/原稿/sms-jisha-短縮案.txt`。
返信導線を**電話だけ**にしてある（名乗り照合は違反0）。

**文面はオーナー支給なので、こちらでは変えない。案を出してCMO経由でオーナーへ。**

---

## ★ 2026-09-12 待ちで止まらない（オーナー指示）と、第3波の検算

### オーナー指示（2026-09-12・全スレッド）

> 承認待ちで作業を止めるのは回避して。オーナー宛の AskUserQuestion は出さない。

- **判断が要るものは掲示板で cmo に出して、他の作業を続ける**
- **オーナーが自分からこのスレッドに来たときは、その場で直接やり取りしてよい。**
  もらった回答は同日中に【オーナー決定】で cmo へ
- 本当に次にやることが無いときだけ止まる。そのときは掲示板に「待ち：◯◯」と書いてから

### LINEユーザーIDは「ブラウザ担当（browser）」が取る

オーナーに催促しない。取得できたら掲示板で返信が来る。**それまで他の作業を進める。**

### 第3波42名の検算：違反0件

**利用年=2022のみ・送信可は42名**（CMOの連絡は43名だったが、追記43行のうち1名は保留）。
全員 送信系統=本舗／送信日の目安=KDDI一括。`honbun_ihan("本舗", 本文)` で**違反0**。

> 🛑 **やらかしかけたこと。** changelog の「964〜1006行に43行を追記」をそのまま見にいったら
> 全行の本文が空に見えて、危うく「違反なし＝合格」と誤報するところだった。
> **本文が無いから違反が出なかっただけ。**
> **シートは送信可を上に集めて並べ替える作り**（`build-sms-list.py` の `okuru.sort`）なので、
> **追記直後の行番号は当てにならない。** 利用年や配信区分で特定すること。

副産物：**送信可511行のうち1行が「優先=テスト」の動作確認用行**（シート6行目・送信日の目安 9/9）。
これを除くと実顧客510行＝自社108・本舗402で、変更34の数字と一致する。
送信済みは空欄なのでまだ送られていない。**上から送る運用だと1件目に当たる**のでCMOへ報告した。

### 配布の手順と原稿を用意した（IDが来たら押すだけ）

- `docs/line-クーポン配布-手順と原稿.md` … テスト送信→見え方の確認→承認依頼→配布の4段階
- `docs/原稿/line-test.txt` … 社内テスト用
- `docs/原稿/line-shokai.txt` … 紹介してくださった方へ

どちらも名乗り照合を通してある（担当者名・社名ともあり／違反0）。
**LINEは友だち登録済みの方だけに届くので、特定電子メール法4条の表示義務はかからない。**
文面に「停止」の案内は入れていない。

---

## ★ 2026-09-11 夜 本番の紹介クーポンを作った（配布はまだ）

**`01M27R6C993BKR6W044VVV7QM4`「ご紹介ありがとうクーポン」／コード `Viral002`**
discount ¥1,000／2026-09-12〜2027-06-01／`UNLISTED`／1人1枚・1回／RUNNING

CMO承認「**作成まで**。配布は内部テストが通ってから改めて」（2026-09-11）。**まだ誰にも配っていない。**

- 旧 `01JWGRWJ9XP59WS2T1GY1944Y7`（cashBack）は **配布停止・終了させない**で確定
- 旧を提示された方への応じ方 → **決着（2026-09-11 オーナー）。「万が一持ってる人がいたら振り込む」**
  ※ オーナーは「振込なんてクーポン無いと思う／使った人はいない」との認識。
  **ただしクーポン自体は実機に確かに存在する**（cashBack・説明文に口座振込と明記・2025-06-01から RUNNING）。
  利用実績は無いものとして扱い、万が一の提示には振込で応じる
- `maxTicketPerUser` は `CouponCreateRequest` に無いプロパティだが、**作成後に見たら `1` が入っていた**（既定値）
- KDDIは**そのまま**（CMO判断。差473円より結果通知APIの価値が大きい）

### 🛑 内部2人のユーザーID特定は、実行できなかった

CMOの指示は「候補語（レイ／佐々木／嶺／渡辺／和真／ONE HITTER）に当たるまで順に引いて止める・
保存しない」だった。そのとおり書いたが、**実行環境の安全機構に『個人情報の一括取得』として
止められた。** 回避はしていない。

**理由は納得できる。** 表示名で照合するには結局429人ぶんのプロフィールを引くことになり、
一致しない方の情報を捨てるとしても「429人ぶんの個人情報を読む」ことに変わりない。

**残る手は、オーナーが LINE Official Account Manager のチャット画面でIDを控える**（10分）。
**2026-09-11 オーナーがこの方法を選択。手順書を渡した**
（`docs/LINEユーザーIDの取り方-オーナー用.md`）。**IDの受け取り待ち。**

ID（2つ）が来たら、その日のうちに：テスト送信2通 → 見え方の確認 → 配布の承認依頼、まで進む。

---

## ★ 2026-09-11 夕 CMOの回答が出た（判断待ち4件が片付いた）

| 件 | 結論 |
|---|---|
| **紹介の制度** | **「紹介した本人の次回料金から一律1,000円割引」が正。** キャッシュバック（口座振込）ではない。振込先を受け取る仕組みは不要 |
| リピート割引¥1,000 | **冬季SMSでは使わない。** 文面はオーナー支給で確定しており割引に触れていない。併用ルールはLINEのクーポン設計の中で「他クーポンとの併用不可」 |
| 内部3人へのテスト送信 | **必要。** お客様に届くものは必ず内部で1回受けてから |
| `honbun_ihan()` の禁止語 | `ONE-HITTER`／`受付番号`／`netlify` を追加（反映済み） |

### 🚨 そこで新たに出た問題：稼働中のクーポンが制度と違う

`01JWGRWJ9XP59WS2T1GY1944Y7` は `reward.type = cashBack`（口座振込）で、
説明文に「紹介先割引￥1,000/回」まで書いてある。**制度は「次回1,000円割引」で確定したので、
このまま配ると違うことを伝えることになる。**

**APIにクーポンの獲得数・利用数を返す項目は無い**（OpenAPI定義で確認）。
既に何人が持っているかは分からない。

こちらの案（CMO判断待ち）：新しく `discount` ¥1,000 を作って配る／旧は**配るのをやめるが
終了させない**（獲得済みの人が使えなくなるため）／提示されたら約束どおり応じる。

### 内部3人のユーザーIDの取り方（実機で確認）

`GET /v2/bot/followers/ids` は**このアカウントで使える**（HTTP 200 を確認）。429人ぶん取れる。
ただし**返るのはIDだけ**で、`GET /v2/bot/profile/{userId}` で表示名は引けるものの、
**3人のLINE表示名が分からないので誰のIDか突き合わせられない。**

→ **3人の表示名をもらえれば、あとはこちらで完結する**（CMOへ依頼済み）。
429人全員の表示名を引けば力ずくで探せるが、**そのために429人ぶんの個人情報を取りに行くのは
過剰なのでやらない。**

### 💴 KDDIの単価は税抜だった。撤退ラインを超えている

measurement からの共有（先方メールで確定）。**こちらの試算が間違っていた。**

| 月間の配信量 | 税抜 | 税込 |
|---|---:|---:|
| **〜5,000通** | **10.0円** | **11.0円** ← 当社はここ |
| 5,001〜10,000通 | 9.5円 | 10.45円 |
| 10,001〜50,000通 | 9.0円 | 9.9円 |
| 50,001通〜 | 8.5円 | 9.35円 |

**公式サイトの「9.35円（税込）〜」は最下段（月50,001通〜）の単価で、当社には適用されない。**
それで試算していたのが誤り。`tools/sms_build.py` の `PRICE_PER_UNIT` は **11.0円** に直した。

> **税込11.0円は、撤退ライン（税込10円）を超えている。**
> NTT CPaaS への切り替えを検討する取り決め。**判断はCMOへ上げた。**
> ※ NTT CPaaS の「10円」が税込か税抜かは、こちらの調査資料では確認できていない。
>   同じ間違いを繰り返さないよう、**切り替えるなら税込で確かめること。**

**1通の文字数上限と超過時の課金単位はKDDIに照会中**（measurement）。
分割分が別の1通として課金されるなら費用が倍になる。**回答が来るまで本文の文字数を確定させない。**

### SMSのリンクは `?src=sms` を必ず付けたまま

APIプランに短縮URL機能が無く、**クリック数はKDDI側で測れない**（先方回答）。
GA4側で測るので、外すと反応率が出せなくなる（measurement）。

---

## 🚨 2026-09-11 名乗りの照合は必須になった（全スレッド共通の決まり）

**本舗名義のお客様3名に、ワンヒッターの文面が届く事故が起きた。**
原因は「冬季見込み客_2026」の**送信系統列を信じたこと**。
あの列がどの規則で入ったか記録が無く、台帳の実際の名義と食い違っていた。
（9/10 に僕がCMOへ「19名いる、確認する側に倒したい」と出した論点が、現実になった形）

> **列の値を信じない。台帳の「最新の施工の名義」と機械で照合する。**
> **照合できない相手には送らない。**
> （オーナー指示。`CLAUDE.md` の「禁止」に入っている）

### 道具

| | |
|---|---|
| `tools/derive-soushin-keitou.py` | CMO作。`meigi_hyou()` が 電話番号／氏名 → (名義, 最新施工日) を返す |
| **`tools/meigi_check.py`** | **こちらで作った共通モジュール。SMSと公式LINEの両方が使う** |

`meigi_check.py` は2つとも見る。**片方だけでは足りない。**

1. **宛先** … `atesaki_ok(hyou, tel, name, "自社"/"本舗")`
2. **本文** … `honbun_ihan(meigi, body)`。自社の文面に本舗のもの、
   本舗の文面にワンヒッターのもの（社名・受付番号・`one-hitter`・`lin.ee`・`netlify.app`）が
   混ざっていないか。**宛先が合っていても、URLが1本混ざれば事故**

`python3 tools/meigi_check.py` で自己診断が走る（照合表が相手方をちゃんと弾くかを確かめる）。

### 入れた場所

- `tools/sms_build.py --check` … 各テンプレートの本文を照合＋台帳が読めるかを確認
- `tools/sms_build.py --csv ...` … **1行ずつ照合。台帳が読めないと終了コード2で止まる**
- `tools/line_client.py push` … お客様あては `--tel` か `--name` が必須。
  台帳＝本舗なら送らない。社内テストは `--internal`

### 効き目

実データで送信可が **60名 → 54名**。**6名が「台帳＝本舗／文面＝自社」で弾かれた。**

### 認証と控え

- 認証は環境変数 `GOOGLE_SHEETS_SA_KEY`。手元では `~/.config/one-hitter/`（**gitの外**）
- 控えは `~/.cache/one-hitter/meigi-cache.json`。**電話番号と氏名が入る。コミット禁止**
  （`.gitignore` に `*meigi-cache*.json` を追加済み）

---

## ★ 2026-09-11 公式LINEのトークンが届き、実機を見た（前提が2つ崩れた）

認証情報：Googleドキュメント「ワンヒッター_認証情報（ここだけ）」第4節。
手元では `~/.config/one-hitter/line.env`（**gitの外**）。**値をチャット・掲示板・gitに書かない。**

### 実測値

| | 値 |
|---|---|
| アカウント | OneHitter(ワンヒッター)／`@366ysqyc`／Channel ID 2011544394 |
| 友だち | **429人**（配信が届く 330人／ブロック 37人） |
| 今月の無料通数 | **200通**（消化 0通） |

**友だち330人への一斉配信は、月200通の無料枠を130通超える。**
紹介クーポンは該当者だけなので問題ないが、全員配信をやるなら費用の判断が要る。

### 🚨 崩れた前提1：紹介クーポンは、もう作ってある

**「紹介キャッシュバック_One Hitter」が2025年6月から稼働中**（`01JWGRWJ9XP59WS2T1GY1944Y7`）。
9/06 に書いた「これから作る」という設計は前提から間違っていた。
**作る前に必ず `coupon-list` を見ること。** 作成後は修正不可、終了させると獲得済みの人も使えなくなる。
※ ただしその後、**このクーポンは確定した制度と中身が違うと分かった**（上の「夕」の節）。

稼働中は3件（紹介キャッシュバック／リピート割引¥1,000／防カビ無料）。
旧世代3件とオータムキャンペーンは CLOSED。重複稼働はしていない。

### 🚨 崩れた前提2：紹介の制度が、資料と違う

| | 資料（cmo-savedata / prices.json） | 実機のクーポン |
|---|---|---|
| 紹介した人 | 次回料金から1,000円引き | **口座へ1,000円振込（cashBack）** |
| 紹介された人 | 記載なし | **1,000円割引** |
| 1件あたり | 1,000円 | **2,000円** |
| 人数上限 | 記載なし | なし |

**1件の紹介で動くお金が倍違う。** → **決着：資料のほう（次回1,000円割引）が正**（2026-09-11 CMO）。
つまり稼働中のクーポンのほうが実態と合っていない。

**リピート割引¥1,000（2回目以降）が稼働していること自体、資料に載っていない。**
→ **冬季SMSでは使わない**（2026-09-11 CMO）。

### クーポンAPIで4か所間違えた（もう繰り返さない）

正は **LINE公式のOpenAPI定義**。リファレンスのページはJS描画で読めない。
`https://raw.githubusercontent.com/line/line-openapi/main/messaging-api.yml`

| 間違い | 正しい |
|---|---|
| ミリ秒 | **epoch秒** |
| `Asia/Tokyo` | **`ASIA_TOKYO`** |
| `PRIVATE` | **`UNLISTED` か `PUBLIC` の2つだけ** |
| maxUseCountPerTicket を書かない | **必須**（最大1／無制限は -1） |

**エラーは `{"message":"Internal Server Error"}` としか返らない。** YAMLと突き合わせるしかない。

テスト用を1件だけ作って仕様を通した（`01M26WJX50VCKACZ2EHM8D7RR7`／UNLISTED／
2026-09-12 09:06 JST に自然終了）。誰にも配っていない。**closeは元に戻せないので触らない。**

### 次の難所は、クーポンではなく受付の運用

**「誰の紹介か」を記録する運用が無い。** LINEのユーザーIDは台帳の顧客IDと繋がらない。
起点を「ご予約の受付」に置く。詳細は `docs/line-referral-coupon.md`。
※ 振込先の受け取りは**不要になった**（制度は「次回1,000円割引」で確定したため）。
なお **LINEのwebhookには署名検証が無い**ので、そもそも個人情報をこの経路に流さないこと。

---

## 0. 任務

**既存のお客様にもう一度買っていただく導線を、動く状態にする。**
手段はSMSと公式LINE。最大の商機はクロスセル（エアコンのみ621名／潜在3,100万円）。

---

## 1. 作ったもの

| ファイル | 何か |
|---|---|
| `data/sms-templates.json` | 冬季SMSの文面（A1/A3=自社、B1/B3=本舗、S1=アンケート後追い、L1=LINE誘導） |
| `tools/sms_build.py` | 宛先の正規化・差し込み・課金通数の見積もり。`--check` で文面を検算 |
| `tools/sms_send.py` | 送信実行。`--confirm SEND` が無いと送らない |
| `docs/sms-copy.md` | 訴求の設計と、特定電子メール法・景表法の自己点検 |
| `tools/line_client.py` | LINE Messaging APIクライアント |
| `data/line-coupon-referral.json` | 紹介クーポン（一律1,000円）の本文 |
| `docs/line-referral-coupon.md` | 紹介の受付運用と実装の設計 |
| `docs/phone-fill-in.md` | 電話番号の穴を埋める運用（渡辺さんへの依頼文つき） |

---

## 2. 実データで分かったこと（savedataに無かった／違っていた）

### 冬季SMSは54名にしか届かない（名乗り照合を入れたあと）

優先1〜3の141名のうち送信可は **54名**。183通分・**約2,013円**（税込11.0円で計算）。

| 除外の理由 | 件数 |
|---|---:|
| おそうじ本舗経由（送信手段が未確定・保留） | 44 |
| 電話番号が空欄 | 27 |
| **固定電話でSMSが届かない** | **8** |
| **名義が食い違う（名乗り照合で弾いた）** | **6** |
| 前回のメニューが台帳に無い | 2 |

**固定電話は「電話番号_未登録」タブに載っていない。** あのタブは空欄しか数えていない。
優先1〜3の全体では17件（うち自社ぶん8件）。

### 未登録タブの優先1〜3（34名）の価値

累計受注額 合計¥6,348,256／平均¥186,713／**中央値¥89,150**／最大¥1,330,190（法人1社）。
CMOセーブデータの「平均LTVは約12万円」は法人12社に引っ張られた値。**中央値で見ること。**

### 冬季の訴求に割引は使えない

早期予約割引は11〜12月＝0%。代わりに事実を2つ使う。

1. **12月から繁忙期料金+3,300円** → 「11月中なら通常価格」（`data/prices.json` の対象月は5・6・7・12）
2. **同時施工なら1箇所3,300円お得**（浴室等 単体18,480円 → 同時15,180円）

「残りわずか」は**使わない**。稼働率はピーク65%で事実に反する。
「98.6%」もSMSでは**使わない**。分母を併記する余白がなく優良誤認になりうる。

### KDDI Message Cast（公式サイトで確認、2026-09-06）

- 初期0円・月額0円・**9.35円（税込）〜**・**到達課金**
- **660文字まで送れる。「文字数に応じて課金通数が異なる」とだけ書かれていて、換算表は非公開**
  → `tools/sms_build.py` は業界慣行の「70文字＝1通」で仮置き。契約時に正式な換算表で直すこと
- ~~差し込み機能あり~~ → **誤り。APIプランには差し込み機能も短縮URL機能も無い**
  （2026-09-11 measurement が先方に確認）。公式サイトの記載は管理画面プランのものだった。
  **ただし実務上は問題ない。** API経由は1通ずつ本文を渡す方式なので、氏名・施工時期・メニューの
  差し込みは送る側で作れる。クリック数だけはKDDIで測れないので、GA4側（`?src=sms`）で測る
- 送信元番号にフリーダイヤル指定可。「送信元番号指定」はオプション扱い

### LINE（公式ドキュメントで確認、2026-09-06）

- クーポン作成：**POST https://api.line.me/v2/bot/coupon**（2025-08 追加）
- 固定額割引：`reward.type="discount"` / `priceInfo.type="fixed"` / `fixedAmount`
- クーポンメッセージ：`{"type":"coupon","couponId":"..."}`。push/multicast/broadcast等で送れる
- **作成後は修正不可。終了させると獲得済みの人も使えなくなり、復活しない**
- **リクエストボディの全プロパティ名は未確認**（公式リファレンスがJS描画で取得できない）
  → `data/line-coupon-referral.json` は下書き。**エラー本文を読んで直す。推測で足さない**
- コミュニケーションプランは**月200通まで無料**。応答メッセージはカウントされない

---

## 3. いまの状態

| | 状態 |
|---|---|
| KDDI Message Cast | **2026-09-05夜に申込済み・返信待ち。** 単価が税込10円超なら NTT CPaaS に切り替える |
| KDDIのAPIキー | 未受領。`private/sms-provider.json` に仕様を書き写してから送信できる |
| LINE Messaging API | 手順書は渡し済み。**トークン待ち** |
| 冬季SMSの文面 | **完成。承認が出ればすぐ送れる** |
| 紹介クーポン | 設計完了。トークンが来たらテスト作成から |
| 電話番号153名 | 渡辺さんの入力待ち（TODO T011・期限 09/12） |

---

## 4. オーナー確認済み（2026-09-06）

| 項目 | 結論 |
|---|---|
| おそうじ本舗経由の44名 | **送らない。** 本舗名義で送る手段がない |
| 受付電話 | **ワンヒッター＝080-8043-8259／おそうじ本舗＝080-1344-3137。** どちらも渡辺さんだが使い分けている。取り違えないこと |
| KDDIの申込 | 2026-09-05の夜に **`info@one-hitter.her.jp`** から送信済み・返信待ち |
| ★KDDIの返信を確認する場所 | **`info@one-hitter.her.jp`。** 接続中のGmail（case.foot.kid@gmail.com）からは検索しても出てこない（14日分を確認済み）。**このスレッドからは中身を読めないので、返信の有無はオーナーに聞くか、転送してもらうこと** |
| LINE手順書 | オーナーへ再送済み。作業はこれから |

### 名乗りのルール（オーナー指定・絶対に崩さないこと）

> 目的は「**あの人**」を想起させること。本舗のお客様には**本舗の安心感**を出すこと。

1. 担当者名で名乗る
2. **必ず直近の施工時期と内容を一文に入れる**
3. ワンヒッター＝**「ハウスクリーニングの渡辺」**／本舗＝**「おそうじ本舗江戸川中央店の渡辺」**
4. それとは別に、会社名・店名を本文のどこかに必ず残す（特定電子メール法 第4条）

型：`{last_when}に{last_service}を施工させていただきました、ハウスクリーニングの渡辺です。`

**`tools/sms_build.py --check` がこの4点を機械で検算する。**

### まだ決まっていないこと

1. 配信停止の受け口 — 送信元番号への返信を受け取れるかをKDDIに確認する
2. 前回メニューが台帳に無い1名（電話番号あり）を渡辺さんが個別に対応する

### 差し込みで踏んだ罠（修正済み・繰り返さないこと）

- **「昨年」と決め打ちしない。** 最終施工 2026/01/11 の方に「昨年1月」と出た。`{last_when}` が年を見て出し分ける
- **台帳の社内用語をそのまま出さない。** 「エアコン(ロボ)」と出た。`MENU_LABEL`（`tools/sms_build.py`）で
  「お掃除機能付きエアコンのクリーニング」に言い換える。先頭が言い換えられない語（残置物撤去・応援）の
  ときは次の項目まで見る。どれも駄目な行（軽作業など）は**送らない**（誤った文面を出すよりは安全側）

---

## 5. 守ること

- **一斉送信は「外に出す」。オーナーの承認が必須。** 文面と対象の準備までは承認なしで進めてよい
- 過去の実績データ（施工日付・売上・氏名・住所・メニュー）を書き換えない
- スプレッドシートを変更する前にDriveでコピーを取り、`docs/sheet-changelog.md` に記録する
- **電話番号を数値として書き込まない**（先頭の0が消える。過去に800件の事故）
- 認証情報はリポジトリに一切コミットしない。Driveの限定公開ドキュメント経由で受け渡す
- 宛先リストなど個人情報を含むファイルは `private/` に置く（`.gitignore` 済み）
- 触らない：`lp/` 配下すべて、`data/prices.json`、GA4・広告タグ、ドメイン・DNS

---

## 6. 次にやること（2026-09-10 更新）

1. **作業開始時に `python3 tools/org.py 読む crm`。** これを飛ばさない
2. CMOからの返事を待つ（掲示板 `20260910-11-cmo`）。配信停止の1行と、本舗19名の確認
3. **LINEのトークンが来たら**：疎通確認 → 今月の通数確認 → テスト用クーポン作成 → 本番クーポン
4. 紹介の受付運用（「どなたのご紹介ですか」を受付の一言に入れる）をCMO経由で和真さんへ
5. アンケートのSMS後追い（S1）は、CMOの送信の仕組みに乗せる形で設計し直す

### やらないこと

- **冬季見込み客SMSを自分で送らない・自分でリストを作らない。** CMOが持っている
- **オーナーに直接承認を求めない。** CMOに掲示板で聞く
- KDDIの申込を追わない。CMOが持っている
