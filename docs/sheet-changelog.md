# 2026_売上/顧客情報管理 シート 変更履歴

対象: `2026_売上/顧客情報管理`（ID `1TK70pwQ8lYmjxUVCfFp1E2T5qDjHOnD4XSviZzUpB64`）
実施方法: Sheets API（サービスアカウント `claude-sheets@one-hitter-sheets.iam.gserviceaccount.com`）

---

## 2026-09-04 記録欄の改善

**バックアップ**: `【バックアップ】2026_売上/顧客情報管理 2026-09-04 改修前`
（`1l6XZPMXsPd5CA3mUg1VblvgfxISt1gxQ8aEIN4LeNY8` / オーナー名義でDrive上に作成済み）

対象範囲: `1月_売上/顧客` 〜 `12月_売上/顧客` の 12タブ、4行目〜504行目

### 変更1: 流入経路（D列）の選択肢を 14 → 19 に拡張

**発見した不具合**: プルダウンが `strict=True`（リスト外を拒否）にもかかわらず、
選択肢に **「紹介」が入っていなかった**。実データには紹介が11件（¥379,460・平均¥34,496）あり、
平均単価の高い経路が正規の手段では入力できない状態だった。

| | 選択肢 |
|---|---|
| 既存14（すべて維持） | 営業／業務提携／チラシ(OH)／チラシ(本舗)／LINE／HP／SMS／早期予約／リピート／スケジュールマッチング／入札案件／その他／楽ラクーン／おそうじ定期便 |
| 追加5 | **紹介**（不具合対応）／**LP(アフィリエイト)**／**地図検索**／**LINE/SMS再販**／**Google口コミ** |

追加4つは、今後のネット施策の成果を台帳で追えるようにするため。これがないと
LP・地図検索・再販の受注が「その他」に埋もれて効果測定できない。

### 変更2〜4: L・M・Q列をチェックボックスから選択式へ

| 列 | 項目 | 変更前 | 変更後の選択肢 |
|---|---|---|---|
| L | 早期予約提案 | チェックボックス | 未記録／未提案／提案済／成約／対象外 |
| M | フォローコール | チェックボックス | 未記録／未実施／SMS送信／LINE送信／電話／返信あり／再依頼 |
| Q | ＯH打診 | チェックボックス | 未記録／未打診／打診済／反応あり／成約／対象外 |

**理由**: TRUE/FALSE では「打診したが反応がなかった」と「そもそも対象外だった」を区別できない。
打診は現場が客ごとに判断する属人的な行為なので、結果まで残らないと何が効いたか分からない。

**既存の値の扱い**: 全行が `FALSE`（＝チェック未入力）だった。オーナー確認により
これは「実施していない」という記録ではなく単なる記録漏れであるため、
**同じ意味の「未記録」に置き換えた**。値の意味は変えていない。
埋め戻し作業は発生せず、無効値の警告マークも出ない。

置き換えたセル: L 313件 ／ M 314件 ／ Q 323件（施工日付のある行のみ）

### 作業中に起きたことと、その修正

チェックボックスは**データのない空行にも敷かれていた**ため、最初の一括置換で
約6,000セル×3列が「未記録」になった。施工日付のない行をすべて空欄に戻して修正済み。
最終状態は施工日付のある323行にのみ値が入っている。

### 触っていないもの

- 施工日付・売上・氏名・TEL・住所・メニュー・備考などの実績データ
- 既存の「売上分析」タブ（壊れているが未着手）
- 過去年（2023・2024・2025）のシート
- **7月_売上/顧客 の49行目** … 売上種類「本舗」／流入経路「楽ラクーン」／メニュー「エアコン(ロボ)」が
  入っているのに施工日付と売上が空欄。元からある不完全な行のため、そのまま残した。**要確認**

## 元に戻したいとき

バックアップファイルを開き、該当タブをコピーして差し替える。
プルダウンだけ外す場合は、範囲を選択 → データ → データの入力規則 → すべて削除。

## 2026-09-04 分析タブの作り直しと再販リストの新設

### 変更5: `分析_件数単価流入_v2` タブを新設

既存の `分析_件数単価流入` タブは**削除していない**。数字を突き合わせてから、どちらを使うか判断できる。

旧タブで確認した不具合：

| 不具合 | 内容 |
|---|---|
| 9月が丸ごと欠落 | 月別リストが 1,2,3,4,5,6,7,8,10,11,12月 のみ |
| 「早期予約」が存在しない | 流入経路の項目がなく、リピートに合算されていた |
| `#REF!` 残存 | L1セル |
| 集計全体が古い | 旧310件/¥12,283,131 に対し実データは **323件/¥12,636,141**。9月以外の月もズレていた |

新タブは **月次タブをSUMIFS/COUNTIFSで直接参照する数式**で組んだ。行を追記すれば自動反映され、
ピボットの更新漏れで数字が古くなることが構造的に起きない。

構成：月別サマリ／流入経路別サマリ（19経路・構成比つき）／月×経路の売上クロス／月×経路の件数クロス

直った結果：

| 項目 | 旧 | 新 |
|---|---|---|
| リピート | 127件 / ¥4,374,773 | 69件 / ¥3,130,845 |
| 早期予約 | （項目なし） | **68件 / ¥1,510,138** |
| 9月 | （行ごと欠落） | 9件 / ¥353,010 |
| 年計 | 310件 / ¥12,283,131 | **323件 / ¥12,636,141** |

### 変更6: `再販リスト` タブを新設

顧客ごとに最終施工日をまとめ、メニュー別サイクルで案内時期が来た人に **★いま** を付ける。
サイクル：おそうじ定期便・定期清掃=1ヶ月／トイレ・洗面台=6ヶ月／その他=12ヶ月。推奨時期の2ヶ月前から ★。

生成結果（2026年シートのみ）：ユニーク顧客 **258名**／2回以上の利用 40名（15.5%）／**★いま 2名**

**★が2名しか出ないのは、2026年シートしか見ていないため。** 2026年に施工した顧客は
まだ1年経っていないので当然そうなる。本当の再販プールは **2025年シートの444名**で、
うち9〜12月に施工した160名がちょうど今「1年後」を迎える。
→ **2025年シートをサービスアカウントに共有すれば、300名規模のリストが作れる（共有クリック1回）**

## 未着手

- 2025年シートを対象に含めた再販リストの再生成（上記のとおり、共有待ち）
- 旧 `分析_件数単価流入` タブの扱い（数字を確認後、削除するかどうかの判断）

## 2026-09-05 打合せシート・冬季見込み客・TODO再構成

2023〜2025年のシートも共有いただいたため、4年分（**1,488件・¥52,300,292**）を横断できるようになった。

### 変更7: `MTG_2026-09-04` タブを新設（先頭）

施工責任者との打合せ用。議題20件（スケジュール／キャパ／写真／連携／台帳／年末／提携）。
「決定」列はプルダウン（決定・保留・対象外・要相談）、決定内容の記入欄つき。

### 変更8: `冬季見込み客_2026` タブを新設

4年分の全施工から顧客を名寄せし、**ユニーク965名**を抽出。冬季施策の優先度でセグメント化。

| セグメント | 人数 | うち個人 | 累計売上 | 定義 |
|---|---|---|---|---|
| A_昨年同期 | 80 | 73 | ¥3,977,531 | 2025年9〜12月に施工（ちょうど1年） |
| B_冬季常連 | 28 | 23 | ¥5,829,724 | 過去に2回以上、9〜12月に施工 |
| C_サイクル到来 | 607 | 572 | ¥24,231,669 | 最終施工から10ヶ月以上 |
| D_リピーター | 42 | 38 | ¥4,536,420 | 2回以上利用 |
| E_その他 | 51 | 46 | ¥1,675,590 | 上記以外 |
| Z_直近施工 | 157 | 152 | ¥12,049,358 | 直近3ヶ月以内。今回は送らない |

**配信対象（Z除く）808名／うち個人752名。自社系統393名・本舗系統415名。TELあり687名。**

列に「送信系統（自社/本舗）」「法人/個人」「今回おすすめ（前回と違う場所を提案）」「送信済み」を持たせ、
配信の実務でそのまま使える形にした。「送信済み」はプルダウン（第1波/第2波/第3波/返信あり/予約/対象外）。

### 変更9: `TODO` タブを再構成

旧タブは番号とFALSEのチェックボックスだけで、実質未使用だった。

**設計方針：担当者が触るのは B列「状態」のプルダウン1箇所だけ。** それ以外はCMO側で埋める。

| 列 | 内容 | 誰が入れる |
|---|---|---|
| A | ID（T001〜。カレンダー予定と対応） | CMO |
| B | 状態（未着手/進行中/完了/保留/対象外） | **担当者・1クリック** |
| C | 期限 | CMO |
| D | 担当 | CMO |
| E | やること | CMO |
| F | なぜ（効果）— 数字で書く | CMO |
| G | 手順・補足 | CMO |
| H | 完了メモ（任意） | 担当者 |
| I | カレンダーの予定へのリンク | CMO |

上部に集計（未着手／進行中／期限切れ／完了）。期限切れは赤、完了は打ち消し線で自動着色。

### 変更10: TODOをGoogleカレンダーに登録する運用を開始

TODOシートは見に行く習慣がないため、**通知はカレンダーが担う**。
CMOがTODOを追加するたびにカレンダー予定を作成する（オーナーの都度承認は不要との合意）。

- 終日予定・「予定なし」扱いなので、スケジュールを圧迫しない
- 説明欄に「なぜ」「やること」「TODOシートの該当行へのリンク」を入れる
- 担当が渡辺さんのものは参加者に追加 → 招待が届き、リマインダーも鳴る

初回10件（T001〜T010）を登録済み。

**注意：終日予定は `+09:00` 付きの時刻を渡すとUTC変換で日付が1日ずれる。**
正午（`T12:00:00+09:00`）を渡すことで回避している。

### 変更11: `目次` タブを新設（先頭）

全43タブが並んでいて何がどこにあるか分からない状態だったため、一番左に `目次`（gid=291423606）を追加した。
各タブの中身を実際に読んだうえで、「シート名（リンク）／分類／何のシートか／誰が使う／更新タイミング」の5列にまとめている。
シート名は `=HYPERLINK("#gid=...")` でクリックするとそのタブに飛ぶ。分類は **日々使う／月次の入力／集計・分析／勤怠・精算／記録・アーカイブ** の5グループ。
1〜12月の `◯月_売上/顧客`・`◯月_支出/成績` は各1行にまとめ、右側（F〜Q列）に12ヶ月分のリンクを横並びにした。全29行で1画面に収まる。

**読んで分かったこと（目次の説明に反映済み）**

- `年間成績` は1〜16行目が空で、表は **17行目** から始まる。
- `分析_件数単価流入`（旧）は L1 のピボットが `#REF!` で壊れており、**9月の行も抜けている**。数字は `_v2` を見ること。
- `紹介カード` は「提携先」の見出しと2社の社名だけの、ほぼ空のタブ。
- `【不使用】勤怠管理` は `【新】勤怠管理` の旧版で、労働時間が時刻形式（4:30:00）のため集計しにくい。最下部に置いた。

既存タブのデータは一切変更していない（新規タブの作成と書き込みのみ）。

### 変更12: `顧客管理台帳` の TEL列を修正（先頭の 0 が落ちていた）

**957件中800件の電話番号から、先頭の `0` が消えていた。** 台帳を作った際に値を数値として書き込んだため、
`09012345678` が数値 `9012345678` として保存されていた（9桁＝固定電話、10桁＝携帯）。
このままではSMSも発信もできないため、次の手順で直した。

1. D16:D972 の表示形式を **書式なしテキスト** に変更（今後入力しても0が消えない）
2. 先頭に `0` を復元した値を書き戻し。`冬季見込み客_2026` タブは作成時に文字列として書いたため0が残っており、
   そちらに正しい値がある行はそれを優先して採用した

結果：**801件が正常（0始まり）／152件が空欄／1件が要確認**。`冬季見込み客_2026` の804件と件数が整合する。

**要確認の1件**
- `C0248 有限会社飯村左官工業` … TEL が `70447793940`（11桁・0始まりでない）。桁数が合わないため復元できず、`要確認:` を付けて残した
- `C0499 青山リアルティ・アドバイザーズ株式会社` … TEL欄に `和美躰` という人名らしき文字列が入っていた（入力ミス）。未登録扱いにした

**過去の売上シート（`◯月_売上/顧客`）は触っていない。** 台帳は各月シートから生成した派生データなので、そちらだけを直した。

### 変更13: `電話番号_未登録` タブを新設

電話番号が無い **153名** を、渡辺さんがスマホの連絡先から埋められる形にまとめた（gid=19259735）。

- 入力するのは **D列「★電話番号」だけ**。D列は書式なしテキストにしてあるので先頭の0が消えない
- `冬季見込み客_2026` の優先順位（1〜9）で並べ、優先1〜3（2回以上ご利用のリピーター）が上に来るようにした
  → 優先1: 13名／優先2: 7名／優先3: 14名／優先4-5: 13名／優先7-8: 73名／優先9（直近施工）: 33名
- 本人を特定できるよう 顧客名・住所・最終施工日・施工メニュー を並べ、A〜E列を固定
- E列「状態」にプルダウン（未確認／入力した／スマホに無い／連絡不可／送信対象外）
- 条件付き書式：入力済みの行は緑、**優先1〜3で未入力の行は琥珀色**
- 3行目に「入力済み／残り／優先1〜3の残り」のカウンタ

内訳：個人115名・法人38名。送信系統は 自社136名・本舗17名（本舗顧客は送信主体がおそうじ本舗江戸川中央店になるため分けてある）。

---

## 2026-09-06 見積ルールタブ：価格改定と紹介料の修正

**バックアップ**: Drive に `【バックアップ】2026_売上_顧客情報管理 2026-09-06 価格改定前`
（`1XdIhxB_TE0RCdZdnmdG6u1fsweaj2_WZI_42imWJ7ok`）としてスプレッドシート全体をコピー済み。

変更したセルは4箇所だけです。

| セル | 変更前 | 変更後 |
|---|---|---|
| A18 | （空欄） | 「※2026-09-06 改定。掲載価格はすべて税込。割引は税込価格に対して掛ける。正データは GitHub の data/prices.json」 |
| C20 | 9800 | **10780** |
| D20 | 15800 | **17380** |
| E20 | 29800 | **32780** |
| B23 | 紹介料：1-2月：5% | **紹介料：消費者→消費者：一律¥1,000** |
| B24 | 紹介料：その他：10% | **紹介料：事業者→顧客：5〜15%（原則12%）** |

理由：

- **定価**：オーナー判断で税込表示に統一（実質10%の値上げ）。`docs/price-master.md` 参照。
- **注記の追加**：「掲載価格は税込」がどこにも書かれていなかったため、
  チャネルごとに計算方法が割れていました（早期予約は9,800円から直接割引、
  マッチング経由は税抜から1.1倍）。その原因を潰すための1行です。
- **紹介料**：旧記載は月別の料率でしたが、実際は紹介元で分かれます（2026-09-06 オーナー確認）。

**変更していないもの**：

- 繁忙期料金の ＋￥3,300 は据え置き（元から税込の形＝3,000円の1.1倍のため）
- 複数台割引の金額（￥500〜￥7,000/台）
- 過去の実績データは一切触っていません

**要確認として残っているもの**：

- E20 の 29,800円 は「複数台（業務用）」の定価ですが、パンフレットの
  業務用エアコン「¥24,800〜（2台以上）」と一致しません。どちらが正か未確認のまま、
  ルールどおり1.1倍して 32,780円 にしています。

---

## 2026-09-06（2回目）まとめ改修：鍵の入れ替え前に済ませたもの

**バックアップ**: Drive に `【バックアップ】2026_売上_顧客情報管理 2026-09-06 まとめ改修前`
（`1AQoOfytzWnJopZIz8KhQZu75gaqxAcMHHJU7y-O0S6Q`）としてスプレッドシート全体をコピー済み。

サービスアカウントの鍵を作り直すと、こちらから触るのに一手間かかるようになるため、
**必要な修正と追加をまとめて先に済ませた**（オーナーの指示）。

### ① 見積ルール：A45〜A53 に「適用の順序」を追記

タブのA42に「上記各基準の適用優先順位」という宿題が残っていたので、それへの回答。
単価の決定 → 小計 → 繁忙期加算 → 割引を1つだけ → 紹介の還元 → 定額値引き、の順。
**この順番でないと金額が変わります。**
未確定の論点（繁忙期加算は割引の前か後か、など）は「要確認」と明記してあります。

### ② 目次：抜けていたタブを追加

- `電話番号_未登録` が目次に載っていなかったので追加（■ 作業用リスト）
- 新設した `分析_流入経路とLTV` を追加（■ 集計・分析）

### ③ 新規タブ `分析_流入経路とLTV`

2026年の全319件と顧客管理台帳を突き合わせた分析結果。**この会社のいちばん重要な数字です。**

- 入口ごとの施工数・平均LTV・リピート率
- マッチングPF経由はリピート率2%、自社直販は36%（観察期間を揃えた再計算つき）
- 公式サイト経由の新規は年4件・63,100円＝売上の0.5%
- マッチングPFの手数料は約35%・年間およそ95万円（備考から実測した60件）
- **広告の獲得単価の上限＝9,500円** とその根拠
- 数字の限界（規約の確認が必要な点など）も記載

元データと詳しい説明は GitHub の `docs/流入経路の分析.md`。

**変更していないもの**：過去の実績データ（施工記録・売上・氏名・住所）は一切触っていません。
既存タブの行の挿入・削除もしていません。追記と新規タブのみです。

## 2026-09-07（CMO）

- シートへの書き込みは**行っていない**。サービスアカウント鍵が再作成待ちのため。
- 反映予定の内容は `docs/TODO消込-2026-09-07.md` に確定させた（T002/T013/T015→完了、T012→進行中、T001→期限9/30、T016〜T019を追加）。
- TODO→カレンダー通知を終日予定から15分枠に変更する仕組みを `tools/todo-calendar.gs` として実装。まだシートには入れていない。
- 本日ぶんの渡辺さんへの指示は、佐々木さんのカレンダー 9/7 14:00-14:15 の予定として登録済み（渡辺さんをゲスト招待）。

### 2026-09-07 15:30〜 消込と架電シート作成（新しい鍵で再開）

バックアップ：`【バックアップ】売上顧客管理 2026-09-07 消込前`
`1gypDiF7eXnSDFwCCWLacNPr1zhAdT47kGsMbeXBggN0`

**TODOタブ（状態・完了メモ・期限のみ。他の列は触っていない）**

| 行 | ID | 変更 |
|---|---|---|
| 7 | T001 | 期限 09/08 → 09/30（ドメイン失効日に合わせた）＋完了メモに理由 |
| 8 | T002 | 未着手 → 完了（年末LP公開済み） |
| 18 | T012 | 未着手 → 進行中（KDDI申込済み・APIプラン再依頼中） |
| 19 | T013 | 未着手 → 完了（アンケート公開済み） |
| 21 | T015 | 未着手 → 完了（Drive API有効化済み） |
| 22-24 | T016-T018 | 新規追加（架電／gs設置／旧鍵の削除） |

**新タブ「架電シート_2026秋」（gid 1264147233）**

`冬季見込み客_2026` から読み取って生成。元タブは1文字も書き換えていない。
優先1〜3・電話番号あり・楽ラクーン経由を除く **108名**。

列：No／優先／かける番号／氏名／TEL／前回／これまでの施工／今回すすめる／
　　読み上げ文／結果／次回予約日／メモ／セグメント／流入経路／受注回数／累計売上

- 「読み上げ文」に、想起とアフターフォローを含んだ文を1行で用意（`docs/架電台本の原則.md` の型）
- 「かける番号」が ★本舗 の行は背景色つき。本舗の番号からかける
- 「結果」はプルダウン（予約／検討／不在／見送り／要対応／番号違い）。予約の行は緑になる
- 見出し6行と左5列を固定

**不具合と対処：電話番号の先頭の0が消えた**

`sheets_client.py` の write が `valueInputOption=USER_ENTERED` 固定だったため、
`09047110981` が数値と解釈され `9047110981` になっていた。
E列をテキスト書式にしたうえで先頭に `'` を付けて書き直し、**108件すべてを元データと突き合わせて一致を確認済み**。
再発防止として write に第4引数（RAW / USER_ENTERED）を追加し、注意書きを入れた。

### 2026-09-07 16:00〜 年間成績の参照ミスを修正（数字が3年ぶんずれていた）

バックアップ：上と同じ `1gypDiF7eXnSDFwCCWLacNPr1zhAdT47kGsMbeXBggN0`

**触ったのは「どのセルを見に行くか」の参照だけ。施工記録・売上の入力値には一切触っていない。**

#### 修正1：9〜11月の売上が「12月」を見ていた

`9月/10月/11月_支出/成績` の B55（売上）が、すべて `='12月_売上/顧客'!I3` を参照していた。
そのため9〜12月の4か月が同じ 125,160円 を表示していた。

| 月 | 修正前の表示 | 実際（月次タブ I3） | 差 |
|---|---:|---:|---:|
| 9月 | 125,160 | **353,010** | +227,850 |
| 10月 | 125,160 | **166,310** | +41,150 |
| 11月 | 125,160 | **339,600** | +214,440 |
| 12月 | 125,160 | 125,160 | 0（元から正しい） |

→ **年間の売上累計が 483,440円 過少に出ていた**（11月末時点 12,027,541 → 12,510,981）。

#### 修正2：年間成績の「営業利益」行が「販売管理費」と同じセルを見ていた

`年間成績` 29行目（営業利益）が、28行目（販売管理費）と同じ `B62` を参照していた。
1〜6月・8〜10月の9か月が該当。7月・11月・12月は元から正しかった。
※6月だけP/Lの行位置が違うため、参照先は B82。

| 月 | 修正前（＝販管費） | 修正後（本来の営業利益） |
|---|---:|---:|
| 1月 | 322,955 | 507,152 |
| 2月 | 518,007 | 88,460 |
| 3月 | 282,242 | 530,848 |
| 4月 | 355,510 | 1,082,276 |
| 5月 | 546,454 | 1,131,238 |
| 6月 | 684,311 | 1,295,433 |
| 8月 | 373,635 | 1,235,497 |

#### 修正3：12月の販売管理費が「1月」を見ていた

`年間成績` M28 が `='1月_支出/成績'!B62`。→ `='12月_支出/成績'!B62` に修正。

#### 検算

修正後、全12か月で **営業利益 − 融資返済38,059 = 経常利益** が成立することを確認した。

#### 波及

経常利益が変わるため、和真さんの理論配分（経常利益×45%）も9〜11月が変わる。
9月 61,193／10月 −125,507／11月 47,783。
ただし実際の支払いは最低保証30万円が入っているため、支給額そのものへの影響は要確認。

## 2026-09-08 打合せ資料タブの追加（MTG_2026-09-08）

**バックアップ：** 変更前にスプレッドシート全体をコピー済み
（`1lejpY6ZMxd9q00bIpA56oW-6OWKtoGXaClxy5CINb30`
「【バックアップ】2026_売上顧客情報管理_2026-09-08_MTG準備前」）。

### 変更12: `MTG_2026-09-08` タブを新設（目次の次）

17:00の対面MTG（議題：継続可否）用。**追加したタブはこの1枚だけ。**
既存タブへの変更は、下の「目次への追記」以外にない。既存の数値は一切書き換えていない。

構成

| 節 | 内容 | 出典 |
|---|---|---|
| ■1 | 会社の数字（1〜8月／9〜12月／通期見込み） | `年間成績` |
| ■2 | 9〜12月の受注は誰が取ったか（早期予約の比率） | `分析_件数単価流入_v2` |
| ■3 | 和真さんの報酬の推移と貸借 | `和真報酬_推移`・`(会社↔和真)貸借状況` |
| ■4 | 前回9/4に決めた21項目の実行状況 | `TODO`・カレンダー実データ |
| ■5 | 今日決めること 15件（決定はプルダウン） | — |

数字はすべて実データから取り、当日時点のスナップショットとして値で入れている
（`MTG_2026-09-04` と同じ扱い）。割合だけは表内の数式。

**この資料で示している事実**

- 1〜8月は予算比 94.5%。**9〜12月だけが 13.2%**（受注984,080／予算7,472,000）
- 9〜12月に入っている受注の **72.9% は渡辺さんが現場で取った次回予約**。
  HP・LINE・SMS・地図検索からの受注は9〜12月で0件
- 不足6,487,920円 ＝ 約166件。9〜12月の空き時間は約940時間（約188セット分）。
  **制約は需要側にある**
- 和真さんの報酬は経常利益×45%なので、受注が埋まらなければ最低保証30万円に張り付く。
  会社と和真さんの利害は一致している

**前回21項目の内訳：** 完了8／実施中・進行中5／一部2／未確認3／未実施・未着手3。

### 変更13: `目次` に3件追記

行を挿入しただけで、既存の行は書き換えていない。

| 行 | 追記 |
|---|---|
| 27 | `MTG_2026-09-08`（記録・アーカイブ） |
| 28 | `LINE_ログ`（記録・アーカイブ） |
| 36 | `架電シート_2026秋`（作業用リスト） |

## 2026-09-08 TODOの最新化と、アフィリエイト議題の追加

バックアップは同日の
`1lejpY6ZMxd9q00bIpA56oW-6OWKtoGXaClxy5CINb30` を引き続き使用（同日内の変更のため）。

### 変更14: `TODO` タブを最新化

**新しいタブは作っていない。** 既存行の状態・期限・完了メモを更新し、末尾に6件追記した。

状態を変えたもの

| ID | 変更 | 根拠 |
|---|---|---|
| T010 | 進行中 → **完了** | オーナー確認「SMSは渡辺さんのスマホからでも送れる。負担は電話の1/10」。一斉配信はKDDIへ引き継ぎ |
| T016 | 進行中 → **未着手** | 9/8 07:39 の渡辺さんの回答「1.B」＝未着手。本人の申告どおりに戻した |

期限を延ばした／前倒しにしたもの

| ID | 変更 | 理由 |
|---|---|---|
| T001 | 9/30 → **9/15** | 期限がドメイン失効日と同じで意味をなしていなかった。アフィリエイトはNetlifyのURLで申込済みのため、DNSは開始の前提ではなくなった |
| T009 | 9/8 → 9/15 | 未取得のまま |
| T019 | 9/8 → 9/12 | **未反映を確認した。** 9/7 19:06 の follow イベント（グループID空）がログに残っているため、`kyokaGroupId` の絞り込みはまだ効いていない |

追記したもの

| ID | 担当 | 期限 | 内容 |
|---|---|---|---|
| T021 | 渡辺 | 9/8 | カレンダーを「予定の変更権限」で佐々木さんに共有（本日のMTGで1分） |
| T022 | 佐々木 | 9/10 | 予約フォームのApps Scriptをデプロイし `/exec` URLを共有 |
| T023 | 佐々木 | 9/12 | レントラックスへ申込書・出稿条件・ヒアリングシートを提出 |
| T024 | 佐々木 | 9/19 | 計測タグをLPに設置し、テスト申込で計測を確認 |
| T025 | CMO | 9/19 | アフィリエイト掲載用バナーの作成 |
| T026 | 佐々木 | 9/26 | 成果承認作業の運用を決める |

### 変更15: `MTG_2026-09-08` にアフィリエイトの議題と要点を追加

議題を4件（#16〜#19）追記し、資料の要点を **■6** として足した。既存行は書き換えていない。

議題
- #16 開始時期の期待値（先方が「初月に一気に取れない」「件数コミット不可」と明記）
- #17 成果単価3,000円で足りるか（平均単価52,374円に対し5.7%。CPA上限9,500円なので余力あり）
- #18 成果承認作業の担当と頻度（**期限切れで自動承認される**ため放置できない）
- #19 計測タグとバナーの担当（LPは別スレッドが触っているため要調整）

詳細は `docs/アフィリエイト-レントラックス.md`。

### 変更16: `MTG_2026-09-08` に議題4件（#20〜#23）を追加、■6を最新化

オーナーから共有されたドキュメント（レントラックスからのメール全文）を読んで追加した。
判明したこと：**受付番号15721**、**電子署名は2026-09-08に対応済み**、
残りの提出物は①出稿条件②ヒアリングシートの2点。

追加した議題
- #20 指名検索での出稿を許すか（空欄だと自社の指名流入に1件3,000円を払うことになる）
- #21 電話で申し込まれた分の扱い（アフィリエイトはWebフォームしか計測できない）
- #22 出稿条件・ヒアリングシートの記入内容（訴求と禁止事項）
- #23 公式サイトの電話番号が旧番号（080-1755-7275）のまま

**フォームのURL（契約番号のハッシュ付き）と電子署名のアクセスコードは、
シートにもリポジトリにも書いていない。** 共有ドキュメントを参照する形にした。

## 2026-09-08（夜）送信シートの再設計と、その途中で起こした事故

**バックアップ：** 作業前に `1dtH3nf41i5BJD-JSq_SmGvUaXdT4BMRds547X_Gprog`
（【バックアップ】2026_売上顧客情報管理_2026-09-08_送信シート再設計前）を作成。
**このバックアップから実際に復旧している。**

### ★ 事故と復旧：冬季見込み客_2026 の電話番号を一度消した

`tools/build-sms-list.py` が列を**位置（A列=優先、F列=TEL…）で読んで**いた。
1回目の実行で列の並びを変えたあと、**同じスクリプトを2回目に実行したとき、
並べ替え後のシートを元の並びとして読んでしまった。**
その結果、TELとして空欄を読み、957行すべてが「SMS不可」になり、
電話番号805件が失われた。

**復旧：** 上記バックアップをサービスアカウントに共有し、
`冬季見込み客_2026` の A1:U1000 を丸ごと書き戻した。805件の番号を復旧済み。

**再発防止：** スクリプトを**見出しの名前で列を探す**方式に変えた。
新しい並びでも古い並びでも同じ意味の列を拾えるようにし、
何度実行しても壊れないようにした。

> **教訓。** 自分で並びを変えるスクリプトを、位置決め打ちで読んではいけない。
> 1回目は成功し、2回目に壊れる。バックアップが無ければ復旧できなかった。

### 変更17: `冬季見込み客_2026` をスマホでの送信作業用に組み替え

オーナー指示。和真がスマホでシートとSMS画面を往復する前提。

A優先／B顧客名／C電話番号／D▶送る（sms:リンク）／E送信する本文／F送信済み（プルダウン）／
G返信メモ／H送信日の目安／I以降 これまでの情報（消していない）。

- 本文は1人ずつ差し込み（前回メニュー・最終施工日・今回おすすめ）。488名ぶん作成
- 名乗りを系統で出し分け（自社＝ワンヒッター／本舗＝おそうじ本舗江戸川中央店）
- スケジュールマッチング経由は除外をやめ、**和真がF列で「対象外」を選ぶ運用**へ
- F列・G列に和真が入れた内容は、スクリプトを再実行しても引き継ぐ

### 変更18: ワンヒッター（自社）のSMS本文を、オーナー支給のものに差し替え

2026-09-08 オーナーから自社用のSMSフォーマットの支給を受けた。
`data/sms-template.txt` を丸ごと入れ替え、`冬季見込み客_2026` の
「送信する本文」列を **488名ぶん作り直した**（自社220／本舗268）。

変更前のバックアップ：
**`【バックアップ】2026年売上スプシ 2026-09-08 SMS本文差し替え前`**
（`13ZDKDURxbanOXgEKV3KiOUzn7Fep2We-LbNQpg8d6bw`）

支給された文面のために、差し込みの目印を2つ足した。

| 目印 | 中身 |
|---|---|
| `"前回施工(クリーニング除く)"` | 「浴室クリーニング」→**浴室**、「お掃除機能付きエアコンのクリーニング」→**お掃除機能付きエアコン**。「その後、○○は快適に使用できていますでしょうか」と続けるため |
| `"予約フォーム"` | 既存客向け予約フォームのURL（オーナー決定の選択肢B） |

**誘導先を申込フォーム（年末LP）から予約フォームへ切り替えた。**

#### 個人と法人で1語だけ出し分ける（こちらの判断。要否は確認してください）

支給文面の「**ご自宅**に気になる汚れがありましたら」を `"ご自宅"` という差し込みにした。

- 個人のお客様 … 「ご自宅」。**支給された文面と1文字も変わらない**
- 法人のお客様 … 「店舗・オフィス」

自社の送信可220名のうち**11名が法人**（三井不動産、ふじたメンタルクリニック、
小澤建設、長谷工エリアルエステートなど）で、そのまま出すと
「ご自宅に気になる汚れが」となるため。戻すのは1行の差し替えでできる。

#### スクリプト側の変更（`tools/build-sms-list.py`）

- `--dry-run` を追加。シートに書かずに、できあがる本文だけ確認できる
- **予約フォームが受付できる状態かを、本番ページを見て確かめる**ようにした。
  `"api": ""`（Apps Scriptの `/exec` 未設定）なら、シート2行目に
  **「【送信は保留してください】」**を出す。いまはこの状態
- 集計に「70文字超の件数」「名乗りの行が落ちた件数」を追加

### 変更19: 台帳の表記ゆれが、そのままお客様の本文に出ていたのを直した

変更18のあと、488件を全数チェックして見つかったもの。**22件が崩れていた。**

| 台帳の表記 | 直す前（お客様に届く文） | 直したあと |
|---|---|---|
| `空室` | その後、**空室**は快適に使用できていますでしょうか？？ | **この行ごと消す**（下記） |
| `洗濯機(ノーマル)` | **洗濯機(ノーマル)**は快適に使用できていますでしょうか？？ | 洗濯機 |
| `追焚配管` | **追焚配管**のクリーニングを担当させて… | 追い焚き配管クリーニング |
| `青山リアルティ様（田辺様）` | **青山リアルティ様（田辺様）さま** | 青山リアルティさま |

**「空室」は「その後、○○は快適に使用できていますでしょうか」が成り立たない。**
退去後の原状回復なので、住んでいる人がいない。差し込みを空にして、
その行だけを丸ごと落とす（ひな形の「空だった行は消える」ルールをそのまま使う）。
「空室クリーニングを担当させていただきました」の行は残るので、
何のお客様かは伝わる。該当10件（小澤建設・青山リアルティ・澁木・杉田ほか）。

姓の取り出し（`sei()`）も直した。括弧の補足を落としてから、
末尾の敬称（様・さま・さん・殿・御中）を外す。

**残っている粗（台帳側の問題。和真の目視で拾う）**

- `ピタットハウス案件 松本 笑子` → 「ピタットハウス案件さま」（1件）
- `江戸前ハーブ　タカラサービス` → 「江戸前ハーブさま」（1件）

### 変更20: 空室クリーニングのお客様を送信対象から外した

2026-09-08 オーナー指示「空室は送らないよ」。

`施工メニュー（内訳）` の先頭が `空室` の行を **除外**（理由列に
「空室クリーニングのお客様（オーナー指示）」）。**29件**。
うち送信可だったのは10件で、残り19件はもともと電話番号の都合で送れない行。

先頭ではないが空室を含む行が7件あるが、送信可はこの1件だけ
（`まるごと(備考に内容)×1／エアコン(ノーマル)×1／空室×1`）。
前回施工がまるごとクリーニングなので、本文は空室の話にならない。

**あわせて、氏名が壊れている行を保留にした。**
姓を取り出した結果が「ひらがな・カタカナ1文字」なら送らない。
台帳の「ご　さん」が「ごさま」で送られるところだった。現在3件。

集計：送信可 **475**（自社211／本舗264）／除外303／SMS不可176／保留3

### 変更21: 予約フォームの受付機能を復旧。TODO T039〜T042 を追加

2026-09-08 オーナー指示「コンペで対応できないから君が直して」。

**原因**：Netlifyの予約サイトが **`ignore_html_forms: true`**（HTMLフォームの検出オフ）
だった。そのため `data-netlify="true"` が配信後も残り、フォームの受け口が作られず、
POSTが通らなかった。LPサイト（one-hitter-lp）は `false` で、こちらは動いていた。

**年末LP（one-hitter-nenmatsu）も `true`。** 9/8に見つけた
「POSTが404／thanksが404」の正体はこれ。当該URLはリダイレクトで畳む方針なので
そのままにしてある（LPスレッドへ連携済み）。

対応
- サイトの `processing_settings.ignore_html_forms` を `false` にして再配信 → 登録された
- 実POSTで200・全12項目の着信・管理画面での受信を確認。テスト送信は削除済み
- Apps Scriptに依存しない構成へ変更（空き枠はslots.json、申込はNetlifyフォーム）

TODOに追加

| ID | 担当 | 内容 |
|---|---|---|
| T039 | 和真 | 予約フォームを自分のスマホで実機テスト（9/9） |
| T040 | 和真 | 今月の夜勤日を「21:00〜翌5:00」の形でカレンダーに入れる（9/9） |
| T041 | マーケ部長 | 予約フォームの受付機能を復旧（完了） |
| T042 | マーケ部長 | 空き枠の1時間ごと更新（定期実行の作成許可待ち） |

### 変更22: 空き枠の自動更新を毎時に設定（T042 完了）

オーナー許可を得て定期実行を作成（`trig_01V94sJkoyZMRdNMnB3iEXen`／**毎時42分**）。
1回の起動でやること：

1. 和真さんのカレンダーを読み直して `data/calendar/watanabe-events.json` を作り直す
   （★氏名・住所・金額は書かない。時刻だけ）
2. `build-slots.py` → `build-booking.py` → `deploy-booking.py`
3. 配信後のHTMLに `data-netlify` が残っていないか確認（残っていたらフォーム未登録）
4. `booking-inbox.py` で届いた申し込みを `予約_Web` に取り込み、グループLINEへ通知
5. カレンダーに【仮】予定を作る
6. **変化が無ければ何もしない**（報告もLINEも出さない）

### 変更23: 【失敗】和真さんの返信を見ずに送信した。仕組みで止めるようにした

2026-09-09 15:08 に和真さんから返信があったのに、15:44 に見ずに次を送った。
返信には**「完了画面をスクショするか残るものがないと、いつに予約したっけとなる」**
という指摘が入っていたのに、それに答えないまま「問題なしです」と送った。

**原因**：テスト成功の判定を Netlify の着信だけで行い、LINE_ログ を見に行かなかった。
14:43 には見ていたが、送信直前には見ていない。
「送る前にログを見る」を手順に入れていなかった。

**再発防止**：`tools/line_client.py` に未読チェックを入れた。
`LINE_ログ` の H列「CMO確認」が空の受信があるあいだは `push` できない。
読んだら H列に日付を入れるか `--midoku-ok` を付ける。**手順書ではなくツールで止める。**

### 変更24: 完了画面に控えとカレンダー登録を追加（和真さんの指摘）

予約したのに手元に何も残らないと、お客様が不安になって電話をかけてくる。
電話の往復を減らすためのフォームなのに、逆に電話が増える。

- **ご予約内容（控え）** … 日時／内容／所要／概算金額／お名前／ご住所／お電話／担当
- 「スクリーンショットで保存を」の一文
- **Googleカレンダーに登録するボタン**（URLで開くのでダウンロード不要）

カレンダーの時刻は、**端末のタイムゾーンに頼らず日本時間（UTC+9）で固定**した。
最初の実装は端末任せで、時刻設定がずれた端末では違う時刻が入るところだった。

### 変更25: 自社210名の送信を開始。本舗264名は「保留」で止めた

2026-09-10 オーナー判断。

**自社（ワンヒッター）210名 → 送信開始。**
・1日20件、9/21ごろまで／シートの上から順
・「▶送る」が効かない場合は **E列の本文をコピーして送る**運用で進める
  （オーナー判断：「エラーが生じた場合でも手動(コピペ)で対応してもらおう。
    その場合は送信ボタンは追って直してね」）
・送信時間帯は **10:00〜19:00 のみ**。早朝・夜間は送らない
  （和真さんは夜勤前後に作業しがちなので、LINEで強く念押しした）

**本舗264名 → 保留。**
`data/sms-template-honpo.txt` を空にした。空の系統は本文もSMSリンクも作られず、
シート上「保留」になるので、誤って送ることができない。

たたき台を `data/sms-template-honpo-draft.txt` に用意した。
オーナー指示（2026-09-10）：**「導線の違いに注意。あくまでSMSへの返信or電話をもらう経路」**
- URLを一切載せない（予約フォームも申込フォームもワンヒッターのものなので使わない）
- 名乗りは「おそうじ本舗の渡辺」、署名は「おそうじ本舗 江戸川中央店」
- 料金には触れない。箇所の提案だけ
- 導線は「このSMSへの返信」と「本舗の電話番号（080-1344-3137）」の2つ

現在の集計：**送信可210（自社）／保留268（本舗265・氏名確認3）／除外303／SMS不可176**

### 変更26: 本舗265名の文面をオーナー支給の確定版に差し替え、送信可にした

2026-09-10 オーナー支給。**本文はオーナーが書いたもの。勝手に言い回しを変えない。**

`data/sms-template-honpo.txt`（それまで空＝保留）に確定版を入れた。空でなくなったので
265名が「保留」から「送信可」に変わった。

**導線はSMSへの返信と 080-1344-3137（渡辺）の2つだけ。URLは一切載せていない。**
署名は「おそうじ本舗　江戸川中央店／渡辺　和真」。265件すべてで確認済み。
本文にワンヒッターの社名・URL・受付番号が混ざっていないことも265件すべてで確認した。

**おすすめメニューを自動で決める仕組み（`teian()`）は取りやめて削除した。**
オーナー判断：「オススメメニューの構想は複雑化（エラー確率上昇）するから一旦取りやめる」。
本舗の文面では、9〜11月の季節提案（エアコン／水まわり／追い焚き配管）を
**全員に同じ固定文**で出している。季節が変わったらオーナーに確認して書き換えること。

**あわせて入れた歯止め（`tools/build-sms-list.py`）**

1. **名乗りの行が落ちた本文は送らない。**
   「"施工時期"に"前回メニュー"を担当させていただきました、…の渡辺でございます」の行は、
   台帳に最終施工日か施工メニューが無いと丸ごと落ちる。落ちると**誰から届いたのか
   分からないSMS**になる。該当2名を「保留」に変えた（台帳を埋めれば送信可に戻る）。
2. **本舗の本文にワンヒッターのものが混ざっていたら、その行を保留にする。**
   目視ではなくツールで止める。
3. **「1年以上経過」に当てはまらない人数を必ず出す。**
   文面が「前回のクリーニングから1年以上経過しております」と言い切っているため。

**★未決：本舗265名のうち63名は経過が1年未満**（8〜11ヶ月台）。文面と事実が合わない。
オーナーの判断待ち。和真さんは自社208名（9/21ごろまで）を先に送るので、当面は影響しない。

**バックアップ**：作り直す前のタブの中身を `data/backup/` に控えた。
このタブは A1:Y1000 を消してから書き直すので、**和真さんが手で入れたF列「送信済み」と
G列「返信メモ」は控えにしか残らない**。取り方は `data/backup/README.md`。

現在の集計：**送信可473（自社208・本舗265）／保留5／除外303／SMS不可176**

### 変更27: ▶送るが効かない原因を突き止めて直した／駐車場の実費を明記した

2026-09-10 和真さん報告：
「1.確認したよー 駐車場がない場合実費でご請求になる旨も付け加えておいてくれると助かる
　2.送るタップで開かないから手打ちで送るねー」

**① ▶送るが効かなかった原因**

スプレッドシートの `HYPERLINK()` が開けるのは **http / https / mailto だけ**。
`sms:` は対象外なので、タップしても無反応になる。端末の問題ではない。

**直し方**：https のページを1枚はさむ。`lp/booking/s.html` を作って、
D列のリンクを `https://one-hitter-booking.netlify.app/s.html#to=…&b=…` に変えた。
このページが `sms:` を組み立てて開く。

- **iPhone は `sms:番号&body=…`、Android は `sms:番号?body=…`**。区切りが違う。
  取り違えると本文が入らないか、そもそも開かない。UserAgentで振り分けている
- 開かなかったときのために、**本文のコピーボタンと全文の表示**を同じページに置いた
- **★お客様の番号と本文は「#」より後ろ（フラグメント）に入れている。**
  フラグメントはサーバーに送られないので、Netlifyのアクセス記録に
  お客様の電話番号も本文も残らない。**「?」に変えないこと**
- iPhone/Android 両方のUserAgentで実際に開いて確認済み（区切り・本文・宛先）
- `noindex` を入れてある。社内用のページ

**② 駐車場の実費を予約フォームに明記した（3か所）**

「お車を停められる場所が無い場合は、近くのコインパーキング代を実費でご請求いたします」

- ご要望欄の下のヒント
- 概算金額のただし書き
- 完了画面の控え（お客様の手元に残るのはこれだけなので）

金額を先に見せているので、あとから駐車場代が乗ると「聞いていない」になる。
先に書いておけば現場で揉めない。

**③ 和真さんの実機テスト（2件同時予約）が通った**

9/14 14:30・エアコン（お掃除機能付き）＋レンジフード・210分・概算32,560円・
ご要望「駐車場なし」。全項目が正しく届いた。テスト送信はNetlify側から削除済み。

**④ 手打ちはしないよう伝えた**

「手打ちで送るねー」とあったので、E列のコピーに戻してもらうようLINEした。
1通260〜410文字あり、一人ずつ前回の時期とメニューが入っている。
打ち直すと**別のお客様の内容が混ざる**。

### 変更28: 「▶送る」を数式のリンクから「本物のリンク」に変えた

2026-09-10。変更27で https 経由にしたのに、和真さんの端末ではまだ開かなかった。
スクリーンショットで原因が分かった。

**タップすると「カット／コピー／ペースト」が出るだけだった。**
つまりリンクとして扱われていない。

**本当の原因**：D列を `=HYPERLINK("…","▶ 送る")` という**数式**で書いていたこと。
**iPhoneのスプレッドシートアプリは、数式で作ったリンクをリンクとして扱わない。**
セルが選ばれるだけで、開きようがない。URLが http か sms かは関係なかった。

**直し方**：数式をやめて、セルの文字そのものにリンクを付ける
（Sheets API の `updateCells` ＋ `textFormatRuns` の `link`）。
D列の値はただの文字列「▶ 送る」で、そこにリンクが乗る。474件すべてに付け直した。

- 書いたあと Sheets 側は、リンクが文字列全体にかかっているときは
  `userEnteredFormat.textFormat.link` に置き直す。
  **`textFormatRuns` だけを読んで「付いていない」と早合点しないこと**（一度やった）
- 1リクエストに詰め込みすぎないよう200行ずつに分けている
- 書式の `repeatCell` がリンクを消さないよう、**書式をつけた後**にリンクを付けている

**シートの2行目の説明も書き換えた。**
「D列の「▶ 送る」をタップ → 出てきたリンクを開く → SMSが宛先と本文入りで開く」

**なお、手打ちの件は誤解だった。**
和真さんは最初からE列をコピーして貼っていた（「手打ちは伝え方が悪かったです」）。
LINEで訂正済み。開かないときは今のコピー＆ペーストを続けてもらう。
送信の手が止まる方が損失が大きい。

### 変更29: 「▶送る」が2人目から更新されない不具合を直した

2026-09-10 和真さん報告：
「一つ開いて次のを開いても前の情報のまま更新されずタブを閉じて再度開くと飛ぶんだけど」

**原因**：`s.html` は宛先と本文を「#」より後ろ（フラグメント）で受け取っている。
**ブラウザは「#」より後ろだけが変わったとき、ページを読み込み直さない。**
読み込み時に1回だけ動くスクリプトだったので、2人目以降は前の人の内容が残っていた。
タブを閉じて開き直すと直るのは、そのとき初めて読み込みが起きるから。

**直し方**：`hashchange` でも同じ処理をやり直すようにした。
あわせて、宛先の下に**本文の1行目（「〇〇さま」）**を出すようにした。
次の人に変わったことが一目で分かる。取り違えの歯止めにもなる。

iPhoneのUserAgentで、1人目 → ハッシュだけ変更 → 2人目、と実際に動かして確認済み。
宛先・本文・sms:リンクのすべてが2人目のものに入れ替わる。

**この日の進捗**：和真さんは40件まで送信。発熱のため中断。

### 変更30: 本舗SMSの「1年以上経過」を、1年未満の方には出さないようにした

2026-09-10 オーナー判断：「その1行だけを1年未満の人には出さない、という扱いでOK」。

本舗265名のうち**63名は経過が1年未満**（8〜11ヶ月台）で、
「前回のクリーニングから1年以上経過しております」が事実と合わなかった。

**直し方**：その一文を差し込みの目印
`"1年以上経過のひとこと"` に置き換えた。
台帳の経過(月)が12ヶ月未満なら空になり、**空だった行はもともと行ごと落ちる**仕組みなので、
新しい判定は増えていない（空室クリーニングの行を落としているのと同じ仕組み）。
**他の文面は1文字も変えていない。**

落ちたあとはこう読める：
```
その後、お掃除機能付きエアコンは快適に使用できていますでしょうか？？

9-11月にかけては、
・夏場フル稼働していたエアコンを冬に向けて綺麗にする
```

**歯止め**：作り直すたびに本文を1件ずつ見て、
**経過1年未満の方の本文に「1年以上経過しております」が入っていないか**を確かめる。
入っていたら「★★ 送信を止めてください」と出す。
シート側でも265件を検算した。1年未満で一文が入っているもの0件、1年以上で一文が無いもの0件。

あわせて、落ちた側の文面も1件かならず表示するようにした。文のつながりを目で見るため。


## 2026-09-11 変更31: 冬季見込み客_2026 ― 配信停止の案内を追加／流入経路が本舗系の自社行を保留

バックアップ：`data/backup/冬季見込み客_2026-20260910-2350.json`（作り直す前の控え）

**1. 両系統の本文の末尾に、受信拒否の通知先を1行足した**（顧客接点担当の指摘 20260910-11-cmo）。
特定電子メール法4条は、送信者名と「受信拒否の通知を受ける連絡先」の表示を求める。
オーナー支給の文面には送信者名はあったが、後者が無かった。
- 自社：`今後のご案内が不要でしたら、その旨ご返信ください。`
- 本舗：`今後のご案内が不要でしたら、その旨ご返信いただくか、080-1344-3137までお知らせください。`
他の文面は1文字も変えていない。送信済み34件は文面を書き換えていない（送った記録のまま）。

**2. 流入経路が本舗系（本舗／本舗(ロイ)／チラシ(本舗)）なのに送信系統が自社の行を、未送信に限って保留にした。**
該当は送信可のうち119名（うち6名は送信済み → そのまま）。113名を保留、理由列に
「どちらの名乗りで送るかオーナーに確認中」と書いた。
「本舗顧客には本舗として営業する」（2026-09-08 オーナー指示）に照らすと、
この行にワンヒッター名義で送るのが正しいか判断がつかないため、確認する側に倒した。
送信系統列がどの規則で入ったかは記録が残っておらず、主な流入経路とは一致していない
（流入経路=本舗 145名のうち 送信系統=本舗 は17名だけ。逆に リピート・HP でも本舗になっている行がある）。
オーナーの回答が出たら `tools/build-sms-list.py` の `HONPO_KEIRO` の判定を外すか、送信系統を直して作り直す。

作り直し後：送信可 360（自社95・本舗265）／保留118／除外303／SMS不可176。F列「送信済み」34件は保持。


## 2026-09-11 変更32: 冬季見込み客_2026 ― 送信系統を「最新の施工の名義」で決め直し（144行）、保留113名を解除

バックアップ：`data/backup/冬季見込み客_2026-20260911-0130.json`

**オーナーの決まり（2026-09-11）**：「経路については最新が ONE-HITTER になっていれば、ONE-HITTER へ転換が成功している顧客」。
→ 送信系統 ＝ **最新の施工の名義**（台帳の「売上種類」列。2023年は先頭の「流入経路」列が名義）。One Hitter なら自社、本舗／本舗(ロイ) なら本舗。

**変更31で保留にした113名の正体**：ほぼ全員が2023年にしか施工の無いお客様で、2023年の台帳には「売上種類」列が無い。
送信系統を最初に作ったとき、名義が読めない行を自社に倒していた。2023年の先頭列（流入経路）が実は名義（本舗／One Hitter）で、
そこを読むと **104名は本舗名義が最新 → 本舗**、9名は One Hitter が最新 → 自社（転換成功）。

`tools/derive-soushin-keitou.py` を追加（決まりをコードに残す。`--cache` で48タブの読み込みを控える。`--write` で書く）。

| 変更 | 行数 | 内訳 |
|---|---:|---|
| 自社→本舗 | 121 | 保留104（→本舗の送信可へ）／SMS不可13／除外1／**送信可・送信済み3**（自社の文面で送ってしまった。取り消せないので記録のみ） |
| 本舗→自社 | 23 | 送信可11（本舗名義の予定だったが最新は One Hitter → 自社の文面へ）／SMS不可6／除外6 |
| 決められない（名義の記録なし） | 22 | 送信可には含まれない。変えていない |

`tools/build-sms-list.py` の一時的な保留規則（`HONPO_KEIRO`）は外した。
作り直し後：**送信可 473（自社 113＝未送信82＋送信済み31・本舗 360＝未送信357＋送信済み3）**／保留 5（氏名の表記・名乗り不能）／除外 303／SMS不可 176。F列「送信済み」34件は保持。


## 2026-09-11 変更33: 冬季見込み客_2026 ― 名義の照合ガードを入れて作り直し（再発防止）

バックアップ：`data/backup/冬季見込み客_2026-20260911-0145.json`

オーナー指示（LINE 10:39）「名乗りミスは重大。絶対に再発させない防止策を立てて、他のエージェントにも遵守させる仕組みに」。

- `tools/build-sms-list.py` が、送信系統を**台帳の最新の施工の名義**（`tools/derive-soushin-keitou.py` の `meigi_hyou()`）と1行ずつ照合するようにした。
  食い違う行・名義の記録が無い行は **送信可にならない**（送信済みの行は記録として触らない）。
- 文面も両方向で検査：自社の文面に「ワンヒッター」の名乗りが無ければ保留、本舗の文面にワンヒッターの名前・URL・受付番号が混ざれば保留（後者は従来から）。
- 名義の控えは `~/.cache/one-hitter/meigi-cache.json`（gitの外。12時間で読み直し）。

作り直し後：**送信可 468（自社 108＝未送信77＋送信済み31・本舗 360）／保留 10**（名義の記録なし5・名乗り不能2・氏名の表記3）。
変更32から新たに5名が保留になった（台帳に One Hitter／本舗 の名義が無い方）。
