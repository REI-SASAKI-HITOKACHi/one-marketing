# 2026_売上/顧客情報管理 シート 変更履歴

対象: `2026_売上/顧客情報管理`（ID `1TK70pwQ8lYmjxUVCfFp1E2T5qDjHOnD4XSviZzUpB64`）
実施方法: Sheets API（サービスアカウント `claude-sheets@one-hitter-sheets.iam.gserviceaccount.com`）

---

## 2026-09-04 記録欄の改善

**バックアップ**: `【バックアップ】2026_売上/顧客情報管理 2026-09-04 改修前`
（`1l6XZPMXsPd5CA3mUg1VblvgfxISt1gxQ8aEIN4LeNY8` / オーナー名義でDrive上に作成済み）

対象範囲: `1月_売上/顧客` 〜 `12月_売上/顧客` の 12タブ、4行目〜504行目

### 変更1: 流入経路（D列）の選択肢を 14 → 19 に拡張

**発見した不具合**: プルダウンが `strict=True`（リスト外を拒否）にもかかわらず、
選択肢に **「紹介」が入っていなかった**。実データには紹介が11件（¥379,460・平均¥34,496）あり、
平均単価の高い経路が正規の手段では入力できない状態だった。

| | 選択肢 |
|---|---|
| 既存14（すべて維持） | 営業／業務提携／チラシ(OH)／チラシ(本舗)／LINE／HP／SMS／早期予約／リピート／スケジュールマッチング／入札案件／その他／楽ラクーン／おそうじ定期便 |
| 追加5 | **紹介**（不具合対応）／**LP(アフィリエイト)**／**地図検索**／**LINE/SMS再販**／**Google口コミ** |

追加4つは、今後のネット施策の成果を台帳で追えるようにするため。これがないと
LP・地図検索・再販の受注が「その他」に埋もれて効果測定できない。

### 変更2〜4: L・M・Q列をチェックボックスから選択式へ

| 列 | 項目 | 変更前 | 変更後の選択肢 |
|---|---|---|---|
| L | 早期予約提案 | チェックボックス | 未記録／未提案／提案済／成約／対象外 |
| M | フォローコール | チェックボックス | 未記録／未実施／SMS送信／LINE送信／電話／返信あり／再依頼 |
| Q | ＯH打診 | チェックボックス | 未記録／未打診／打診済／反応あり／成約／対象外 |

**理由**: TRUE/FALSE では「打診したが反応がなかった」と「そもそも対象外だった」を区別できない。
打診は現場が客ごとに判断する属人的な行為なので、結果まで残らないと何が効いたか分からない。

**既存の値の扱い**: 全行が `FALSE`（＝チェック未入力）だった。オーナー確認により
これは「実施していない」という記録ではなく単なる記録漏れであるため、
**同じ意味の「未記録」に置き換えた**。値の意味は変えていない。
埋め戻し作業は発生せず、無効値の警告マークも出ない。

置き換えたセル: L 313件 ／ M 314件 ／ Q 323件（施工日付のある行のみ）

### 作業中に起きたことと、その修正

チェックボックスは**データのない空行にも敷かれていた**ため、最初の一括置換で
約6,000セル×3列が「未記録」になった。施工日付のない行をすべて空欄に戻して修正済み。
最終状態は施工日付のある323行にのみ値が入っている。

### 触っていないもの

- 施工日付・売上・氏名・TEL・住所・メニュー・備考などの実績データ
- 既存の「売上分析」タブ（壊れているが未着手）
- 過去年（2023・2024・2025）のシート
- **7月_売上/顧客 の49行目** … 売上種類「本舗」／流入経路「楽ラクーン」／メニュー「エアコン(ロボ)」が
  入っているのに施工日付と売上が空欄。元からある不完全な行のため、そのまま残した。**要確認**

## 元に戻したいとき

バックアップファイルを開き、該当タブをコピーして差し替える。
プルダウンだけ外す場合は、範囲を選択 → データ → データの入力規則 → すべて削除。

## 2026-09-04 分析タブの作り直しと再販リストの新設

### 変更5: `分析_件数単価流入_v2` タブを新設

既存の `分析_件数単価流入` タブは**削除していない**。数字を突き合わせてから、どちらを使うか判断できる。

旧タブで確認した不具合：

| 不具合 | 内容 |
|---|---|
| 9月が丸ごと欠落 | 月別リストが 1,2,3,4,5,6,7,8,10,11,12月 のみ |
| 「早期予約」が存在しない | 流入経路の項目がなく、リピートに合算されていた |
| `#REF!` 残存 | L1セル |
| 集計全体が古い | 旧310件/¥12,283,131 に対し実データは **323件/¥12,636,141**。9月以外の月もズレていた |

新タブは **月次タブをSUMIFS/COUNTIFSで直接参照する数式**で組んだ。行を追記すれば自動反映され、
ピボットの更新漏れで数字が古くなることが構造的に起きない。

構成：月別サマリ／流入経路別サマリ（19経路・構成比つき）／月×経路の売上クロス／月×経路の件数クロス

直った結果：

| 項目 | 旧 | 新 |
|---|---|---|
| リピート | 127件 / ¥4,374,773 | 69件 / ¥3,130,845 |
| 早期予約 | （項目なし） | **68件 / ¥1,510,138** |
| 9月 | （行ごと欠落） | 9件 / ¥353,010 |
| 年計 | 310件 / ¥12,283,131 | **323件 / ¥12,636,141** |

### 変更6: `再販リスト` タブを新設

顧客ごとに最終施工日をまとめ、メニュー別サイクルで案内時期が来た人に **★いま** を付ける。
サイクル：おそうじ定期便・定期清掃=1ヶ月／トイレ・洗面台=6ヶ月／その他=12ヶ月。推奨時期の2ヶ月前から ★。

生成結果（2026年シートのみ）：ユニーク顧客 **258名**／2回以上の利用 40名（15.5%）／**★いま 2名**

**★が2名しか出ないのは、2026年シートしか見ていないため。** 2026年に施工した顧客は
まだ1年経っていないので当然そうなる。本当の再販プールは **2025年シートの444名**で、
うち9〜12月に施工した160名がちょうど今「1年後」を迎える。
→ **2025年シートをサービスアカウントに共有すれば、300名規模のリストが作れる（共有クリック1回）**

## 未着手

- 2025年シートを対象に含めた再販リストの再生成（上記のとおり、共有待ち）
- 旧 `分析_件数単価流入` タブの扱い（数字を確認後、削除するかどうかの判断）

## 2026-09-05 打合せシート・冬季見込み客・TODO再構成

2023〜2025年のシートも共有いただいたため、4年分（**1,488件・¥52,300,292**）を横断できるようになった。

### 変更7: `MTG_2026-09-04` タブを新設（先頭）

施工責任者との打合せ用。議題20件（スケジュール／キャパ／写真／連携／台帳／年末／提携）。
「決定」列はプルダウン（決定・保留・対象外・要相談）、決定内容の記入欄つき。

### 変更8: `冬季見込み客_2026` タブを新設

4年分の全施工から顧客を名寄せし、**ユニーク965名**を抽出。冬季施策の優先度でセグメント化。

| セグメント | 人数 | うち個人 | 累計売上 | 定義 |
|---|---|---|---|---|
| A_昨年同期 | 80 | 73 | ¥3,977,531 | 2025年9〜12月に施工（ちょうど1年） |
| B_冬季常連 | 28 | 23 | ¥5,829,724 | 過去に2回以上、9〜12月に施工 |
| C_サイクル到来 | 607 | 572 | ¥24,231,669 | 最終施工から10ヶ月以上 |
| D_リピーター | 42 | 38 | ¥4,536,420 | 2回以上利用 |
| E_その他 | 51 | 46 | ¥1,675,590 | 上記以外 |
| Z_直近施工 | 157 | 152 | ¥12,049,358 | 直近3ヶ月以内。今回は送らない |

**配信対象（Z除く）808名／うち個人752名。自社系統393名・本舗系統415名。TELあり687名。**

列に「送信系統（自社/本舗）」「法人/個人」「今回おすすめ（前回と違う場所を提案）」「送信済み」を持たせ、
配信の実務でそのまま使える形にした。「送信済み」はプルダウン（第1波/第2波/第3波/返信あり/予約/対象外）。

### 変更9: `TODO` タブを再構成

旧タブは番号とFALSEのチェックボックスだけで、実質未使用だった。

**設計方針：担当者が触るのは B列「状態」のプルダウン1箇所だけ。** それ以外はCMO側で埋める。

| 列 | 内容 | 誰が入れる |
|---|---|---|
| A | ID（T001〜。カレンダー予定と対応） | CMO |
| B | 状態（未着手/進行中/完了/保留/対象外） | **担当者・1クリック** |
| C | 期限 | CMO |
| D | 担当 | CMO |
| E | やること | CMO |
| F | なぜ（効果）— 数字で書く | CMO |
| G | 手順・補足 | CMO |
| H | 完了メモ（任意） | 担当者 |
| I | カレンダーの予定へのリンク | CMO |

上部に集計（未着手／進行中／期限切れ／完了）。期限切れは赤、完了は打ち消し線で自動着色。

### 変更10: TODOをGoogleカレンダーに登録する運用を開始

TODOシートは見に行く習慣がないため、**通知はカレンダーが担う**。
CMOがTODOを追加するたびにカレンダー予定を作成する（オーナーの都度承認は不要との合意）。

- 終日予定・「予定なし」扱いなので、スケジュールを圧迫しない
- 説明欄に「なぜ」「やること」「TODOシートの該当行へのリンク」を入れる
- 担当が渡辺さんのものは参加者に追加 → 招待が届き、リマインダーも鳴る

初回10件（T001〜T010）を登録済み。

**注意：終日予定は `+09:00` 付きの時刻を渡すとUTC変換で日付が1日ずれる。**
正午（`T12:00:00+09:00`）を渡すことで回避している。

### 変更11: `目次` タブを新設（先頭）

全43タブが並んでいて何がどこにあるか分からない状態だったため、一番左に `目次`（gid=291423606）を追加した。
各タブの中身を実際に読んだうえで、「シート名（リンク）／分類／何のシートか／誰が使う／更新タイミング」の5列にまとめている。
シート名は `=HYPERLINK("#gid=...")` でクリックするとそのタブに飛ぶ。分類は **日々使う／月次の入力／集計・分析／勤怠・精算／記録・アーカイブ** の5グループ。
1〜12月の `◯月_売上/顧客`・`◯月_支出/成績` は各1行にまとめ、右側（F〜Q列）に12ヶ月分のリンクを横並びにした。全29行で1画面に収まる。

**読んで分かったこと（目次の説明に反映済み）**

- `年間成績` は1〜16行目が空で、表は **17行目** から始まる。
- `分析_件数単価流入`（旧）は L1 のピボットが `#REF!` で壊れており、**9月の行も抜けている**。数字は `_v2` を見ること。
- `紹介カード` は「提携先」の見出しと2社の社名だけの、ほぼ空のタブ。
- `【不使用】勤怠管理` は `【新】勤怠管理` の旧版で、労働時間が時刻形式（4:30:00）のため集計しにくい。最下部に置いた。

既存タブのデータは一切変更していない（新規タブの作成と書き込みのみ）。

### 変更12: `顧客管理台帳` の TEL列を修正（先頭の 0 が落ちていた）

**957件中800件の電話番号から、先頭の `0` が消えていた。** 台帳を作った際に値を数値として書き込んだため、
`09012345678` が数値 `9012345678` として保存されていた（9桁＝固定電話、10桁＝携帯）。
このままではSMSも発信もできないため、次の手順で直した。

1. D16:D972 の表示形式を **書式なしテキスト** に変更（今後入力しても0が消えない）
2. 先頭に `0` を復元した値を書き戻し。`冬季見込み客_2026` タブは作成時に文字列として書いたため0が残っており、
   そちらに正しい値がある行はそれを優先して採用した

結果：**801件が正常（0始まり）／152件が空欄／1件が要確認**。`冬季見込み客_2026` の804件と件数が整合する。

**要確認の1件**
- `C0248 有限会社飯村左官工業` … TEL が `70447793940`（11桁・0始まりでない）。桁数が合わないため復元できず、`要確認:` を付けて残した
- `C0499 青山リアルティ・アドバイザーズ株式会社` … TEL欄に `和美躰` という人名らしき文字列が入っていた（入力ミス）。未登録扱いにした

**過去の売上シート（`◯月_売上/顧客`）は触っていない。** 台帳は各月シートから生成した派生データなので、そちらだけを直した。

### 変更13: `電話番号_未登録` タブを新設

電話番号が無い **153名** を、渡辺さんがスマホの連絡先から埋められる形にまとめた（gid=19259735）。

- 入力するのは **D列「★電話番号」だけ**。D列は書式なしテキストにしてあるので先頭の0が消えない
- `冬季見込み客_2026` の優先順位（1〜9）で並べ、優先1〜3（2回以上ご利用のリピーター）が上に来るようにした
  → 優先1: 13名／優先2: 7名／優先3: 14名／優先4-5: 13名／優先7-8: 73名／優先9（直近施工）: 33名
- 本人を特定できるよう 顧客名・住所・最終施工日・施工メニュー を並べ、A〜E列を固定
- E列「状態」にプルダウン（未確認／入力した／スマホに無い／連絡不可／送信対象外）
- 条件付き書式：入力済みの行は緑、**優先1〜3で未入力の行は琥珀色**
- 3行目に「入力済み／残り／優先1〜3の残り」のカウンタ

内訳：個人115名・法人38名。送信系統は 自社136名・本舗17名（本舗顧客は送信主体がおそうじ本舗江戸川中央店になるため分けてある）。

---

## 2026-09-06 見積ルールタブ：価格改定と紹介料の修正

**バックアップ**: Drive に `【バックアップ】2026_売上_顧客情報管理 2026-09-06 価格改定前`
（`1XdIhxB_TE0RCdZdnmdG6u1fsweaj2_WZI_42imWJ7ok`）としてスプレッドシート全体をコピー済み。

変更したセルは4箇所だけです。

| セル | 変更前 | 変更後 |
|---|---|---|
| A18 | （空欄） | 「※2026-09-06 改定。掲載価格はすべて税込。割引は税込価格に対して掛ける。正データは GitHub の data/prices.json」 |
| C20 | 9800 | **10780** |
| D20 | 15800 | **17380** |
| E20 | 29800 | **32780** |
| B23 | 紹介料：1-2月：5% | **紹介料：消費者→消費者：一律¥1,000** |
| B24 | 紹介料：その他：10% | **紹介料：事業者→顧客：5〜15%（原則12%）** |

理由：

- **定価**：オーナー判断で税込表示に統一（実質10%の値上げ）。`docs/price-master.md` 参照。
- **注記の追加**：「掲載価格は税込」がどこにも書かれていなかったため、
  チャネルごとに計算方法が割れていました（早期予約は9,800円から直接割引、
  マッチング経由は税抜から1.1倍）。その原因を潰すための1行です。
- **紹介料**：旧記載は月別の料率でしたが、実際は紹介元で分かれます（2026-09-06 オーナー確認）。

**変更していないもの**：

- 繁忙期料金の ＋￥3,300 は据え置き（元から税込の形＝3,000円の1.1倍のため）
- 複数台割引の金額（￥500〜￥7,000/台）
- 過去の実績データは一切触っていません

**要確認として残っているもの**：

- E20 の 29,800円 は「複数台（業務用）」の定価ですが、パンフレットの
  業務用エアコン「¥24,800〜（2台以上）」と一致しません。どちらが正か未確認のまま、
  ルールどおり1.1倍して 32,780円 にしています。

---

## 2026-09-14 『【毎月更新】リピート/業務提携』タブ：2026年5〜8月の転記

依頼：`20260914-01-crm`（経営企画室の再集計 `20260914-05-cmo` で、このタブが2026年4月で
更新停止していることが判明）。実行ツール：`tools/teikei-tenki.py`（既定は計画表示のみ、
書き込むときだけ `--confirm WRITE`）。

### バックアップ

**Driveのコピーは取れませんでした。** サービスアカウントには Drive の保存容量が無く、
`files/copy` が `403 storageQuotaExceeded` になります（Drive コネクタの `copy_file` も
この環境では無効）。代わりに**同じスプレッドシート内にタブを複製**しました。

- 控えタブ：**`控え_業務提携_20260913-2326`**（変更前の内容そのまま）

> **申し送り：今後スプレッドシート全体のバックアップが要る作業は、オーナーの手で
> Drive のコピーを取っていただく必要があります。** サービスアカウントでは作れません。

### 変更1: 売上23〜32 の列を追加（AW〜BP）

株式会社レジェンド様が**22組を使い切っていた**ため、追記できる場所がありませんでした。
`合計`（旧AW）の手前に 日付/売上 を10組ぶん挿入。見出しと表示形式（日付／通貨）も設定。

この挿入で `合計`→**BQ**、`総合計`→**BR**、`平均単価`→**BS**、`案件発生`→**BT**、`施策`→**BU** に移動。
**他のタブからこのタブを参照している数式は1つもありません**（全62タブの数式を機械で確認済み）。

### 変更2: 2026年5〜8月の業務提携売上 19件 ¥2,622,730 を追記

出所は `◯月_売上/顧客` の **流入経路＝業務提携** の行。**既存のセルは1つも書き換えていません**
（控えタブと全セル比較して、差分28箇所はすべて「空欄→値」でした）。

| 企業 | 件数 | 金額 |
|---|---:|---:|
| 株式会社レジェンド | 7 | ¥2,102,540 |
| 生活彩館かさい電器 | 5 | ¥169,200 |
| タカラサービス | 2 | ¥142,020 |
| 株式会社エコハウス | 2 | ¥44,660 |
| 才木工業 | 1 | ¥46,130 |
| 株式会社プレジャー | 1 | ¥34,800 |
| **インテリアエージェント（新規行18）** | 1 | ¥83,380 |

総合計（BR2）：¥8,997,082 → **¥11,619,812**（差 ¥2,622,730・一致）。

### 変更3: 合計と平均単価を、列を増やしても壊れない式にした

- `合計`（BQ2:BQ101）… 売上1〜32 の全列を足す形に伸ばした
- `平均単価`（BS2:BS101）… `=IF(COUNTA(日付32列)=0,"",合計/COUNTA(日付32列))`
  **これまで割る数が手打ちで、実態とズレていました**（例：レジェンド様は21件あるのに `/19`）

### 転記しなかったもの（判断待ち・7件 ¥344,755）

| 日付 | 金額 | 記載 | なぜ保留か |
|---|---:|---|---|
| 2026/06/13 | ¥104,015（5件） | 加藤さま本人／加藤さんご紹介×4 | 提携先ではなく**個人からの紹介**に見える |
| 2026/06/17 | ¥149,000 | 山口様 | **提携先を特定できない** |
| 2026/08/02 | ¥91,740 | 山口様 | 同上 |

### 触っていないもの

- `紹介カード` タブ（`株式会社吉昇エコハウス` / `Kan.工房`）…
  **2026年のシートにこの2社の受注記録は1件もありません。**「吉昇エコハウス＝エコハウス」の
  根拠とされた「初回受注が同額同日」は2025年の記録なので、このシートだけでは確かめられません
- `◯月_売上/顧客`（過去の実績データ）
- 旧・平均単価の値（BR列に `=BQ7/19` の形で残っています。BS列の新しい平均単価と二重になるので、
  **消してよいか CMO の判断待ち**）

---

## 2026-09-15 未明 本舗SMSの3通案への差し替えと、提携タブの後片付け

いずれも **Drive の全体コピーは取れない**（サービスアカウントに保存容量が無い）ため、
**タブ複製で控えを取った**。

### 変更4: `冬季見込み客_2026` の F列（送信する本文）を3通案に作り直した

依頼 `20260913-02-crm`（オーナー決定 9/14 11:5x「この3通案で送る」）。
ツール：`tools/sms-honpo-3tsu.py`（既定は検算のみ、書き込みは `--confirm WRITE`）。
ひな形：`data/sms-template-honpo.txt`（3通案へ差し替え。元の451文字版は git log で追える）。

- 控えタブ：**`控え_冬季見込み客_20260914-1754`**（F列）／**`控え_冬季見込み客_20260914-1805`**（D列）
- 書き換えたのは **本舗 × 送信済み空欄 × 配信区分=送信可 の 399行の D列と F列だけ**
- **控えと全セル比較して、F列以外の差分は0件**

> ### ⚠️ 最初 F列だけ直して、D列を見落としていた（同じ晩に気づいて直した）
>
> **D列「▶ 送る」は、リンクの中に本文がまるごと埋まっている**
> （`…/s.html#to=電話番号&b=本文`）。F列は和真さんが読むための控えで、
> **実際に送られるのはD列のリンクの中身**。
> F列だけ直した時点では、**タップすると古い451文字の本文が送られる状態**だった。
> 同じ晩に気づいて、399行ぶんのリンクを貼り直した（`tools/sms-honpo-3tsu.py` が両方直すようにした）。
> **自社の行のリンクは触っていない**（261〜271文字のまま・確認済み）。

| | |
|---|---:|
| 対象 | **399名**（個人392・法人7） |
| 文字数 | 最短163／中央169／**最長184** |
| 通数 | **全員3通**（210文字の枠に収まった） |
| 費用 | 1,197通 × 11円 = **¥13,167**（元の451文字版は7通＝¥30,096） |
| 名乗り照合の違反 | **0件** |

触っていない行：本舗の送信済み3件／配信区分が送信可でない262件（SMS不可59・除外200・保留3）／
自社の全行。**1通も送っていない**（KDDI 利用開始は 9/25 ごろ）。

> **見つけたもの：1行だけ、送信系統=本舗なのに台帳の最新名義が「自社」の行がある**
> （行583・最終施工 2025-08-29）。配信区分が「送信可」ではないので今回の399名には
> 入っていないが、**区分が変わると本舗名義で送られる危険がある。** CMO に報告済み。

### 変更5: 『【毎月更新】リピート/業務提携』に 株式会社テック山口 を追加（行19）

依頼 `20260914-02-crm`（オーナー指示 9/14 15:5x）。ツール：`tools/teikei-seiri.py`。

- 控えタブ：**`控え_業務提携_20260914-1755`**
- 行19 に `株式会社テック山口` を追加。2026/06/17 ¥149,000 と 2026/08/02 ¥91,740 を紐づけ
- 備考に「台帳では「山口様」（C0878）。2026-09-14 オーナー確認で株式会社テック山口と判明。
  本舗→自社への転換成功事例。過去行の氏名は変更していない」
- **`◯月_売上/顧客` の「山口様」という氏名は書き換えていない**（実績データのため）
- 総合計：¥11,619,812 → **¥11,860,552**（差 ¥240,740・一致）

### 変更6: BR列に残っていた古い平均単価を消した（8セル）

CMO 判断（9/14 08:5x）。9/14 の列追加で BS列に件数を数える新しい平均単価を入れたが、
BR列（見出しは「総合計」）に `=BQ7/19` のような**手打ちの割る数**の式が残り、
新旧の平均単価が二重に見えていた。

消したのは **BR5・BR6・BR7・BR8・BR10・BR11・BR14・BR16 の8セルだけ**。
**BR2（総合計 `=SUM(BQ2:BQ101)`）は残している。**

### 変更7: `予約_Web` に照合用の13列を追加（O〜AA）

依頼 `20260913-03-crm`（CMO 2026-09-14 08:5x 全面採用）。ツール：`tools/yoyaku-match.py`。
**このタブは0行だったので、バックアップの必要がありません**（消える中身がない）。

```
O:src｜P:cid｜Q:★台帳の顧客ID｜R:★台帳の名義｜S:★台帳の最終施工日｜T:★照合の手がかり｜
U:★属性｜V:★売上行への参照｜W:★売上（税込）｜X:レントラックス注文番号｜
Y:成果発生日｜Z:入金確認日｜AA:★却下判定
```

- **★は機械が書く列。** X・Y・Z は人が手で入れる列で、値が入っていれば機械は上書きしない
- **AA 却下判定**：`=IF(AND(注文番号<>"",入金確認日="",成果発生日<>"",TODAY()-成果発生日>=38),"要却下","")`
  レントラックスの45日自動承認の前に、毎週月曜「要却下」で絞れば対象が出る
- **条件付き書式**：`★台帳の名義` が「本舗」の行に色が付く。
  **その方には確認連絡を SMS・LINE で送らず、和真さんが電話で受ける**（2026-09-11 の事故の再発防止）

照合は `meigi_shiraberu()` と同じ規則（電話でも氏名でも引き、いちばん新しい施工を採る）。
**台帳は書き換えません**（`◯月_売上/顧客` から作る派生データのため）。

自己診断：`python3 tools/yoyaku-match.py --自己診断` で、台帳の実在のお客様6名を
「申し込みが来た」ことにして同じ道を通し、全員が照合できること・本舗の方に印が付くことを確認済み。

---

## 2026-09-15 CMO判断6件への対応（法人の線引き・名義不一致の停止・法人名列）

### 変更8: 名義が食い違う4行を、シートの側でも止めた

依頼 `20260913-02-crm`（CMO 2026-09-15）。ツール：`tools/meigi-fuitchi-hold.py`。
控えタブ：`控え_冬季見込み客_20260915-0024`。

> **止め方は「ツールに任せる」ではなく「シート上でも止まっている」でなければいけない**（CMO）。
> 和真さんはスマホでこのシートを直接触るので、配信区分を手で「送信可」に戻せば道具を通らずに送れる。

**CMO の指示は行583の1件でしたが、機械で全行を照合したら4件ありました。**
うち3件は **9/11 の事故と同じ向き**（送信系統=自社なのに台帳=本舗）です。

| 行 | 顧客名 | 送信系統 | 台帳 | もとの配信区分 |
|---|---|---|---|---|
| 567 | R.Kプランニング | 自社 | **本舗**（2022-11-29） | 除外 |
| 573 | RKプランニング | 自社 | **本舗**（2022-09-26） | 除外 |
| 583 | アンカークリニック船堀 | 本舗 | **自社**（2025-08-29） | SMS不可 |
| 717 | 高幡不動店 | 自社 | **本舗**（2022-12-13） | SMS不可 |

4件とも **配信区分を「保留」**にし、`送らない理由` に
`名義不一致（送信系統=◯◯／台帳=△△／最新施工 ...）2026-09-15 確認中` を**書き足し**ました。
**もとの理由（固定電話・楽ラクーン経由など）は消していません。**

あわせて **台帳で名義を照合できない19行**にも、理由を1文書き足しました（配信区分は触っていません。
19行ともすでに 除外／SMS不可／保留 です）。

### 変更9: 本舗の対象を 399 → **396名**に。外した3件は `法人_別対応` タブへ

CMO の線引き（2026-09-15）は「法人だから外す」ではなく
**「お客様本人が会社か／会社は紹介元にすぎないか」**。外す理由も費用ではなく、
**3通案の本文が追い焚き配管まで含む住居向け**だからです。

- 判定の順序を `tools/sms-honpo-3tsu.py` に入れた（①顧客名の法人格 ②提携先タブと一致
  ③`data/sms-houjin-hantei.txt` のCMO判断 ④それでも決まらず「法人/個人」列が法人の行は
  **本文を作らず一覧で出す**）。**かっこの中は見ません**
  （「古庄(株式会社エコハウス紹介)」のかっこは紹介元の注記で、お客様は古庄さま個人のため）
- 外した3件（株式会社スターツツーリスト／三井不動産／長谷工エリアルエステート）は
  **F列とD列のリンクを消し、配信区分を「除外」**にして、`送らない理由` に
  `法人_別対応へ（web-inflow の法人向け文面）2026-09-15 CMO判断` を入れた
- **`法人_別対応` タブ**を新設（`tools/houjin-betsutaiou-tab.py`）。**6件**入っています。
  うち3件が今回外したぶん、**残り3件はもともと対象外だった法人**（株式会社ソフトファクトリー／
  株式会社アシスト／株式会社エコラインシステム　篠崎さま）。列で見分けられるようにしてあります
- 396名・1,188通・**¥13,068**（無料トライアルが付けばまるごと無料枠に収まる）
- 控えタブ：`控え_冬季見込み客_20260914-002440`

### 変更10: `◯月_売上/顧客` の**末尾**に「法人名」列（U列）を12タブぶん追加

依頼 `20260914-02-crm` ／ CMO 承認（2026-09-15）。ツール：`tools/houjinmei-column.py`。

- **必ず末尾。** 途中に挿入すると `SUMIF('5月_売上/顧客'!R4:R501,…)` のような列指定がずれて、
  支出/成績と年間成績の数字が黙って変わります
- **6月だけで先に試し**、売上・販管費・営業利益が1円も変わらないことを確認してから12タブへ
- 12タブぶん、**実行の前後で集計値はすべて一致**（ツールが自動で突き合わせます）
- 既知の紐づけとして、6月行37・8月行8 の「山口様」に **`株式会社テック山口`** を書いた。
  **氏名は1文字も書き換えていません**
- **控えは取っていません。** 末尾に列を足すだけで既存のセルを1つも書き換えないためです
  （書いた2件も空欄への追記）

---

## 2026-09-16 返信を「本文ごと」残す列を、2タブに追加

依頼 `20260916-01-crm`。ツール：`tools/henshin-bunrui.py`。

### なぜ

**お客様が何と言ってきたかを保存している列が、どこにも無かった。**
`返信メモ` は和真さんの要約（「感謝の意の返信」など）で本文ではない。
その結果、**「返信あり」が立っているのにメモが空欄の行が2件**あった。
**その方が何を言ってきたのかは、和真さんの携帯の中にしか無い。**

### 変更11: 両タブの末尾に2列

| タブ | 足した列 |
|---|---|
| `お詫びSMS_20260912` | **H列 `返信内容（本文）`** ／ **I列 `返信の分類`**（列数を 7→9 に拡張） |
| `冬季見込み客_2026` | **Z列 `返信内容（本文）`** ／ **AA列 `返信の分類`**（列数を 25→27 に拡張） |

- `返信の分類` はプルダウン：**予約／断り／質問／その他／不明・未記録**（`strict=False`）
- **`返信メモ` は1文字も書き換えていない。** 隣に足しただけ
- **控えは取っていない。** 末尾に列を足すだけで、既存のセルを1つも書き換えないため

> CMO が 9/13 に「G列『返信メモ』の先頭に『予約：』と書く運用」を決めたが、
> **既存のメモに手を入れずに済むので別の列にした。** 不都合があれば戻せる。

### 入れた分類（9行。すでに値がある行は触らない）

**返信をくださったのは実人数で5名。** 両タブに載っている方がいるので行数は9。

| | 分類 | 根拠 |
|---|---|---|
| お詫び行4／冬季行6 | **予約** | 「また頼まれる予定」 |
| お詫び行9／冬季行15 | その他 | 「感謝の意の返信」 |
| お詫び行23／冬季行37 | **予約** | 「換気扇をご依頼予定。」 |
| **お詫び行8** | **不明・未記録** | **返信メモが空欄** |
| **お詫び行19／冬季行31** | **不明・未記録** | **返信メモが空欄** |

**5名とも台帳の名義は「自社」**（`meigi_hyou()` で機械照合。列の値は見ていない）。

> **「不明・未記録」は逃げではなく印。** 返信があったのに中身が残っていない行を、
> 画面上で数えられるようにするためのもの。**2名は和真さんに聞かないと埋まらない。**

---

## 2026-09-17 本舗396名の文面に「このSMSへの返信・お電話は届きません」を入れた

依頼 `20260915-02-crm` ／ CMO 判断（2026-09-17）。

> **送信元がFAX専用と確定した以上、返信もできず折り返すとFAX音が鳴る状態で何も書かないのが最悪です。
> 「返信できません」＋「折り返し先の番号」を必ず両方。片方だけだとお客様は行き場を失います。**

### 変更12: `冬季見込み客_2026` の F列と D列（本舗396行）

- 控えタブ：**`控え_冬季見込み客_20260917-103250`**
- ひな形：`data/sms-template-honpo.txt`（1行だけ差し替え）

```
変更前： ご入用でしたら080-1344-3137（渡辺）へお電話ください。
変更後： このSMSへの返信・お電話は届きません。ご用件は080-1344-3137（渡辺）へ。
```

| | 変更前 | 変更後 |
|---|---:|---:|
| 文字数（最短／中央／**最長**） | 163／169／**183** | 173／179／**193** |
| 通数 | 全員3通 | **全員3通** |
| 費用（396名） | ¥13,068 | **¥13,068**（変わらない） |
| 名乗り照合の違反 | 0件 | **0件** |

- **F列とD列の両方**を書き換えた（D列は「▶ 送る」のリンクに本文が埋まっている。
  9/14 に F列だけ直して D列を見落とした失敗の再発防止）
- 検算：396行すべてに新しい一文が入っていること、最長193文字・1,188通であることを機械で確認
- **1通も送っていない。** 送信のGOは CMO が文面を見てから

> ### ⚠️ 未確認のまま残っている論点
>
> **70文字を超えて分割されたとき、お客様の端末で1通に結合して表示されるのか、別々に届くのか。**
> いまの文面だと**電話番号が2通目と3通目にまたがります**（`080-` / `1344-3137`）。
> **別々に届くなら番号がちぎれて押せません。** KDDIへの確認事項として出してある。

> ### 自社の文面には電話番号が1つも入っていない
>
> `data/sms-template.txt`（cmo ブランチ）の配信停止は「その旨ご返信ください。」だけ。
> **いまは和真さんが 080-8043-8259 から手送りなので実害なし。**
> **自社をKDDIに回すなら、その前に必ず番号を入れること**（特定電子メール法4条）。

### 変更13: 堀江さまの行に、和真さんの確認内容を入れた（2タブ）

依頼 `20260916-01-crm`（和真さん回答 2026-09-17）。「お客様からご連絡いただく」形で話がついている。

| タブ | 行 | H 返信内容（本文） | I 返信の分類 |
|---|---:|---|---|
| `お詫びSMS_20260912` | 4 | 「また頼まれる予定。お客様からご連絡いただく形で話がついている（和真さん確認 2026-09-17）」 | その他 |
| `冬季見込み客_2026` | 6 | 同上 | **予約 → その他** |

- **2タブを同じ値で揃えた。** 冬季側は「予約」のままだったので、こちらも直した
- `返信メモ` は触っていない。**残る食い違いは「メモ」2箇所**（お詫び側に「お客様から連絡すると申し出」
  「進展なし」の追記があり、冬季側に無い）。**どちらへ揃えるかは CMO の判断待ち**

### 変更14: `◯月_支出/成績` の科目別集計が明細の最終行に届いていなかったのを直した（12タブ・87個の式）

依頼 `20260917-01-crm`（planning 2026-09-17）の2番。**原因は金額ではなく「式の範囲」だった。**

```
6月  F72 合計    =(SUM(F2:F71))-J80      ← 明細の最終行 71 まで届いている
     F77 広告費  =SUMIF($E$2:$E$70,…)    ← 70 で止まっていた。71行目の ¥1,320 が落ちる
```

このタブは**行を足して使う形**なので、**足すたびに、どれかの科目が静かに小さくなる。**
`tools/shukei-hani-check.py` を作って12タブ全部を調べ、**明細の頭から始まる縦の範囲だけ**を最終行まで伸ばした。

#### 実際に値が動いたのは5セルだけ（ほかの82個は将来の予防）

| タブ | セル | 科目 | 変更前 | 変更後 | 差 |
|---|---|---|---:|---:|---:|
| 6月 | K32 | 交通費（クレカ・立替えでない） | ¥49,454 | **¥64,467** | +¥15,013 |
| 6月 | F77 | 広告費 | ¥61,048 | **¥62,368** | +¥1,320 |
| 7月 | K32 | 交通費（クレカ） | ¥57,739 | **¥59,219** | +¥1,480 |
| 7月 | F91 | 広告費 | ¥112,120 | **¥117,316** | +¥5,196 |
| 7月 | F92 | **人件費** | **¥0** | **¥15,000** | +¥15,000 |

**7月の人件費 ¥15,000 は planning の指摘に入っていなかったぶん。** 範囲が `$E$2:$E$74` で、
バイト代の行が77行目にあり、**まるごと落ちていた。**

#### 営業利益は1円も動いていない（確認済み）

販管費は `合計` から作られていて、そちらの範囲は最初から届いていた。**動いたのは内訳の表示だけ。**

| | 変更前 | 変更後 |
|---|---:|---:|
| 6月 営業利益 | ¥1,295,433 | **¥1,295,433**（同じ） |
| 7月 営業利益 | ¥825,467 | **¥825,467**（同じ） |
| 7月 原価合計 | ¥16,859 | ¥31,859（人件費が入った） |
| 7月 販管費 | ¥738,631 | ¥723,631（同額だけ減る。式が `−F92` のため） |

**和真さんへの立替経費（`K33` → `F98`）は全月で差 ¥0。** 支払額は動いていない。
`K32` はどこからも参照されていない（全タブの式を機械で検索して確認）。
12タブすべてでエラー値（`#REF!` 等）が出ていないことも確認した。

#### 控え

Driveのコピーはサービスアカウントに容量が無くて取れないため、**変更前の式そのものを
`data/backup/shukei-shiki-20260917-230407.json` に書き出してからコミットした。**
戻すときはこのファイルの `変更前` を書き戻せばよい。

### 変更15: 旧版の分析タブに「使わない」をタブ名で明示した

依頼 `20260917-01-crm` の3番（planning「旧版はそのうち畳んでください」）。

| | 変更前 | 変更後 |
|---|---|---|
| タブ名 | `分析_件数単価流入` | **`【旧版・使わない】分析_件数単価流入`** |
| 目次 C20 の説明 | 【旧版】…数字は v2 を見ること | 件数が v2 と1〜2件ずれる旨を追記 |

- **消していません。** 中身は1セルも触っていません。**タブ名と目次の説明だけ**
- 式からこのタブを参照しているのは **目次のリンク1つだけ**（全タブの式を機械で検索して確認）。
  そのリンクは gid 指定なので、タブ名を変えても切れません
- 目次では前から「見なくてよい・更新しない」と書いてあったが、**タブの一覧では分からなかった。**
  planning が旧版を見て食い違いに気づいたので、**タブ名のほうに出した**

### 変更16: 電話番号が「通の境界」をまたがない形に組み替えた（F列・D列 396行）

CMO 決定 2026-09-18。**分割された3通が端末で1通に結合されるかは KDDI 未回答。**
**結合されない端末では、通をまたいだ番号が2つにちぎれて押せません。**
**片方に倒れたときだけ壊れる設計は採らない**ということで、確認を待たずに組み替えました。

#### 組み替え前は 396名中 **75名** がちぎれていました

```
--- 2通目（70文字）--- …このSMSへの返信・お電話は届きません。ご用件は080-
--- 3通目 ---           1344-3137（渡辺）へ。…
```

**全員ではありません。** 差し込み（顧客名・施工時期・前回メニュー）で本文の長さが
173〜193文字と動くので、**番号の位置も動き、140文字目の境界にかかる人だけが割れます。**
`tools/sms-honpo-3tsu.py` に検算3（`matagi()`）を足して数えました。

#### 変えたのは最後の3行だけ

```
変更前
  このSMSへの返信・お電話は届きません。ご用件は080-1344-3137（渡辺）へ。
  ご案内が不要な方も同じ番号へお知らせください。
  おそうじ本舗　江戸川中央店

変更後
  このSMSへの返信・お電話は届きません。
  ご用件も、ご案内が不要な方も、下記の番号へお知らせください。
  おそうじ本舗　江戸川中央店　080-1344-3137（渡辺）
```

**言っていることは同じです。番号を最終行へ move しただけ。**
「ご案内が不要な方」「お知らせください」はオーナーの言い回しのまま残しました。

| | 変更前 | 変更後 |
|---|---:|---:|
| ひな形 | 179文字 | **181文字** |
| 差し込み後 最短／中央／**最長** | 173／179／**193** | 175／181／**195** |
| 通数 | 全員3通 | **全員3通** |
| 費用（396名・1,188通） | ¥13,068 | **¥13,068**（変わらない） |
| 名乗り照合の違反 | 0件 | **0件** |
| **番号が通の境界をまたぐ行** | **75件** | **0件** |

#### なぜ0件を保証できるか

**番号のうしろに残る文字は「（渡辺）」の4文字だけ**なので、本文がどれだけ短くなっても
番号は末尾近くに居ます。**番号の位置は 159〜191文字目。3通目は141文字目からなので、上に18文字、
下に19文字の余裕があります。**

> ⚠️ **番号のうしろを長くすると、また境界にかかります。** ひな形の頭にも書いておきました。

#### 検算と控え

- 控えタブ `控え_冬季見込み客_20260918-025523`（書き換え前）
- **F列 396行・D列（▶ 送る）のリンク 396行**を作り直し。シート側を読み直して
  **F列396行すべてに新しい本文／またぎ0件／D列のリンク396件も新しい本文**であることを確認
- **送信済み・対象外の3行（118・119・120）は触っていません。** 過去に送った本文の記録です
- **ツールは、またぎが1件でもあれば書き込みません**（`--confirm WRITE` でも止まる）
- **1通も送っていません。**

### 変更17: 提携タブに楽ラクーンを登録し、9月の受注日3件を追記した（追記のみ）

依頼 `20260919-01-crm`（CMO 2026-09-19）。**控えタブ `控え_業務提携_20260919-045306` と
`控え_業務提携_20260919-045425` を取ってから実行。既存のセルは1つも書き換えていない。**

#### 🛑 「2社追加」のうち1社は、止めました

**クラスリフォームは No.4 として最初から入っています**（2023年から）。
そのまま足していたら**同じ会社の行が2つでき、合計が二重に**なっていました。
`tools/teikei-tsuika.py` を作り、**企業名の重複と受注日の二度足しを機械で止める**形にしました。

#### 入れたもの

| 行 | 中身 |
|---|---|
| **20行目（新規・No.19）** | **楽ラクーン**（業種「toBマッチング」）。**受注日は入れていません**（下記） |
| 5行目 クラスリフォーム | `AG5` に **2026/09/16**（金額は空欄） |
| 17行目 株式会社プレジャー | `K17` に **2026/09/25**（金額は空欄） |
| 7行目 株式会社レジェンド | `BI7` に **2026/09/26**（金額は空欄） |

**D列（備考）は書き足しです。もとの記述は消していません。**

#### 楽ラクーンに受注日を入れなかった理由

**楽ラクーン経由の施工は、月次タブで流入経路「楽ラクーン」として別に数えられています**
（2026年 **67件・¥1,845,833**／5〜8月）。
ここに受注日を入れると、**同じ仕事が2か所で数えられます。**
**この表に載せるのは「会社が存在する」という登録だけ**にして、
どちらで数えるかのルールが決まってから受注日を入れます。

> **CMO が起こした 10/17 の2行だけ流入経路が「業務提携」**で、ほかの67件と揃っていません。
> **「楽ラクーン」へ寄せるのが筋**だと考えますが、**月次タブは書き換えていません**（CMO判断待ち）。

#### ⚠️ 金額が空のあいだ、平均単価が下がります

平均単価は **合計÷日付の個数**なので、日付だけ入れると一時的に低く出ます。**金額が入れば自動で戻ります。**

| 提携先 | 変更前 | いま |
|---|---:|---:|
| クラスリフォーム | ¥88,903 | **¥82,976** |
| 株式会社レジェンド | ¥146,686 | **¥141,628** |
| 株式会社プレジャー | ¥67,010 | **¥50,258** |

合計金額（BQ列）は**1円も動いていません**（クラスリフォーム ¥1,244,640／レジェンド ¥4,107,210）。

### 変更18: 返信をくださったお客様の行に色を付けた（2タブ・条件付き書式）

オーナー指示（2026-09-19 MTG／3-3【B】No.5・235行、依頼 `20260919-04-crm`）。

> 返信のあるお客様が視覚的に判るように設定しておいてほしい

**薄い緑 `#C9E6C9` で、行まるごと**（`tools/henshin-iro.py`）。

| タブ | 条件 |
|---|---|
| `冬季見込み客_2026` | `=OR($E6="返信あり",$G6<>"",$AA6<>"")` |
| `お詫びSMS_20260912` | `=OR($D4="返信あり",$F4="返信あり",$G4<>"",$I4<>"")` |

#### ★ 1列では足りません

**返信の印が、タブごとに別の列に散っています。**
とくに**お詫びタブは `元の状態` を見ないと2名落ちます**（堀江さま・黒田さま。`送信済み` は「送信済み」のまま）。

#### ★ ルールは先頭（index 0）に入れました

`冬季見込み客_2026` には**条件付き書式が66本**すでにあり、うち**65本が背景色を塗ります**。
その多くが `=$F6<>""`（本文のある行ぜんぶ）です。
**条件付き書式は先に当たったものが勝つ**ので、**末尾に足したら緑は一生表示されません。**
先頭に1本足しました。**既存のルールは1本も消していません。**

> 📌 申し送り：**66本のうち大半が同じ2本（`=$F6<>""` と `=$I6<>"送信可"`）の重複**です。
> 動作に実害はありませんが、読みにくくなっています。**整理は別途、指示があれば。**

#### 控え

書式だけの変更なので、タブの複製ではなく**いまのルールを JSON で書き出しました**。
`data/backup/joken-tsuki-shoshiki-20260919-085609.json`。戻すときはこれを書き戻せばよい。

#### あわせて：濱田 里花さまの行を2タブで揃えた

**冬季側だけ緑にならなかったため**（`送信済み` が「送信済み」のままで、返信メモも分類も空欄）。
**空欄だった2セルに、お詫びタブと同じ値を入れました。既存の値は書き換えていません。**

| | `Z 返信内容（本文）` | `AA 返信の分類` |
|---|---|---|
| `冬季見込み客_2026` 13行 | 「10月5日ノーマル受注」 | 予約 |

**この方が、2026-09-19 のオーナー訂正でいう「SMS経由の受注1件」ご本人です。**

#### 検算

| タブ | 緑になる行 |
|---|---|
| `冬季見込み客_2026` | **5件**（堀江・濱田・松田・黒田・齋藤の各さま） |
| `お詫びSMS_20260912` | **5件**（同上） |

**2タブで同じ5名になりました。**
