# 露出した資格情報：何を・どの順で差し替えるか（2026-09-20）

作成 ブラウザ担当 ／ 依頼 CMO ／ **値は1文字も書きません。**道順と「止まるもの」だけ。

## なぜやるか

CMO の報告：**認証情報ドキュメントの中身が `get_file_metadata` の `contentSnippet` で丸ごと返る事故**
（サービスアカウント秘密鍵・Netlify PAT・LINEチャネルトークン2本・チャネルシークレット・CMSのID/パスワード）。
セーブデータ 577行に「**一度チャットに貼られた鍵を作り直す（T018）**」が 9/10 から開いたままで、**9/20 に同じことが再発**。

**流出先はオーナー本人のアカウントに紐づくセッション記録であって、公開の場ではありません**（CMO補正）。
**外部の第三者に渡った証拠はありません。**ただし**追跡していない場所に複製され、それが2回**起きているので、作り直します。

## 順番の考え方（CMO指定）

**止まる範囲が狭いものから。**Netlify PAT が最初、サービスアカウント鍵が最後。

---

## 1. Netlify PAT `claude-lp-deploy`（最初にやる）

| | |
|---|---|
| 道順（作成） | `app.netlify.com` → 右上のアカウント → **User settings → Applications** → **Personal access tokens → New access token** → 名前を入れる → 期限を選ぶ → Generate token |
| 道順（失効） | 同じ画面のトークン行 → **Options → Revoke** |
| 期限の選択肢 | 7日／30日／60日／90日／Custom／**No expiration**（Netlifyは無期限を非推奨と表示） |
| **止まるもの** | **この1本で21サイト全部に配信できる**（サイト単位に絞れない） |
| **止まる時間** | **新旧を並行させれば0分。**先に新トークンで配信を1回通し、通ってから旧を Revoke する |
| 誰の手 | **生成と貼り付けはオーナー。**ブラウザ担当は画面の用意と、差し替え後の動作確認 |

### 21サイトの内訳（2026-09-20 に画面で数えた）

**ワンヒッター・稼働中（直近10日以内に配信）— 11件**

| サイト | 最終配信 |
|---|---|
| `yoyaku.onehitter.jp` | Sep 19 |
| `oh-naibu-sms-k7q3x` | Sep 19 |
| `araidoki` | Sep 19 |
| `survey.onehitter.jp` | Sep 19 |
| **`lp.onehitter.jp`** ← **広告の着地先。いま配信中** | Sep 19 |
| `one-hitter-booking` | Sep 19 |
| `one-hitter-sns-media` | Sep 18 |
| `dokuhon.onehitter.jp` | Sep 13 |
| `tenken.onehitter.jp` | Sep 12 |
| `one-hitter-nenmatsu` | Sep 12 |
| （上記10件＋下の別事業1件で、直近稼働は11件） | |

**別事業・稼働中 — 1件**

| サイト | 最終配信 |
|---|---|
| **`kidsmoney-crm-2014997530`（キッズマネースクール CRM）** | **Sep 17** |

**休眠 — 9件**（既定の自動生成名。2025年6月〜2026年5月が最終）

`adorable-brioche-5ac83d`（May 10）／`candid-puppy-cd84cf`（May 10）／`subtle-sable-a7e919`（May 10）／
`inquirycounter`（2025 Sep 14）／`incomparable-cobbler-5c1752`（2025 Jun 9）／`eclectic-profiterole-a09869`（2025 Jun 9）／
`vermillion-peony-b32ca6`（2025 Jun 9）／`curious-figolla-262114`（2025 Jul 21・作成のみ）／
`capable-profiterole-987361`（2025 Jul 23・作成のみ）／`lustrous-frangollo-9a2170`（2025 Jul 24・作成のみ）

> ⚠ **`kidsmoney-crm-2014997530` は別事業です。**この1本の鍵は、そこへの書き込み権限も含みます。
> 期限を「無期限」にするかどうかは、**ワンヒッターだけの話ではない**ものとして判断してください。

### 貼り先（未確定）

ツール7本が `~/.config/one-hitter/netlify-token.txt` を読む：
`deploy-netlify.py` `deploy-booking.py` `deploy-juchu.py` `booking-inbox.py` `juchu-inbox.py` `kanryo-inbox.py` `check-domain.py`
（環境変数 `NETLIFY_TOKEN` の参照は19か所）

- **オーナーのPCには存在しない**（`~/.config/one-hitter/` ごと無し・環境変数も未設定）＝ブラウザ担当は使っていない
- **CMOのコンテナには存在する**（CMOが実測）
- **他スレッドが持っているかは未確認。**LP担当の返事待ち。**推測で進めない**

---

## 2. LINE チャネルアクセストークン（長期）

| | |
|---|---|
| 道順 | **LINE Developers** → 該当チャネル → **Messaging API設定** → 一番下 **チャネルアクセストークン（長期）** → **再発行** |
| **旧トークンの扱い** | **再発行した瞬間に旧は無効。**並行運用できない |
| **止まるもの** | グループLINEの**送信**（`line_client.py push`）、**表示名の取得**、Apps Script 側の `LINE_TOKEN`（現在未設定なので影響なし） |
| **止まる時間** | **貼り替えるまで。**`~/.config/one-hitter/line-token.txt`（CMOのコンテナ）を直すまで送信できない |
| 注意 | **受信（Webhook）は止まりません。**受け口はウェブアプリURLで動いていて、トークンを見ていない |

## 3. LINE チャネルシークレット

| | |
|---|---|
| 道順 | LINE Developers → チャネル基本設定 → **チャネルシークレット** → 再発行 |
| **止まるもの** | **いまは何も止まりません。**署名検証に使う値ですが、Apps Script の受け口は
（`doPost` がヘッダーを受け取れないため）**署名検証をしていない** |
| 止まる時間 | 0分 |

## 4. CMS（リアライズ）の ID／パスワード

| | |
|---|---|
| 道順 | CMS管理画面 → アカウント設定 → パスワード変更（**画面の実物は未確認**。ブラウザ担当が開ける回に道順を確定します） |
| **止まるもの** | 公式サイト `one-hitter.jp` の**編集**。表示は止まらない |
| 止まる時間 | 新しいパスワードを知っている人が入れるまで |
| 注意 | **プライバシーポリシーの節番号など、直近で触った箇所の作業が途中なら先に終わらせる** |

## 5. KDDI の API キー

| | |
|---|---|
| 道順 | 提供元へ**再発行依頼**（画面で自分では替えられない可能性が高い。未確認） |
| **止まるもの** | `kddi-test-send.py`（SMS送信のテスト）。**本番のSMS配信経路は別**（要確認） |
| 止まる時間 | 提供元の対応次第 |

## 6. Google サービスアカウント秘密鍵（最後にやる）

| | |
|---|---|
| 道順 | GCP コンソール → IAM と管理 → **サービス アカウント** → `claude-sheets@one-hitter-sheets.iam.gserviceaccount.com` → **鍵 → 鍵を追加（新規作成・JSON）** → 各所に配布 → **古い鍵を削除** |
| **止まるもの** | **全スレッドのシート読み書きが同時に死ぬ**（`sheets_client.py` を使う全ツール）。SMS送信系・LINEログの読み取り・見積アプリのマスタ参照など |
| **止まる時間** | **新しい鍵を全スレッドに配り終えるまで。**配布先が未把握なので、**まず「誰が持っているか」の棚卸しが要る** |
| **やってよい時間帯（提案）** | **日曜の夜〜月曜の早朝**（施工が動かず、SMS配信の予定が無い時間）。**10/1 の広告判断・月初の請求処理とは重ねない** |
| 備考 | セーブデータ 965行に手順あり。**今日はやらない**と既に判断されている |

---

## 未確認（推測で埋めない）

- **漏えいの範囲**：CMOのコンテキストに入ったことは確認済み。**他スレッドにも同じ書類が渡っているか**は未確認
- **悪用の形跡**：Netlifyの配信履歴／GCPの監査ログ／LINEの送信履歴を見れば分かる。**未確認**
- **CMS・KDDI の画面の実物**：道順は一般的な想定。**開ける回に確定する**
- **本番SMSがKDDI経路かどうか**

## 分担

- **値の発行・貼り付け**：オーナー（ブラウザ担当は値に触らない）
- **差し替え後の動作確認**：ブラウザ担当（配信が通るか・シートが読めるか・LINEが送れるか）
- **貼り先の確定**：CMO（クラウド側スレッドの実態を知っているのはCMOだけ）
