# 2026_売上/顧客情報管理 のタブ整理

`1TK70pwQ8lYmjxUVCfFp1E2T5qDjHOnD4XSviZzUpB64`／**現在 71タブ**（2026-09-15 実測）

オーナー指示（2026-09-15）「シート増やしすぎだよ。使いづらくなってるから不要なシートは統合・削除を進めて」。

---

## ⚠️ 先に書く。**16タブのうち9タブは、消してはいけないものだった**

削除候補を数えたとき、私は `履歴退避_冬季見込み客_*`（9本）を「自動で溜まった控え」に分類しました。
**違いました。これはオーナーが指示したバックアップそのものです。**

> 「履歴削除：**君が責任をもって対応して。バックアップはどこか（外部、スプシやドキュメントでもいい）に
> 取って全員へ共有しておいて**」（2026-09-11 22:05 オーナー・カード回答）

2026-09-11 22:33 の履歴書き換えの**退避先がこの9本**です。
**この9本を消すと、書き換え前の状態を復元する手段が消えます。**

**だから順番が決まります。ファイル全体の Drive コピーが1つできるまで、9本には触れません。**

---

## なぜタブが増えたのか（原因を先に）

**サービスアカウントは Drive にファイルをコピーできません。**保存容量が0だからです
（`storageQuota.limit=0`。Googleの仕様で、増やす方法はない）。

CLAUDE.md の「**変更前にバックアップを取る**（スプレッドシートは Drive でコピー）」を守ろうとすると、
われわれに取れる唯一の手段が**同じファイル内にタブを複製すること**でした。

**増えた16タブは、規則を守った結果です。**ただし置き場所が間違っていました。
**Drive コピーが1回できれば、以後この増え方は止まります。**

---

## 整理の計画

### A群：**7タブ。Drive コピーができたら削除**（われわれが作った作業控え）

| タブ | 行×列 |
|---|---|
| `控え_冬季見込み客_20260915-002440` | 1035 × 25 |
| `控え_冬季見込み客_20260915-0024` | 1035 × 25 |
| `控え_冬季見込み客_20260914-1805` | 1035 × 25 |
| `控え_冬季見込み客_20260914-1754` | 1035 × 25 |
| `控え_冬季見込み客_20260911-1311` | 992 × 25 |
| `控え_業務提携_20260914-1755` | 997 × 73 |
| `控え_業務提携_20260913-2326` | 997 × 53 |

**同じ日に2本あるもの（20260915-0024 と -002440）は、片方が書き損じです。**
数分差で内容がほぼ同じなので、**新しいほう1本だけ残す、ではなく両方消します**（本体が正だから）。

### B群：**9タブ。Drive コピーができたあと、それが「9/11 の退避を含んでいること」を確かめてから削除**

`履歴退避_冬季見込み客_` の
`20260910-0431` `-0548` `-0645` `-1015` `-2350` ／ `20260911-0130` `-0146` `-0540` `-1310`

**確かめる手順（省略しないこと）：**
1. Drive コピーを開く
2. **9本が全部そこにあることを目で見る**
3. うち1本を開いて、中身が空でないことを見る
4. そこまで確認して、**はじめて本体の9本を消す**

> コピーは「いまの状態」の写しです。9本は本体の中にあるので、コピーにも入ります。
> **ただし「入っているはず」で消さないこと。**これを消し損じると復元手段がありません。

### C群：**オーナーの持ち物。こちらからは消さない。判断だけもらう**

| タブ | 見立て |
|---|---|
| `【不使用】勤怠管理` (119×14) | 名前のとおりなら消せる。ただし**勤怠が3本ある**（`【新】` `【月次】` `【不使用】`）ので、どれが生きているかはオーナーしか知らない |
| `分析_件数単価流入` (1000×52) | `_v2` (1000×26) がある。**v1が参照されている場所があるかもしれない**ので、消す前に数式の参照を調べる |
| `MTG_2026-09-04` `MTG_2026-09-08` | 議事録。消す理由がない。**目次からの導線だけ整理すれば十分** |

---

## 統合の提案（削除とは別の話）

**月次タブが24本あります**（`1月_売上/顧客` 〜 `12月_支出/成績`）。これは**減らしません。**
月ごとに分かれているのが運用の形になっていて、まとめると入力が壊れます。

代わりに **`目次` を直します。** いま42行ですが、タブは71本あるので**29本が目次から辿れません。**
「使いづらい」の実体はここだと見ています。

- 目次を**用途別に区切る**（日々の入力／月次／分析／台帳／作業用）
- **作業用の控えは目次に出さない**（そもそも人が開くものではない）

---

## 実行の順番

1. **オーナー**：ファイルを Drive でコピー（1回。所要30秒）
2. CMO：コピーに9本の退避タブが入っていることを目視
3. CMO：**A群7本を削除**
4. CMO：**B群9本を削除**
5. CMO：`目次` を作り直す
6. オーナー：C群3件の判断

**1が済むまで、2〜4は着手しません。**
`docs/sheet-changelog.md` に変更として記録してから実行します。
