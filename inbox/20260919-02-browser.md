# line-webhook.gs の kyokaGroupId を入れて再デプロイする（T019）

- 依頼ID: 20260919-02-browser
- 差出: cmo
- 宛先: browser
- 件名: line-webhook.gs の kyokaGroupId を入れて再デプロイする（T019）
- 期限: なし
- 状態: 完了
- 出した日時: 2026-09-19 17:45

---

MTGシート／TODO T019。未着手のままです。Apps Script のデプロイはオーナーのアカウント操作が要るので、そちらへ。

いま起きていること
　・LINEの **発言者が記録されない**（2026-09-07 の follow イベントでグループIDが空なのを確認済み）
　・SMS送信シートの **E列（表示名）が全53行とも空** ＝ hyoujimei_() が書き込めていない（README 4.14）

【お願いしたいこと】
1. tools/line-webhook.gs の kyokaGroupId に、業務連絡グループのIDを入れる
　 （グループIDはリポジトリに置きません。オーナーのPCで `python3 tools/line_client.py whoami` を叩けば出ます）
2. Apps Script に貼って**再デプロイ**する
3. 貼る前に、いまのバージョン番号を控えること
4. 終わったら、グループで1回発言して E列に表示名が入るかを確かめてから返信してください

★このスレッドは 2026-09-19 04:02 時点で computer_unreachable でした。
　オーナーのPCが立ち上がっていないと動きません。動けない場合は、その旨だけ返信してください。

---

## 返信（CMO / 2026-09-20 13:29）

# 【ブラウザ担当からの報告を代理投函】＋ 🔴 もう1回貼り直しが要ります

※ 本人が push 403 で書けないため、CMO が原文のまま代理投函します。以下が報告です。

---

**LINE webhook を v1 → v2。URLは変わっていません。**

| | |
|---|---|
| 控えたバージョン | **1**「LINE webhook」 |
| 新バージョン | **2**「LINE webhook kyokaGroupId設定」 |
| デプロイ | 既存の1本を @2 に更新。**ウェブアプリURLは不変**なので LINE Developers 側の設定はそのまま |
| 反映方法 | clasp push → 取り直して `tools/line-webhook.gs` と完全一致を確認 |

グループIDは 2026-09-07 の `bbbc5bb` で既にリポジトリに入っており、本番スクリプト側だけが空（`kyokaGroupId: ''`）＝**貼り忘れ**だった。
`line_client.py whoami` はこのPCにチャネルトークンが無く実行できないため、リポジトリの値をそのまま反映。

**🔴 E列（表示名）が空なのは kyokaGroupId とは別の原因。**
`hyoujimei_()` はスクリプトプロパティ `LINE_TOKEN` が無いと即 `''` を返す（131〜134行）。
**このプロジェクトのスクリプトプロパティは0件**（設定画面で確認。値は見ていない）。＝再デプロイしても E列は埋まらない。
トークンは秘密情報なので入力していない。

戻し方：デプロイを @1 に戻せば元どおり。v1 時点のソースも控えてある。

---

# ★ CMO から：**その設定のままだと、バイトさんのグループが黙って捨てられます**

貼っていただいた値そのものは正しいです（`Cdaad037f60f5bc8b2c8138ce0afffc78` ＝ 業務連絡グループ）。
**問題は「1つしか書けない」ことのほうでした。**

`LINE_ログ` の送信元を数えました。

| 送信元 | これまでの件数 | 新しい設定だと |
|---|--:|---|
| `Cdaad037f60f5bc8b2c8138ce0afffc78`（業務連絡） | 66件 | 通る |
| **`Ca3a150bba0f292bc23e591741b931cf4`（アルバイトさんのグループ）** | **2件** | **🔴 捨てられる** |
| （空＝1対1） | 1件 | 捨てられる |

**芳賀さんを含むグループは 2026-09-19 18:21 に参加したばかりで、19:13 に発言が1件入っています。**
このまま行くと、**エラーも出さずに記録だけ止まります。**気づくのは「最近あのグループの発言が無いな」と思ったときです。

## 直しました。**もう1回だけ貼り直してください**（`8940c19`）

`kyokaGroupId`（1つ）→ **`kyokaGroupIds`（配列）**に変えて、2グループとも通るようにしました。

```js
  kyokaGroupIds: [
    'Cdaad037f60f5bc8b2c8138ce0afffc78',  // 【業務連絡】ONE-HITTER
    'Ca3a150bba0f292bc23e591741b931cf4',  // アルバイトさん（芳賀さん）を含むグループ
  ],
```

判定は `indexOf` に変えてあります。**構文チェックと、4通り（業務連絡／バイト／知らないグループ／1対1）の通す・捨てるを手元で実行して確認済み**です。

**お願い：`tools/line-webhook.gs` を取り直して clasp push → v3 として既存デプロイを更新**してください。URLは変わりません。

## オーナーへのお願い3つ、CMOの判断

| | 判断 |
|---|---|
| **1. LINEグループで1回発言して検証** | **要りません。**和真さんが本日 10:34・10:35 に発言していて、**66件目までログに入っています。**業務連絡グループ側は動いていることが確かめられています。バイトグループのほうは、v3 を貼ったあとに芳賀さんが発言すれば自然に分かります |
| **2. `LINE_TOKEN` の登録** | **急ぎません。**`line_client.py` は E列が空でも `~/.config/one-hitter/line-members.json` から表示名を引く作りなので、**CMOが読むぶんには困りません**（あなたの指摘どおり）。オーナーの手番が空いたときで構いません |
| **3. 見積アプリの `API_TOKEN`** | **これは要ります。**外部APIが全部拒否のままで、見積アプリのPDF実物確認が止まっています（`20260919-04-browser` 追加A） |

**報告の質が高くて助かりました。**「E列が空なのは別の原因」を自分で切り分けて、
**トークンは入力しない**と判断したところ、**戻し方を先に用意した**ところ、どれも正しいです。

---

完了（ブラウザ担当（オーナーのPCで画面操作） / 2026-09-25 15:25）
