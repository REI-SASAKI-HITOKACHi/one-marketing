# 【広告の配信開始条件】LP4本の再配信をもう1回。LINEタップの計測が入りました（1行の差分）

- 依頼ID: 20260916-01-lp
- 差出: measurement
- 宛先: lp
- 件名: 【広告の配信開始条件】LP4本の再配信をもう1回。LINEタップの計測が入りました（1行の差分）
- 期限: なし
- 状態: 未処理
- 出した日時: 2026-09-16 22:12

---

**広告の配信が、この再配信だけで止まっています。** 変更は `tracking/measurement.json` の**1行**です。

## 何が入るか

**LINEタップが、Google広告のコンバージョンとして記録されるようになります。**

```json
"line_click": "AryNCK7n7PkcENqTjt5E"   ← これが空だった
```

**両LPがファーストビューで「LINEで無料クーポン」と誘導しているのに、
そこから来た申込が広告の成果として1件も残らない状態でした。**
10/1 にCPAで継続・停止・増額を決めるので、**効いている広告を止める判断をしかねません。**

> **GA4側では既に記録されています**（`line_click` は本番のLP4本で発火済み）。
> **足りていなかったのはGoogle広告側のラベルだけ**で、LPのHTMLの作りは変わりません。

## 取り込んでほしいもの

```
git fetch origin claude/measurement-ga4-calltracking
git cherry-pick 973b0d6        # チェックとテストの守り
git cherry-pick fcb26c9        # LINEタップのラベル
```

**`lp/` の中身には触れていません。**

## 配信の前に通してほしいもの（こちらでは全部通っています）

```
python3 tools/build-site.py netlify
python3 tools/check-tracking.py netlify       → すべて問題ありません
python3 tools/test-ads-tracking.py            → 全項目PASS
python3 tools/check-public-page.py deploy/netlify/mizumawari/index.html
```

**`check-tracking.py` は、取り込みが漏れているとこう言って止まります。**

```
× measurement.json: conversion_id は入っているのに labels.line_click が空です。
  **LINEタップが広告側に1件も届きません**（GA4には残ります）
```

**「配信の条件」を人の記憶ではなく機械に置いてあります。** 止まったら取り込み漏れです。

`test-ads-tracking.py` で見ているLINEまわり：

```
PASS mizumawari: LINEリンク 2本ぜんぶで line_click が出る
PASS mizumawari: LINEタップ → LINEのコンバージョンが撃たれる  ['AW-18450975194/AryNCK7n7PkcENqTjt5E', ...]
PASS LINEのラベルが、申込・電話と違う（同じだと入札の目標に混ざる）
```

## 配信が終わったら

返信を1行ください。**その場で `python3 tools/measure-production.py` を回して、
本番のHTMLを取ってきてブラウザで開き、実際に発火するかを確かめて結果を貼ります。**

---

**配信してから足すのでは、足す前の期間のデータが永久に欠けます**（CMO判断で広告は一時停止中）。
**この1本が、いま広告を止めている最後の作業です。**

---

## 返信（計測担当（GA4・広告タグ・電話CV） / 2026-09-16 22:18）

# 追記：**混ぜてはいけないものが1つあります。ただし、機械で見張るようにしました**

依頼の本文に書き漏らしていました。**取り込むコミットが1つ増えます（`1b0d916`）。**

```
git cherry-pick 973b0d6        # チェックとテストの守り
git cherry-pick fcb26c9        # LINEタップのラベル
git cherry-pick 1b0d916        # ← 追加。アンケートとLPの取り違えを見張る
```

## 何を混ぜてはいけないか

**アンケート（`/survey/`）にもLINEボタンが4本ありますが、あれはLPのLINEボタンとは意味が違います。**

| | 意味 | イベント |
|---|---|---|
| **LP**のLINEボタン | **公式アカウントを友だち追加**（＝これから申し込むかもしれない人） | `line_click` ← **広告の成果** |
| **アンケート**のLINEボタン | **友だちに紹介文を送る**（＝本人は**施工が終わった既存のお客様**） | `refer_share` ← **広告の成果ではない** |

**ここを混ぜると、広告費を1円もかけていない既存のお客様が「広告の成果」に化けます。**

**CPAが実態より良く見えて、出稿の判断を誤ります。** 10/1 に継続・停止・増額を決めるので、
ここが汚れていると効いていない広告を増額しかねません。

## いまは正しく動いています（実測）

`tracking/events.js` が `page_kind` で分けているので、**今日の実測でも分かれていました。**

```
survey  LINEリンク4本 → line_click 0件 / refer_share 4件
```

## ただし、口で伝えるだけだと、いつか崩れます

**`1b0d916` でテストに3項目足しました。** 配信前の `python3 tools/test-ads-tracking.py` で見張ります。

```
PASS アンケートのLINEボタン 4本は refer_share になる
PASS アンケートでは line_click を出さない（出すと既存客が広告の成果に化ける）
PASS アンケートからは広告のコンバージョンを撃たない
```

**アンケートの `page_kind` が変わったり、LINEの扱いに手が入ったりすると、ここで止まります。**
**あなたが覚えておく必要はありません。** 配信前にテストを通してもらえれば大丈夫です。

---

依頼の本体は変わりません。**LP4本の再配信1本が、いま広告を止めている最後の作業です。**
