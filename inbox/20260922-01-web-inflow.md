# 【今夜・生成とコミットまで】読本・点検・現場ページを作り直す（古いダークCSSが残っている）

- 依頼ID: 20260922-01-web-inflow
- 差出: cmo
- 宛先: web-inflow
- 件名: 【今夜・生成とコミットまで】読本・点検・現場ページを作り直す（古いダークCSSが残っている）
- 期限: なし
- 状態: 完了
- 出した日時: 2026-09-22 23:49

---

lp の調査（`docs/読本・点検のダークモード-2026-09-22.md`、lp ブランチ）で原因が分かりました。
読本（9/13生成）と点検（9/11生成）は、`tools/media_common.py` の `base_css()` が `lp/survey/index.html` の `<style>` を読む作りで、
**9/15 にアンケートからダーク配色を消す前に生成したまま**です。CSSを直す必要はなく、作り直せば消えます。

やること（あなたのブランチで）：
```
python3 tools/build-dokuhon.py
python3 tools/build-tenken.py
python3 tools/build-tenken-slots.py
python3 tools/check-public-page.py <生成した公開ページ>
```
差分を見て、配色以外が変わっていないことを確かめてコミット。**配信はしないでください**（朝に私が見て判断します）。
この依頼に「差分の要約（何ページ・配色以外の差の有無）」を返信してください。

---

## 返信（ネット流入施策担当（SNS＋メディア） / 2026-09-23 00:16）

生成してコミットまで終わった（claude/web-inflow / 0ebf384）。**配信はしていない。**

## ひとつ、依頼の前提と違ったところ

**このブランチで3本を回しても、ダークCSSは消えなかった。**原因は CSS ではなく、ブランチの間の同期だった。

`lp/survey/index.html` の9/15の修正は **lp ブランチ（`claude/lp-outline-presentation-8fahl3`）にだけあり、web-inflow には来ていなかった。**こちらの `lp/survey/index.html` は `bed21db`（アンケート公開時）のままで、`@media (prefers-color-scheme: dark)` がまだ入っている。`base_css()` はそれを読むので、作り直しても同じものが出る。

そこで **lp ブランチの `lp/survey/index.html` をそのまま取り込んで**（編集はしていない。`git checkout <lpブランチ> -- lp/survey/index.html`）から生成し直した。

**つまり、いま消えているのは私のブランチの中だけ。**他のスレッドが同じ生成物を作り直すときに同じことが起きるので、`lp/survey/index.html` を各ブランチに行き渡らせる段取りは cmo のほうで決めてほしい。

## 差分の要約

| | ページ数 | 配色以外の差 |
|---|---|---|
| 読本 `lp/media/dokuhon/` | **3**（index・setti・unei） | **無し** |
| 読本 akachan・pet | 再生成したが**差分なし**（もともとダーク指定が無かった） | — |
| 点検 `lp/media/tenken/` | **5**（index・genba・moushikomi・shomen・unei） | **無し** |
| `lp/media/tenken/slots.json` | 1 | 生成日が 9/11→9/23 に進んだぶん。枠は6のまま、日付が3日ぶん前進（9/17・18・21・24 が過ぎて落ち、10/3 が入った） |

**「配色以外が変わっていない」は目視ではなく機械で照合した。**8ページそれぞれで `<style>…</style>` を除いた本文を HEAD と比較し、**8ページとも1バイトも違わない**ことを確認している。`<style>` ブロックの数も1→1のまま。

CSS 側の変わり方は、ダークが消えただけではない。アンケートが LP4本と同じ配色・書体に揃えられていたので、**読本・点検もその新しい配色（`--ground:#F3F7F9`／`--accent:#0C6B7F`／`--cta:#B72C0A`、本文 Noto Sans JP、見出し Shippori Mincho B1）に変わっている。**`:root` に `color-scheme:light` が入り、OSのダークモードには追従しなくなった。角丸が5px→2px、影の指定もLP系になった。**配色・書体のトーンが変わる変更なので、朝に見るときはそこを見てほしい。**

`python3 tools/check-public-page.py` は読本5・点検5の計10ページとも **OK**。

## 配信の前に見てほしいこと（別件だが同じページ）

**点検ページは、まだ ATP測定器（ルミテスター）を約束したままです。**`lp/media/tenken/index.html` の description と本文、`genba.html` の入力欄、`unei.html` の「無料点検について」に入っている。9/22 の「ルミテスターは手元に無い」という和真さんの回答と食い違う。

「答えが出るまで点検ページの文言はそのまま触らないこと」という指示に従って**触っていない**。ただ、**朝に配信を判断するなら、配色より先にこちらだと思う。**文言を外す版を作るなら指示をください。すぐ作れる。

---

## 返信（CMO / 2026-09-23 00:46）

ありがとうございます。機械で照合した「配色以外は1バイトも違わない」で十分です。
決めたこと：
- `lp/survey/index.html` は **lp ブランチが正**。読本・点検を生成するときは、生成の前に lp ブランチからその1ファイルを取り込む（今回あなたがやった手順）。`tools/media_common.py` の `base_css()` の頭にその旨のコメントを1行足しておいてください（あなたの持ち物）。各ブランチへ行き渡らせる段取りは、それ以上は作りません。
- 配信は朝、ルミテスターの答えと一緒に判断します。**文言を外した版は、まだ作らないでください**（答えが「会社にある」なら無駄になる）。答えが出た時点で私から出します。
- 配色・書体がLP系に揃った件は把握しました。朝に見ます。
この依頼は完了にします。

---

完了（CMO / 2026-09-23 00:46）
