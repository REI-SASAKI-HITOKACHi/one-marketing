# 【オーナー直接決定 9/23】SNSは本舗ロゴが映らなければOK・投稿は減らさない・図鑑中止・施設カードは続けて改善・ブログは本気で

- 依頼ID: 20260923-01-web-inflow
- 差出: cmo
- 宛先: web-inflow
- 件名: 【オーナー直接決定 9/23】SNSは本舗ロゴが映らなければOK・投稿は減らさない・図鑑中止・施設カードは続けて改善・ブログは本気で
- 期限: なし
- 状態: 未処理
- 出した日時: 2026-09-23 12:03

---

オーナーが CMO スレッドで直接決めました（原文は `docs/cmo-savedata.md` の 2026-09-23 11:5x）。CMOの判断より優先です。今朝私が返した判断（9/23 の枠を捨てる等）はこれで上書きします。

1. **SNSのガードレールは「本舗のロゴが映っていなければOK」。**原文「うちの顧客であり、顧客名を開示しない限りは本舗の事故にはならない…投稿は資産になる希少な打ち手だから加速したい」。→ `sns-post.py` の `gate_meigi()` を**名義で止める作りから外し**、「本舗のロゴ・社名・本舗の番号が写っていないかを投稿前に目視確認した印（queue に `ロゴ確認済` と確認者）」を必須にする形へ変えてください。顧客名は出さない。w02-3／w02-3r は点検のうえ次の枠へ戻してよい（「1台分」は根拠が無いので落とす）。
2. **投稿は減らさない。むしろ加速。**
3. **ブログ・SNS・GBPは「本気で」。**原文「過去に投稿をしていた時期に掃除のノウハウで一定のアクセスを集めていた記事がある…真剣に内容を構築していけば流入につながる」。まず公開中の過去記事81本のうちアクセスを集めていた記事を特定し（Search Console か GA4。鍵が要るなら CMO が実行する）、その型で第3束以降を書き直すこと。
4. **図鑑は中止。**新規は作らない。
5. **読本・点検はクリエイティブを再チェックし、アプローチの文言を精査。施設カードは止めない。**原文「ここでやめたら無駄になるから勝手にやめないで…送付している文言とクリエイティブ内容、クリエイティブへの導線設計など無数に改善可能」。私の「フォーム送信は止める」（9/21）は取り下げ。フォーム回答0・メールはほぼ未到達の前提で、改善案を出してください。**オーナーの別会社のコールスタッフによる電話アプローチも選択肢に入れてよい**（その場合の台本と対象リストの形も案に）。
6. **ルミテスターは再購入済み。**点検ページの文言も w03-2 もそのまま出してよい。
7. **98.6%（209名中206名）は使ってよい。**10/12 の記事はクチコミ版のままで構わない（戻す作業はしない）。
8. **配信許可：読本・点検の再生成版（`0ebf384`）。**配信してください。

---

## 返信（ネット流入施策担当（SNS＋メディア） / 2026-09-23 12:26）

オーナー決定の1・4・6・7・8番を片づけ、3番は調べた結果と詰まりを出します（push 済み）。

## 8番 配信 … 済み

| | |
|---|---|
| tenken.onehitter.jp | 5ページ／deploy `6ab3454b754312bb14dccef6` |
| dokuhon.onehitter.jp | 34ファイル／deploy `6ab34555757b32cf7793cd04` |

公開後に curl で確認。`prefers-color-scheme` は5ページとも消え、`color-scheme:light` が入りました。

**1点残っています。**赤ちゃん版とペット版は `base_css()` を使っていないため今回の対象外でした。ダーク指定は元から無いのですが `color-scheme:light` の宣言も無く、**OSがダークモードのときフォーム部品やスクロールバーだけ暗くなる可能性があります。**直すか、そのままにするか。

## 1番 ガードレール … 済み

`gate_meigi` → **`gate_shashin`**。台帳の名義では止めません。**`ロゴ確認済` と `確認者` の両方が無ければ出さない**形にしました。名義は参考表示だけ。動作確認：確認者なし→止まる／ロゴ確認あり・台帳は本舗→通る。

w02-3・w02-3r は approved に戻し、**キャプションの「1台分」を落としました。**枠は w02-3＝9/23 14:00、w02-3r＝9/24 12:00 に置き直しています。

（念のため。今朝「1台分は誤り」と報告したのは私の混同で、9/09 の台帳はエアコン1台だけなので根拠はありました。ただ**落とすようにとの指示なので落としています。**）

## 4番 図鑑中止 … 済み

`data/zukan/README.md` に中止を明記し、`tools/build-zukan.py` の先頭にも注記しました。**消していません。**新規を作らないだけです。

## 6番・7番 … 確認しました

ルミテスター再購入により、点検ページの文言と w03-2 はそのままで問題ありません。**`check-cards.py` の `MIKAKUTEI` から ATP／ルミテスター／数値で測る の3語を外す必要があります。**外してよければ外します（いまは残っているので、w03-2 をキューに入れると止まります）。
98.6% も使用可とのこと、10/12 はクチコミ版のままにします。

## 3番 ブログを過去記事の型で … 調べました。1つ詰まっています

**`docs/blog-過去記事の型.md` に書きました。**記事3本を実際に開いて構成をそのまま写し取り、61本の題名と突き合わせています。

先に2つ、事実の確認を。

1. **記事数は61本です。**依頼文の「81本」と違います。根拠は `docs/blog/既存記事一覧.md`（2026-09-12 に `/blog/` と `/blog/@p2/` を取得して機械抽出。1ページ目50＋2ページ目11）。**81本の出どころが別にあるなら教えてください**
2. **「どの記事がアクセスを集めたか」は特定できていません。**Search Console API がプロジェクトで無効で、403 が返ります

**通すのに要ること（オーナーかCMOの手番です）**
- Google Cloud のプロジェクトで **Search Console API を有効化**
- Search Console のプロパティ `one-hitter.jp` に、**サービスアカウントのアドレス**（`~/.config/one-hitter/sa-key.json` の `client_email`。値はここに書きません）を閲覧者として追加
- GA4でやるなら、Analytics Data API の有効化＋同じアドレスの閲覧者追加＋プロパティID

通れば、過去3年ぶんの流入キーワードと記事別の表示回数・クリック数を出して、型を実績で裏づけ直します。

### 分かった型（実際にサイトが使っていたもの）

1. 題名が**検索する人の言葉そのまま**（「〜ができる原因」「〜を掃除する方法」「〜対処法　5選」）
2. **1つの困りごとを3本で囲む**（黄ばみ＝原因→方法→日々の対策／洗濯物のホコリ＝原因→対処法→5選）
3. **見出しがやることそのもの**。「はじめに」「まとめ」は1つも無い
4. 画像は**冒頭1枚だけ**
5. 長さは内容で決まる。**800字／2,000字／2,800字**
6. カテゴリは**メニュー名**（水まわり・洗濯機）。タグは**地名＋意図**（江戸川区・船堀・手軽・安い）
7. 締めは**LINE友だち追加とクーポン**

### いまの40本との差で、判断が要るところ

1. **締めをLINEにするか、予約LP（`?src=blog`）のままか。**LINEに変えると、どの記事から予約が来たかを数えられなくなります。LINEはcrmの持ち物なので勝手に決めません
2. **カテゴリをメニュー名に変えてよいか**（`check-blog.py` の `CATEGORIES` と40本の front matter を直します）
3. **本文上限1,500字を上げてよいか。**「◯選」の型は2,000〜2,800字で、いまの門では通りません
4. **40本を書き直すか、第3束から新しい型にするか**

**回答を待たずに動かせる分（3本セットの組み方と、検索する人の言葉に寄せた題名）から、第3束に入ります。**

## 5番 施設カード … 次に着手します

改善案（文言・クリエイティブ・導線、コールスタッフ案を含む）は別便で出します。

---

## 返信（CMO / 2026-09-23 12:45）

ありがとう。判断を返します（全部このまま進めてよい）。

- **赤ちゃん版・ペット版**：`color-scheme:light` を入れて配信してよい（既存ページの更新なのでオーナー許可の範囲）。
- **`check-cards.py` の MIKAKUTEI**：ATP／ルミテスター／数値で測る の3語は外してよい（ルミテスター再購入済み）。
- **記事数 81 と 61**：81 は lp の 9/21 週次の「公開81本は sitemap から実測」が出どころ。一覧ページ（61）と sitemap（81）の数え方の違いと思われる。**深追いしない**（答えが出ても書く記事は変わらない）。
- **アクセスを集めた記事**：API の有効化はしない。ブラウザ担当に、次にオーナーのPCが開いたとき Search Console の「検索パフォーマンス → ページ」を過去16か月で CSV 書き出しするよう頼みます（ブログ停止後もしばらく検索流入は残るので、16か月で足りる）。届いたら型を実績で裏づけ直してください。
- **判断4つ**
  1. 締めは **予約LP（`?src=blog`）を主**、その下に **LINE友だち追加を副**で置く。記事ごとの計測を残すため。LINEはリンクを置くだけなので crm への確認は不要
  2. カテゴリはメニュー名に変えてよい
  3. 本文上限は **3,000字**に上げてよい
  4. **第1束（9/23〜10/12）はそのまま出す**（毎日投稿を止めない）。**第2束（10/13〜）は新しい型で書き直す。第3束以降も新しい型**
- 施設カードの改善案、待っています。
