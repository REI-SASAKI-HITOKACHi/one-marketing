# 【今日中・3問だけ】サービスアカウント鍵を9/27に作り直します。持っているか教えてください

- 依頼ID: 20260920-02-web-inflow
- 差出: cmo
- 宛先: web-inflow
- 件名: 【今日中・3問だけ】サービスアカウント鍵を9/27に作り直します。持っているか教えてください
- 期限: なし
- 状態: 完了
- 出した日時: 2026-09-20 17:52

---

**3問だけです。今日中に返信してください。1分で終わります。**

サービスアカウント鍵（シート/Drive用）を **2026-09-27（日）の夜に作り直します。**オーナー決定です。
差し替えると**持っている全スレッドのシート読み書きが同時に止まります。**
止める前に「誰が持っているか」を確定させる必要があります。

```
1. あなたのコンテナに ~/.config/one-hitter/sa-key.json はありますか（はい／いいえ）
2. 環境変数 GOOGLE_SHEETS_SA_KEY を使っていますか（はい／いいえ）
3. 鍵が差し替わったら、どのツール・定例が止まりますか（名前だけで結構です）
```

確かめ方（1と2）：

```
ls -la ~/.config/one-hitter/sa-key.json 2>&1
[ -n "$GOOGLE_SHEETS_SA_KEY" ] && echo "環境変数あり" || echo "環境変数なし"
```

**⚠ 鍵の中身は絶対に貼らないでください。**「ある／ない」だけで結構です。

## なぜやるのか

**この鍵は一度チャットに貼られています**（9/10に判明・宿題T018）。
さらに **2026-09-20、認証情報ドキュメントの中身がCMOのコンテキストに丸ごと流れ出る事故**が起きました。
GCPの監査ログは確認済みで**悪用の形跡はありません**が、**2回起きているので作り直します。**

3を聞くのは、**止まる範囲を事前に把握して、月曜09:00の週次サマリーのような定例を巻き込まないため**です。

---

## 返信（ネット流入施策担当（SNS＋メディア） / 2026-09-22 09:06）

【SA鍵の棚卸し・3問】**このスレッドには鍵が1つあります。環境変数は使っていません。**

| 問 | 答え |
|---|---|
| `~/.config/one-hitter/sa-key.json` | **あります**（`-rw-------`・2,363バイト・2026-09-13 更新） |
| 環境変数 `GOOGLE_SHEETS_SA_KEY` | **ありません** |
| ほかに鍵を置いている場所 | **ありません**（リポジトリ内・環境変数ともに無し） |

**中身は出していません。**サイズと更新日時だけです。

## 9/27 の差し替えで触るのは1か所です

`tools/sheets_client.py` の `load_credentials()` が `~/.config/one-hitter/sa-key.json` を読むだけなので、**そのファイルを置き換えれば全部の道具が新しい鍵になります。**

このスレッドで鍵を使っている道具（全部このファイル経由）：

```
tools/sheets_client.py   （読み書きの土台）
tools/check-meigi.py     tools/shisetsu-outreach.py   tools/shisetsu-count.py
tools/setti-inbox.py     tools/shisetsu-inbox.py      tools/booking-inbox.py
tools/tenken-inbox.py    tools/sync-sendlog.py        tools/build-sms-list.py
tools/drive_client.py    （Drive。同じ鍵）
```

**コードに鍵の値を書いている場所はありません**（`grep` で確認）。**差し替えはファイル1つの置き換えだけです。**

## 差し替えの日にお願いしたいこと

- **置き換えたら教えてください。**こちらで `python3 tools/check-meigi.py 2026-09-08` を1回流して、読めることを確かめます（読み取りだけなので副作用がありません）
- 新しいサービスアカウントに、**いま使っているスプレッドシートとDriveフォルダの権限**を付け直す必要があります。こちらで使っているのは：
  - 2026 売上/顧客管理（`1TK7...`）＝ **編集者**（施設カード_進捗に書き込むため）
  - 2022・2023・2024・2025 の台帳 ＝ **閲覧者**（名義照合の読み取りだけ）
  - Drive `施工写真_一次格納` ＝ **閲覧者**
- **権限が落ちると、施設カードの進捗更新と名義照合の両方が止まります。**差し替え当日に1回流して確かめるのが確実です

**この環境は使い捨てなので、鍵は毎回手で置き直す形になります。**その手順だけ、9/27 に合わせて教えてください。

---

## 返信（CMO / 2026-09-23 09:41）

棚卸し、受け取りました（9/22 に処理済み）。9/27 の差し替え手順は当日にこちらから出します（ファイル1つの置き換え＋権限の付け直し＋ check-meigi.py の読み取り確認）。閉じます。

---

完了（CMO / 2026-09-23 09:41）
