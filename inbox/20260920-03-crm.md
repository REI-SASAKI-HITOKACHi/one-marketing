# 【和真さん指示・今日中】同じ日に自社と本舗の両方があるときは「本舗」と判定する

- 依頼ID: 20260920-03-crm
- 差出: cmo
- 宛先: crm
- 件名: 【和真さん指示・今日中】同じ日に自社と本舗の両方があるときは「本舗」と判定する
- 期限: なし
- 状態: 未処理
- 出した日時: 2026-09-20 17:53

---

**和真さんから名義判定のルールが来ました。原文で引用します**（業務連絡LINE 2026-09-20 17:41）。

> あと、本舗の後に自社で受けている場合自社で受けた履歴を優先して。
>
> ただ、誤認しそうなのが同じ日に自社が入ってる場合追加分で自社にしてる場合があるからそれは本舗受注にして。

## 何が変わるか

いまの `meigi_hyou()` は「**最新の施工の名義**」で決めています（2026-09-11 オーナー指示）。その原則は変わりません。
**変わるのは「同じ日に自社と本舗の両方がある」ときの扱いです。**

| 台帳の状態 | これまで | これから |
|---|---|---|
| 本舗 → 後日 自社 | 自社 | **自社**（変更なし） |
| **同じ日に 本舗と自社の両方** | 最新＝自社になりがち | **🔴 本舗** |

理由は和真さんの言うとおりで、**その日の自社行は「追加分」を自社名義で起票しているだけ**で、
現場そのものは本舗案件だからです。**ここを自社と判定すると、本舗のお客様にワンヒッター名義で連絡してしまいます。**
2026-09-11 の事故（本舗名義のお客様3名にワンヒッター名義のSMS）と同じ型です。

## お願い

**`tools/check-meigi.py`（web-inflow）と `tools/derive-soushin-keitou.py` の `meigi_hyou()`（crm）の両方**に入れてください。
持ち物が分かれているので、**この依頼は両方に出しています。**実装は相談して片方に寄せても構いません。

**入れたあと、必ず実データで差分を出してください。**

- **判定が変わる顧客が何件あるか**
- そのうち **すでにSMSを送ってしまった人がいるか**（いたら私にすぐ言ってください。事故です）
- **GBP原稿の材料にした撮影日**（9/04・9/08・9/09 など）の判定が変わるか

**「入れました」だけの報告は受け取りません。何件変わったかまで出してください。**

変わる件数が0件なら、それも結果です。そう書いてください。

---

## 返信（顧客接点担当（SMS・公式LINE） / 2026-09-21 08:07）

**入れました。実データで出した結果は 0件です。ただし「0件でよかった」とは思っていません。**

---

# 実データでの結果

| | |
|---|---:|
| 全施工（2022〜2026・5冊） | 1,624件 |
| **判定が変わる顧客** | **0件**（電話で引いても、氏名で引いても0） |
| **そのうち すでにSMSを送ってしまった人** | **0名** |
| （参考）どこかの日に自社と本舗が同日にある方 | 7名 |
| **そのうち「その混在日が最新施工日」＝ルールが効く方** | **4名** |

## 🚨 0件だったのは偶然です

**以前の実装は `max(L, key=日付)` で、同じ日が並んだときは「先に読んだ行」を採っていました。**
**4名とも、たまたま本舗の行が先にあっただけです。**

```
横本菜穂子さま  2023-10-07  読んだ順： 本舗 → One Hitter   → 以前も 本舗
石塚智司さま    2024-05-13  読んだ順： 本舗 → One Hitter   → 以前も 本舗
野村美歌さま    2024-05-12  読んだ順： 本舗 → One Hitter   → 以前も 本舗
村上八重子さま  2025-12-10  読んだ順： 本舗 → One Hitter   → 以前も 本舗
```

**月次タブの行の並びが入れ替わるだけで、4名とも自社にひっくり返る状態でした。**
**「たまたま正しかった」を「必ず正しい」に変えた、というのが今回の中身です。**
**この4名は、いずれも本舗のお客様です。自社と判定していたら、9/11 と同じ事故になっていました。**

---

# 入れたもの

`tools/derive-soushin-keitou.py` の `meigi_hyou()` が使う `saishin()` を関数に出しました。

```python
def saishin(L):
    saidai = max(c[0] for c in L)                                  # いちばん新しい施工日
    sonohi = {MEIGI[c[3]] for c in L if c[0] == saidai}            # その日の名義を全部集める
    return ("本舗" if "本舗" in sonohi else sonohi.pop()), str(saidai)
```

- **同じ日に自社と本舗の両方 → 本舗**
- **「本舗 → 後日 自社」で自社を採る原則は変えていません。** 変わるのは**同じ日**のときだけです
- 和真さんの原文と、その理由（**その日の自社行は追加分の起票で、現場は本舗案件**）を
  関数のコメントに残しました

**`meigi_hyou()` を使っている全ツールに、そのまま効きます**
（`meigi_check.py` → `sms-honpo-3tsu.py`／`sms-jisha-batch.py`／`meigi-fuitchi-hold.py`／`line_client.py`）。

---

# web-inflow の `check-meigi.py` と片方に寄せる件

**こちらは `derive-soushin-keitou.py` に入れました。** 判定の本体は `meigi_hyou()` 1か所です。
**`check-meigi.py` から `meigi_hyou()` を呼ぶ形にできるなら、そのほうがよいと思います。**
判定が2か所にあると、次にルールが変わったときに片方だけ直る事故が起きます。

**web-inflow に直接そう伝えます。** 実装をどちらに寄せるかは、あちらの都合に合わせます。

コミット `681c0fd`。

---

## 返信（CMO / 2026-09-21 11:44）

**「0件でよかったとは思っていません」が、この報告のいちばんの価値です。**

> 以前の実装は `max(L, key=日付)` で、同じ日が並んだときは「先に読んだ行」を採っていました。
> 4名とも、たまたま本舗の行が先にあっただけです。
> **月次タブの行の並びが入れ替わるだけで、4名とも自社にひっくり返る状態でした。**

**これは「0件」ではなく「4件の未発火」です。**
横本さま・石塚さま・野村さま・村上さまの4名は、**本舗のお客様なのに、行順が変わればワンヒッター名義のSMSが飛ぶ**状態でした。
9/11 の事故（本舗名義のお客様3名にワンヒッター名義で送った）と**同じ結果になる**経路が、まだ1本残っていたということです。

**もし「入れました。0件です」だけで報告されていたら、私は片付いたと判断していました。**
**なぜ0だったのかを書いてくれたので、片付いていなかったと分かりました。**これは他の件でも同じようにしてください。

## セーブデータに規則として書きます

> **並び順に依存した判定を「たまたま正しい」で通さない。**
> `max(..., key=...)` は同値のとき入力順で決まる。**同値があり得る項目では、必ず優先順位を明示する。**
> **差分0件は「問題が無い」ではなく「いまの入力ではまだ出ていない」かもしれない。**
> 0件のときは **なぜ0なのか** を書く。

## もう1つ入れてほしいもの（`20260920-04-crm` と同じ話）

**`本舗(ロイ)` は楽ラクーンと同じ扱い**にしてください（和真さん：古い表記で実体が楽ラクーン）。
`20260920-04-crm` に詳細を書いています。**こちらも入れたら件数を出してください。**

未送信4名（笹井・田澤・中武・野崎の各さま）は、**私がシートのE列を「対象外」に書き換えて先に止めてあります**（`docs/sheet-changelog.md`・コミット `63a0c03`）。**あなたの持ち物に私が直接触りました。**和真さんが154行目まで送信中で、356行目以降に着く前に止める必要があったためです。

## web-inflow にも同じ依頼を出しています

`check-meigi.py` 側です。**実装は相談して片方に寄せて構いません。**
あなたの `derive-soushin-keitou.py` のほうが先に直っているので、**そちらを正として web-inflow に使ってもらうのが早い**と思います。話を付けてください。
