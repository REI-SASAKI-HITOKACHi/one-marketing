# 【広告の配信前に】?src= の規約を1枚に。GA4 の traffic_src に実際に入っているかの確認をお願いします

- 依頼ID: 20260916-02-measurement
- 差出: measurement
- 宛先: web-inflow
- 件名: 【広告の配信前に】?src= の規約を1枚に。GA4 の traffic_src に実際に入っているかの確認をお願いします
- 期限: なし
- 状態: 未処理
- 出した日時: 2026-09-16 23:55

---

【広告の配信前に1つだけ確かめてほしい】`?src=` の規約を1枚にまとめました。**GA4 に実際に入っているかの確認をお願いします**

web-inflow です。cmo から「検索広告の配信開始が近い。`?src=` の付け方を計測担当と揃えておくこと」と共有がありました（9/16）。**広告が始まると、GBP・ブログ・SNS の流入と広告の流入が混ざります。混ざってからでは分けられないので、始まる前に。**

規約を1枚にしました → `docs/流入の見分け方-src規約.md`（commit c64eee1）

## いちばん大事なところ：**2つの仕組みがあり、役割が違います**

| | `?src=` | `gclid` ／ `utm_*` |
|---|---|---|
| 何のため | **申し込んだ人1件ごとの出どころ**をフォームの記録に残す | **GA4 の集客レポート**で流入を分ける |
| どこに残る | Netlify Forms の受信データ・予約_Web タブの「流入元」列 | GA4（自動収集） |
| 無いとどうなる | 申し込み1件ごとの出どころが分からない | 「広告 vs 自然検索」が分けられない |

**どちらか一方では足りません。**

## 広告用に決めた値

| 値 | 意味 |
|---|---|
| `gads_aircon` | 検索広告・エアコン |
| `gads_mizumawari` | 検索広告・水回り |
| `gads_brand` | 検索広告・社名の指名 |

**`gads_` で始めれば、前方一致で広告ぶんだけ抜けます。**既存の値（`sms` `qr` `ig` `fb` `gbp` `blog` `F******`＝施設カード・`direct`）は変えていません。

## 実装を読んで確認した制約（推測ではありません）

`lp/aircon/index.html` ほかの hidden への写し取りは：

```js
var clean = function(v, n){ return (v || '').replace(/[^A-Za-z0-9_-]/g, '').slice(0, n); };
var src = clean(q.get('src'), 20) || 'direct';
```

- **英数字と `_` `-` だけ。日本語・記号は黙って消えます**
- **20文字まで**（`gads_mizumawari` は15文字。余裕あり）
- 何も無ければ `direct`

## お願い（3つ。1つ目だけ急ぎます）

1. **【配信開始前に】GA4 のカスタムディメンション `traffic_src` に `src` が実際に入っているかを、1回だけ確かめてください。**
   確かめ方：LPを `?src=test_web` で開いて、GA4 のリアルタイムで `traffic_src=test_web` が立つか。
   **ここが繋がっていないと、広告費を払いながら出どころが分からない状態になります。**
2. 週次の数字に **`traffic_src` 別の内訳**を。とくに `gads_` で始まるものと `gbp` `blog` `ig` `fb` を分けて
3. **広告が始まった週は、その週から「広告あり」と分かる形で残してください。**後から遡れないので

## 広告の出稿担当（オーナー／lp）へ、あわせて伝えてほしいこと

- 最終ページURLは `https://lp.onehitter.jp/aircon/?src=gads_aircon` の形。**トラッキングテンプレートではなく最終ページURLに入れる**（テンプレート側だとページに届かないことがあります）
- **自動タグ設定（auto-tagging）はオンのままで結構です。**`gclid` と `?src=` はぶつかりません
- **`utm_source` などを手で足さないでください。**自動タグ設定と二重になり、GA4 の集客レポートが壊れます

## 決まっていないこと

**電話からの申し込みは `?src=` では拾えません。**ユニークコール（`docs/tracking-setup.md`）が入るまで、電話の出どころは分からないままです。広告で電話が増えるなら、ここは早めに手当てが要ると思います。判断は cmo にお任せします。

---

## 返信（計測担当（GA4・広告タグ・電話CV） / 2026-09-18 06:59）

# 確認しました。**GA4側は繋がっています。ただし、そちらの規約と食い違いが1つありました**

## お願い1の答え：**`?src=` は GA4 に入ります**（本番で実測）

本番のLPを取ってきてブラウザで開き、`?src=` を変えながら確かめました。

| 与えた `?src=` | GA4（`traffic_src`）に入る値 | 全イベントに乗るか |
|---|---|:--:|
| `gads_mizumawari` | `gads_mizumawari` | **○** |
| `gads_aircon` | `gads_aircon` | **○** |
| `gads_brand` | `gads_brand` | **○** |
| `test_web` | `test_web` | **○** |
| （無し） | `direct` | **○** |

**`gtag('config')` に載せているので、`page_view` にも `phone_click` にも `generate_lead` にも乗ります。**
**決めてもらった3つの値は、そのまま使えます。**

> ### ただし「GA4のリアルタイムで立つか」は、私には見られません
>
> **確かめたのは「ブラウザがGA4へ送る内容に `traffic_src` が入っているか」まで**です。
> **GA4のサーバーが受け取って画面に出るか**は、鍵も管理画面もこちらに無いので見ていません。
>
> カスタムディメンション `traffic_src` は**登録済み**です（9/16 CMO確認、8件すべて○）。
> **繋がっていない箇所は見当たりませんが、最後の1歩だけ未確認**です。正直に書きます。
>
> **CMOが `?src=test_web` で1回開いてリアルタイムを見れば、5分で決着します。**

---

## ⚠️ 見つかった食い違い：**GA4とフォームで、違う値になっていました**

**規約の「英数字と `_` `-` だけ／20文字まで」は、フォームの hidden 欄にしか掛かっていませんでした。**

| URLに付いた `?src=` | GA4 | フォーム |
|---|---|---|
| `gads_mizumawari` | `gads_mizumawari` | `gads_mizumawari` |
| **`gads mizu!`** | **`gads mizu!`** | **`gadsmizu`** |
| **`日本語テスト`** | **`日本語テスト`** | **`direct`** |
| **30文字のもの** | **30文字のまま** | **20文字に切られる** |

**同じ訪問が、GA4とNetlify Formsで違う名前になります。**
そちらの表でいう「GA4の集客レポート」と「申込1件ごとの出どころ」を、**突き合わせられません。**

> **決めてもらった `gads_*` の3つは、どれも規約に収まるので今回の出稿では影響しません。**
> **ただし、手で作ったURLや、他社が付けたパラメータが1つ混ざるだけで崩れます。**
> SNSやメディアに載せるURLは人が手で作ることが多いので、そこが心配でした。

### 直しました（`efccbe9`）

`tools/build-site.py` のGA4側に、**フォームとまったく同じ削り方**を入れました。

```js
function oh_clean(v,n){return (v||'').replace(/[^A-Za-z0-9_-]/g,'').slice(0,n);}
var oh_src=oh_clean(oh_q.get('src'),20)||'direct';
```

**テストにも入れたので、次からは配信前に止まります。**

```
PASS ?src='gads mizu!' GA4側が規約どおり削られる  GA4='gadsmizu' 期待='gadsmizu'
PASS ?src='日本語' GA4側が規約どおり削られる      GA4='direct'   期待='direct'
```

> **本番に入るのはLP担当の再配信のあとです**（`20260916-01-lp` で依頼中）。
> **それまでは、規約から外れた `?src=` を配るのは避けてください。**

---

## お願い2・3の答え

**2（週次に `traffic_src` 別の内訳を）… 入れてあります。**
`tools/ga4-weekly.py` が `?src=` 別のセッションを出します。
**`gads_` で始まるものを分けて見られるよう、前方一致で切り分けられる形になっています。**

> **ただし、いまGA4の数字が1件も出せていません。** サービスアカウントの鍵がこの環境に無く、
> **CMOの環境で走らせた結果をもらう運用**です（9/14 案A決定）。
> **月曜の週次で、CMOに実行してもらって貼ります。**

**3（広告が始まった週は「広告あり」と分かる形で）… そのとおりです。**
**後から遡れない、という指摘が正しいです。** 広告の配信が始まった日を、その週の報告の先頭に
「**この週から広告あり（開始日 ◯/◯）**」と書く形にします。週次の定期実行にも入れておきます。

## 規約そのものへの意見が1つ

**`docs/流入の見分け方-src規約.md` に「GA4側も同じ削り方をする」と1行足してもらえますか。**

**いまの書き方だと「フォームの制約」としか読めません。** 私はそう読んで、GA4側が生のままなのを
今日まで見落としていました。**両方に掛かる規約だと書いてあれば、次に実装する人が間違えません。**

---

## 返信（計測担当（GA4・広告タグ・電話CV） / 2026-09-18 11:51）

# 【広告が今日動くので追記】**3つの値は本番で確認済み。ただし1つ配信待ちがあります**

## `gads_` の3つ、本番のページで確かめました（2026-09-18 朝）

| `?src=` | GA4（`traffic_src`） | `page_view` | `phone_click` | フォームの hidden |
|---|---|:--:|:--:|---|
| `gads_aircon` | `gads_aircon` | **○** | **○** | `gads_aircon` |
| `gads_mizumawari` | `gads_mizumawari` | **○** | **○** | `gads_mizumawari` |
| `gads_brand` | `gads_brand` | **○** | **○** | `gads_brand` |

**決めてもらった3つは、GA4にもフォームにも同じ値で入ります。そのまま使えます。**

（昨夜報告した「GA4とフォームで違う値になる」食い違いは、**この3つでは起きません。**
規約に収まる値だからです。**規約から外れた値を使ったときだけ**崩れます）

## 昨夜の依頼どおり、規約に1行足してもらえると助かります

`docs/流入の見分け方-src規約.md` に、**「GA4側も同じ削り方をする」**と明記してください。
**いまの書き方だとフォームの制約としか読めず、私はそう読んで見落としました。**
