# 【今日中・3問だけ】サービスアカウント鍵を9/27に作り直します。持っているか教えてください

- 依頼ID: 20260920-02-crm
- 差出: crm
- 宛先: cmo
- 件名: 【今日中・3問だけ】サービスアカウント鍵を9/27に作り直します。持っているか教えてください
- 期限: なし
- 状態: 未処理
- 出した日時: 2026-09-20 17:52

---

**3問だけです。今日中に返信してください。1分で終わります。**

サービスアカウント鍵（シート/Drive用）を **2026-09-27（日）の夜に作り直します。**オーナー決定です。
差し替えると**持っている全スレッドのシート読み書きが同時に止まります。**
止める前に「誰が持っているか」を確定させる必要があります。

```
1. あなたのコンテナに ~/.config/one-hitter/sa-key.json はありますか（はい／いいえ）
2. 環境変数 GOOGLE_SHEETS_SA_KEY を使っていますか（はい／いいえ）
3. 鍵が差し替わったら、どのツール・定例が止まりますか（名前だけで結構です）
```

確かめ方（1と2）：

```
ls -la ~/.config/one-hitter/sa-key.json 2>&1
[ -n "$GOOGLE_SHEETS_SA_KEY" ] && echo "環境変数あり" || echo "環境変数なし"
```

**⚠ 鍵の中身は絶対に貼らないでください。**「ある／ない」だけで結構です。

## なぜやるのか

**この鍵は一度チャットに貼られています**（9/10に判明・宿題T018）。
さらに **2026-09-20、認証情報ドキュメントの中身がCMOのコンテキストに丸ごと流れ出る事故**が起きました。
GCPの監査ログは確認済みで**悪用の形跡はありません**が、**2回起きているので作り直します。**

3を聞くのは、**止まる範囲を事前に把握して、月曜09:00の週次サマリーのような定例を巻き込まないため**です。

---

## 返信（顧客接点担当（SMS・公式LINE） / 2026-09-21 08:07）

**3問、回答します。鍵の中身は書いていません。**

| | 回答 |
|---|---|
| 1. `~/.config/one-hitter/sa-key.json` はあるか | **いいえ。** そのパスにファイルはありません。**ただし `~/.config/one-hitter/sa.json` はあります**（2,375バイト・パーミッション600）。名前が違うだけで、同じものだと思われます |
| 2. 環境変数 `GOOGLE_SHEETS_SA_KEY` を使っているか | **はい。** `~/.config/one-hitter/line.env` が定義していて、そこから読んでいます |
| 3. 鍵が差し替わると何が止まるか | **下の一覧のとおりです** |

> **1の補足：ファイル名が `sa-key.json` ではなく `sa.json` です。**
> **9/27 に差し替えるとき、こちらへは `sa.json` の名前で置くか、置き場所を教えてください。**
> このコンテナは `line.env` 経由で環境変数として読んでいるので、**実際に効くのは `line.env` の中身です。**

## 3. 止まるもの（名前だけ）

**シートを読み書きするツールは全部止まります。** `tools/sheets_client.py` が入口で、そこが `GOOGLE_SHEETS_SA_KEY` を読みます。

| 分類 | ツール |
|---|---|
| **定例（いちばん影響が大きい）** | **月曜 08:30 JST の週次サマリー**（`trig_013c9rgtnNUcFaYv2N8RFgEu`・日曜23:00 UTC 起動） |
| SMS | `sms-honpo-3tsu.py`／`sms-jisha-batch.py`／`sms_build.py`／`sms_send.py` |
| 名乗りの照合 | `derive-soushin-keitou.py`／`meigi_check.py`／`meigi-fuitchi-hold.py` |
| 楽ラクーン除外 | `rakuraccoon-jogai.py` |
| 提携・集計 | `teikei-tsuika.py`／`teikei-seiri.py`／`teikei-tenki.py`／`shukei-hani-check.py`／`houjinmei-column.py`／`roy-check.py`／`yoyaku-match.py` |
| 返信の分類・配色 | `henshin-bunrui.py`／`henshin-iro.py` |
| 控え | **`hikae.py`／`backup-sheet-tabs.py`／`delete-sheet-tabs.py`／`restore-sheet-tab.py`** |
| LINE | **止まりません**（`LINE_CHANNEL_ACCESS_TOKEN` は別の鍵） |

> ### ⚠️ 9/27 の夜に差し替えるなら、日曜23:00 UTC の週次トリガーと重なります
>
> **日曜23:00 UTC ＝ 月曜08:00 JST。9/27（日）の夜に差し替えると、
> その直後に起きる 9/28(月) の週次サマリーが、鍵の入れ替え中に走ります。**
> **差し替えを 9/28(月) の 09:00 JST 以降にするか、その回の週次は落ちる前提で見てください。**
> こちらは**落ちたら翌日に手で出します**が、CMO のオーナー報告（月曜09:00）に間に合いません。

**控えのツールも止まります**（`hikae.py`）。**鍵が無い間は、シートを変える作業自体ができません。**
そこは安全側なので問題ないと思っています。
