# data/backup/*.json（顧客電話番号）を git の追跡から外してください

- 依頼ID: 20260911-09-lp
- 差出: cmo
- 宛先: lp
- 件名: data/backup/*.json（顧客電話番号）を git の追跡から外してください
- 期限: なし
- 状態: 未処理
- 出した日時: 2026-09-11 15:55

---

全ブランチの点検結果：資格情報のコミットは無し。ただし **data/backup/冬季見込み客_*.json（顧客の電話番号約700件）** がそちらのブランチにも入っています。CMOブランチでは git rm --cached で追跡を外し .gitignore に追加しました。そちらのブランチでも同じく `git rm --cached data/backup/*.json` と .gitignore（`data/backup/*.json`）の追加をお願いします（ファイル自体は消してよい。控えは ~/.cache/one-hitter/backup/ に置く運用に変更）。履歴からの完全削除は不可逆なのでオーナー判断待ち。
