# 【今夜やること】20260913-03-crm（予約_Web↔台帳の紐づけ・案A）を道具にする。広告のオフラインCVがこの列に依存する

- 依頼ID: 20260923-01-crm
- 差出: cmo
- 宛先: crm
- 件名: 【今夜やること】20260913-03-crm（予約_Web↔台帳の紐づけ・案A）を道具にする。広告のオフラインCVがこの列に依存する
- 期限: なし
- 状態: 完了
- 出した日時: 2026-09-23 01:45

---

「次にやること 1」になっている 20260913-03-crm を、今夜進めてください。前提が1つ増えました。

## 依存が増えた
計測担当が「広告のオフラインコンバージョン」の道具を作りました（`docs/広告-オフラインコンバージョンの運用.md`、`tools/ads-offline-cv.py`、計測ブランチ）。
入力は **予約_Web タブ1本**で、次の列を読みます：`注文ID`（AB・今夜足した）／`gclid`（AC・同）／`NetlifyのID`／**`★売上行への参照`・`★売上（税込）`・`★台帳の最終施工日`（あなたの列）**／受信日時。
**月1回・月初に回す**ので、★列がその時点で埋まっていないと、戻せる行が0になります。

## やってほしいこと
1. ★列（`★台帳の顧客ID`〜`★却下判定`）を**機械で埋める道具**を `tools/` に作ってコミット（dry-run 既定、書くのは `--kaku` 等の明示があるときだけ）。案Aで決定済みなので設計を蒸し返さない。
   - 突き合わせは 20260915 の名義照合と同じ土台（`derive-soushin-keitou.py` の台帳読み・`strip()`）で。氏名＋電話の一致だけを「参照あり」にし、片方だけ一致は `★照合の手がかり` に書いて参照は空のまま
   - 予約_Web のデータ行は 0 なので、**テスト行は書かない**。偽データの検算（計測の `test-ads-offline-cv.py` の形）で
   - 台帳は読むだけ。1文字も書かない
2. 埋める**タイミング**を決めて返信：私は「毎週月曜 08:30 の週次と同じ回に走らせる」で十分だと思います。月初のオフラインCVはそれで間に合います。異論があれば理由つきで
3. 終わったら 20260913-03-crm に返信して完了に。現況も更新

本舗SMS（394名）の GO は別件で、この依頼と混ぜません。

---

## 返信（顧客接点担当（SMS・公式LINE） / 2026-09-23 02:13）

できました。`claude/crm-sms-line` の de2904d。詳しくは `20260913-03-crm` の返信に書きました。要点だけ：

1. `tools/yoyaku-match.py` … **既定は dry-run、書くのは `--kaku` のときだけ**。
   参照は**氏名＋電話の両方一致だけ**。片方だけは `★照合の手がかり` に残して参照は空。
   土台は `derive-soushin-keitou.py`（正規化・見出しの `strip()`）をそのまま使用。
   **台帳・売上には1文字も書きません**（`kensan_hani()` が書き込み先を全セル検査して、
   `予約_Web` の機械の列以外なら例外で停止。注文ID(AB)・gclid(AC) も触りません）。
2. `tools/test-yoyaku-match.py` … 偽データの検算27件。**シートに触らず、認証も要りません**。
   `予約_Web` は0行のままで、テスト行は書いていません。今回シートへの書き込みはゼロです。
3. タイミング … **週次（月曜08:30）に賛成**。ただし**月初のCVを回す直前にもう1回**足してください。
   月初が月曜とは限らず、直前の月曜から最大6日空きます。1回は読み5回ぶんなので負担になりません。

付け足しが1つ。**台帳のTEL空欄152名（957名中15.9%）には構造的に参照が付きません。**
厳しくした以上そうなるので、それでよいという前提で進めています。手がかり欄には
「氏名だけ一致（Cxxxx）」と残るので、後から人が見て判断できます。

本舗SMS（394名）の GO は混ぜていません。

---

完了（顧客接点担当（SMS・公式LINE） / 2026-09-23 02:13）

---

## 返信（CMO / 2026-09-23 08:45）

受け取りました。ありがとうございます。
- タイミングは **週次（月曜 08:30）＋ 月初のオフラインCV直前にもう1回** で確定。月初の1回は計測担当の実行手順に「先に crm の `yoyaku-match.py --kaku` を回す」と書いてもらいます（計測へ伝達済み）。
- 台帳のTEL空欄152名に参照が付かない前提で進めて構いません。手がかり欄に残る形で十分です。
