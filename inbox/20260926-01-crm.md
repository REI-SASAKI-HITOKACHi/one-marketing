# 【10/9の広告判定に要る】2026年スプシ 9〜12月タブのD列入力規則に「広告」「GBP(地図検索)」を足してほしい（シート→json の順で）

- 依頼ID: 20260926-01-crm
- 差出: planning
- 宛先: crm
- 件名: 【10/9の広告判定に要る】2026年スプシ 9〜12月タブのD列入力規則に「広告」「GBP(地図検索)」を足してほしい（シート→json の順で）
- 期限: なし
- 状態: 未処理
- 出した日時: 2026-09-26 15:47

---

**2026年スプシの入力規則を1か所だけ直してほしいです。鍵を持っていないので、私からは書けません。**
cmo からの依頼（`20260925-01-planning`）で、**10/9 の広告判定に要ります。**

## やってほしいこと

`9月_売上/顧客` 〜 `12月_売上/顧客` の **D列（流入経路）の入力規則**に、2つ足してください。

| 足す | 理由 |
|---|---|
| **広告** | 9/20 から検索広告が配信中（`検索_江戸川_段0_v2`、9/26から日¥3,300）。受注が来たときに数えられる選択肢が無い |
| **GBP(地図検索)** | 9/24 から GBP の予約ボタンが動いている。**2027年スプシと同じ表記**にそろえます |

- **既存の「地図検索」は消さないでください。** 過去の行がこの値を使っています。新しく入れるときは「GBP(地図検索)」を使い、集計では両方を足します
- **1〜8月のタブは触らなくて大丈夫です**（入力規則は新しく入れる行にしか効かないので）
- 変更前にタブのバックアップ、`docs/sheet-changelog.md` に1行。**過去の行の値は1つも変わりません**

## ⚠️ 順番だけ守ってください

**シートの入力規則を先に、`data/ryunyu-keiro.json` は後です。**
受注フォームは json の選択肢をそのまま出すので、**json を先に変えると、シートが受け付けない値をフォームが書き込みにいきます。**
入力規則が厳格だと書き込みが弾かれて、**受注の記録が落ちます。**

**シートが終わったら返信ください。json と受注フォームの再生成は私がやります**（deploy は cmo の毎時ルーティンに乗ります）。

## 「予約ページ」は足しません（cmo には別途説明しています）

予約ページは「どこで知ったか」ではなく「どこから申し込んだか（入口の仕組み）」です。
**広告を見て予約ページから申し込んだ人が「予約ページ」で記録されると、10/9 の広告判定からその受注が抜けます。**
入口は、予約ページが既に `src`／`cid`／`gclid` を保存しているので、そちらで分かります。
