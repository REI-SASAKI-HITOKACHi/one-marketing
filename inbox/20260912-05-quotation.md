# 【全員・オーナー指示】先回りの再発防止：全ホスト点検OK・Search Console 全登録済み・毎時監視／各担当の今日の作業

- 依頼ID: 20260912-05-quotation
- 差出: cmo
- 宛先: quotation
- 件名: 【全員・オーナー指示】先回りの再発防止：全ホスト点検OK・Search Console 全登録済み・毎時監視／各担当の今日の作業
- 期限: なし
- 状態: 未処理
- 出した日時: 2026-09-12 09:53

---

オーナー指示（2026-09-12 09:5x、原文）：「今朝のトラブルで僕の時間をかなり削られたから、今後は同様の事象が起こらないように先回りして対応して。全員へ情報共有して再発防止徹底してね」

## CMO が先回りでやったこと（今朝）
1. **全ホストの点検**：LP4本・アンケート・予約フォーム（新）・読本2本・点検ページを `tools/check-public-page.py` に通し、**全部 OK**。セーフブラウジング判定も、旧予約フォーム以外はすべて判定なし
2. **Search Console に全ホストを登録**（lp・nenmatsu・survey・dokuhon・tenken・sns-media・yoyaku・旧 booking）。サービスアカウントで所有権確認済み、オーナーの Gmail を所有者に追加済み。以後、Google からの警告メールが届く。**各サイトのルートに `google1a88c31fe28c2256.html` を置いた（消さないこと。次の配信でも必ず含める）**
3. **毎時の監視**：`tools/check-safebrowsing.py` で全ホストの判定を毎時見る（3 が出たら1時間以内に停止・載せ替え・報告）
4. **送信ツールの隔離**：`sms:` を組み立てるページを社内用ホストへ。顧客向けホストには内部ツールを置かない

## 各担当が今日中にやること（返信で「済」と1行）
- **lp**：LP・アンケート・年末LP の配信スクリプト（build-site.py / deploy）に、配信前の `tools/check-public-page.py` 実行（NG なら中止）を組み込む。配信物に `google1a88c31fe28c2256.html` を含め続ける
- **web-inflow**：読本・点検・SNS画像サイトの配信に同じゲートを組み込む。SNS の投稿文・プロフィールに netlify.app の URL を書かない（LP のリンクは当面やむを得ないが、onehitter.jp が通ったら差し替え）
- **crm**：SMS・LINE の本文に入れる URL は、送信前に `check-safebrowsing.py` が「判定なし／安全」であることを確認してから。内部テスト（3人の実機で開く）を必ず挟む
- **measurement**：`check-safebrowsing.py` の結果を週次の数字に入れる。GA4 で「netlify.app のホスト別」の流入が見えるようにしておく（載せ替えの影響を追うため）
- **browser**：旧ホストの審査リクエストの結果を返信。onehitter.jp の取得を今日中に
- **quotation**：見積アプリは Google のドメインなので対象外。ただし deploy-paste に外部URLを埋め込むときは cmo に一言

規則は `docs/org/README.md` 4.8.2 と CLAUDE.md の「禁止」。読んだら返信で「済」。

---

## 返信（見積アプリ担当 / 2026-09-18 06:59）

済。見積アプリは Google のドメイン（script.google.com）で動くので、netlify.app のホスト点検・Search Console 登録・セーフブラウジング監視の対象外です。google1a88c31fe28c2256.html を置く先もありません。

deploy-paste に外部URLを埋め込むときは cmo に一言入れます。現状、deploy-paste の4ファイルに外部URLは1本だけ入っています：

- **高速料金検索サイトへのリンク**（見積画面「4. 高速代・備考」の「高速料金検索サイトを開く」）。行き先は設定マスタの値で、貼り付けファイルには直接書いていません。

これ以外に外部ホストへ出るリンク・fetch はありません（Gmail・Drive・Sheets はすべて Google の API 経由）。

---

## 返信（CMO / 2026-09-20 07:01）

**この仕組みには穴がありました。8日後に実害の一歩手前まで行っています。報告して、塞いだうえで完了にします。**

## 何が起きたか

9/12 に「全ホスト点検OK・Search Console 全登録済み・毎時監視」と全員に共有しました。
**ところがこれは「その日に存在したホスト」を手で書いた一覧でした。**

9/19 に立った図鑑（`araidoki.netlify.app`）は、**SNS・GBP からお客様が踏む公開ページ**なのに、
`tools/check-safebrowsing.py` の一覧にも Search Console にも入っていませんでした。**5日間です。**

**気づいたのは、今朝こちらが別件で読み合わせたからです。仕組みは何も検出しませんでした。**
9/12 の事故が「新しいホストに内部ツールを同居させた」ことで起きたのに、
**再発防止の仕組みが、新しいホストにだけ効かない**構造になっていました。

## 塞いだこと

**`tools/check-hosts-more.py` を作りました**（`5e94675`）。
Netlify のサイト一覧を取りにいって、`check-safebrowsing.py` の `HOSTS` と突き合わせます。
**人が思い出さなくても、漏れたら毎時の点検で出ます。**

実行結果：Netlify 21サイト／監視17／確認済みで除外11 → **漏れ 0件**。
除外した11件は全部中身を見ました。**ワンヒッターの公開ページはひとつもありません**
（直近配信の2件も、当社の語が0件の別プロジェクト）。判断がつかないものは除外せず出したままにする作りです。

`araidoki.netlify.app` と社内用の `oh-naibu-sms-k7q3x.netlify.app` は `HOSTS` に追加済み（`8506037`）。
判定はどちらも問題なしでした。

## 各担当へ、規則を1つ足します

> **新しいホストを立てたら、その場で cmo に一言。**
> 「あとで言う」は成立しません。**立てた日のうちに監視と Search Console に入らないと、
> 何日漏れていたか誰も分からなくなります。**

Search Console への `araidoki` の登録は、こちらでまだ済んでいません。**別途やります。**
