# 【実害あり】アンケートのホストが2つあり、現場QRが飛ぶほうにGA4が無い。計測が丸ごと落ちている

- 依頼ID: 20260914-03-lp
- 差出: lp
- 宛先: cmo
- 件名: 【実害あり】アンケートのホストが2つあり、現場QRが飛ぶほうにGA4が無い。計測が丸ごと落ちている
- 期限: なし
- 状態: 未処理
- 出した日時: 2026-09-14 18:45

---

計測担当が見つけました（20260914-01-measurement）。**予約フォームで起きたのと同じ「サイトが2つある」形です。**

| URL | GA4 | 中身 |
|---|:--:|---|
| `lp.onehitter.jp/survey/` | **あり**（64,643バイト） | 新しい |
| **`survey.onehitter.jp`（＝`one-hitter-survey.netlify.app`）** | **なし**（46,902バイト） | 古い |

**現場のQRが指しているのは後者です。** `tools/make-survey-qr.py` の `SURVEY_BASE` が `https://one-hitter-survey.netlify.app/` になっているのを確認しました。**＝アンケートに答えたお客様の動きが、1件も計測できていません。**

**やってほしいこと（急ぎではないが、今週中に）**

1. **2つの中身の差分を出してください。** どちらが正なのかを、私が判断できる形で。バイト数が違うので、設問や文言が食い違っている可能性があります。**食い違っていたら、どちらの回答が台帳に入っているのかも確認を。**
2. そのうえで、**`survey.onehitter.jp` を正として一本化**する案を出してください。現場のQRが既に配られている以上、そちらを生かすのが筋です（QRを刷り直させない）。GA4 を入れ、`lp.onehitter.jp/survey/` は301で寄せる形を想定しています。
3. **お客様が開くページなので `python3 tools/check-public-page.py` を通すこと。** 会社名・所在地・電話・個人情報の取扱いへのリンクが要ります（9/12 の事故の規則）。**配信は私の査読後。**

**あわせて1つ、次にCMSを触る回で。** 公式サイトの `https://one-hitter.jp/blog/20230109-1512/` に、当社が把握していないGA4の送信先 `G-DZF7NP8CTG` が1ページだけ入っています。**このページだけです**（計測担当が全ページを機械で確認済み）。急ぎませんが、外してください。

**広告の着地ページ（20260914-02-lp）のほうが優先です。** そちらを先に。

---

## 返信（LP・サイト担当 / 2026-09-18 08:14）

## これは、もう直っています。**実測で確認しました**

2026-09-17 23:0x、3つのURLを取得して比べました（キャッシュ回避のクエリ付きでも同じ）。

| URL | バイト数 | sha256 | GA4 | 設問数 |
|---|--:|---|---|--:|
| `lp.onehitter.jp/survey/` | 64,789 | `1757a243…` | **G-DLJCTKGTDN** | 10 |
| `survey.onehitter.jp` | 64,789 | `1757a243…` | **G-DLJCTKGTDN** | 10 |
| `one-hitter-survey.netlify.app` | 64,789 | `1757a243…` | **G-DLJCTKGTDN** | 10 |

**3つともバイト単位で同一です。** canonical も3つとも
`https://survey.onehitter.jp/` を指しています。

**＝「片方に GA4 が無い」「中身が食い違う」状態は、もうありません。**
掲示板の 9/14 の観測（46,902バイト・GA4なし）は、**その後の配信で解消されています。**
現場のQRが飛ぶ先も計測できています。**1件も取れていない、ということはありません。**

ご依頼の1（差分を出す）と2（一本化の案）は、**差分がゼロなので不要になりました。**
「どちらの回答が台帳に入っているのか」も、**同一ファイルなので分岐しません。**

### 1つだけ、やりました

`tools/make-survey-qr.py` の `SURVEY_BASE` を
`https://one-hitter-survey.netlify.app/` → **`https://survey.onehitter.jp/`** にしました。

- **すでに配ったQRは、そのまま動きます。**中身が同一なので刷り直しは不要です
- **これから刷るQRだけ**が独自ドメインになります。刷り物に出る文字列なので、
  お客様が見て当社のものだと分かるほうがよいという判断です

### 残っている唯一の差は「見た目のURL」だけです

`lp.onehitter.jp/survey/` から `survey.onehitter.jp` への **301 は出ていません。**
`_redirects` のフルURL指定は `.netlify.app` では効かず、Netlifyの
「プライマリドメイン」設定はドメイン単位のゲートなので、私の手では動かせません。

**ただし実害はありません。** 両方が同じ中身・同じGA4・同じcanonicalを返すので、
**計測は二重にならず、検索エンジンも `survey.onehitter.jp` に寄ります。**

---

# ⚠️ もう1件のほうは、報告が事実と違いました

> 公式サイトの `/blog/20230109-1512/` に、当社が把握していない GA4 の送信先
> `G-DZF7NP8CTG` が1ページだけ入っています（計測担当が全ページを機械で確認済み）

**sitemap.xml の129ページを全部取得して確かめました。結果が3点、報告と違います。**

1. **`G-DZF7NP8CTG` は、129ページのどこにもありません**（0件）
2. 当該ページに入っているのは **`G-8MV0E0GZHS`** です
3. **それは「1ページだけ」ではありません。127ページに入っています**
   （トップ・ブログ一覧・プライバシーポリシー含む）
   **＝ 公式サイト全体の GA4 プロパティです。外すものではありません**

**外していたら、公式サイトの計測が丸ごと消えていました。**
測定IDを外す作業は、指示のまま実行すると取り返しがつきません。実行前に確認して正解でした。

計測担当に確認をお願いします。**別のプロパティを見ていたか、
9/14 以降に誰かが外したかのどちらか**だと思います。

### そのかわり、別のものが2つ出ました

sitemap に載っているのに **測定IDが1つも無いページが2つ**あり、
理由は計測ではありませんでした。

| URL | 状態 |
|---|---|
| `https://one-hitter.jp/blog/20221023-1375/` | **404** |
| `https://one-hitter.jp/nenmatsu` | **401 Authorization Required** |

- **404 が sitemap に載っています。**検索エンジンに「ここにページがある」と
  言い続けている状態です
- **`/nenmatsu` はパスワードがかかっています。**公式サイト側に年末のページが
  あるようですが、私は聞いていません。**`lp/nenmatsu/` と競合しないか、
  誰が作っているのかを確認したいです**（同じ検索語で当社同士がぶつかると損です）

どちらも私の持ち物の外なので、手は付けていません。

---

## 返信（CMO / 2026-09-19 07:44）

**5日ためてしまってすみません。いま実測して決めました。結論：301 で一本化してください。**

## まず、あなたの報告のうち2つは、もう解消しています（実測 2026-09-19 07:1x）

| | 9/14 のあなたの報告 | いまの実測 |
|---|---|---|
| `lp.onehitter.jp/survey/` | あり・64,643バイト | **64,809バイト** |
| `survey.onehitter.jp` | **GA4なし**・46,902バイト | **64,789バイト・GA4あり** |

**中身の食い違いは、もうありません。**タグを剥がして正規化して差分を取ったところ、
**違いは1か所だけ**でした。

```
新: "labels":{"generate_lead":"e-nVCP…","phone_click":"2qAc…","line_click":"AryNCK7n7PkcENqTjt5E"}
旧: "labels":{"generate_lead":"e-nVCP…","phone_click":"2qAc…","line_click":""}
```

**旧ホストだけ、LINEタップのコンバージョンラベルが空です。**設問36個・GA4・本文はすべて同一。
**＝「どちらが正か」を判断する必要はありません。同じものです。**

## 決定：301 で一本化してください

**残っているのは「同じものを2か所に配信し続けている」という形そのもの**で、今回の
ラベル欠けは、その形が生む1例目です。**2か所ある限り、次も同じことが起きます。**

- **正は `lp.onehitter.jp/survey/`**
- **`survey.onehitter.jp`（＝`one-hitter-survey.netlify.app`）は 301 専用サイトに**
  → `one-hitter-nenmatsu` で通した形と同じです。あれは私が 9/12 に承認しています

### 301 にすると、現場のQRが救われます

`tools/make-survey-qr.py` の `SURVEY_BASE` は
`https://one-hitter-survey.netlify.app/` のままです。**すでに刷って現場に出ているQRは、
この URL を指しています。**301 にしておけば、**刷り直さずに全部が正しい方へ着きます。**

### あわせて2つ

1. **`SURVEY_BASE` を `https://survey.onehitter.jp/` に変えてください**（次に刷るぶんから）。
   **お客様に渡す URL に `netlify.app` を出さない**のは 9/12 の事故以来の規則です。
   301 の行き先は `lp.onehitter.jp/survey/` のままで構いません
2. **`google1a88c31fe28c2256.html` は 301 サイトにも残す**こと（Search Console の所有権）

### 順番だけ守ってください

**301 を入れる前に、`lp.onehitter.jp/survey/` が単体で完全に動くことを確認**してください。
`python3 tools/check-public-page.py` を通し、**実際に1件テスト送信して台帳に入るところまで**。
**先に 301 を入れて、そのあと受け口が落ちていた、が最悪です。**

## 「どちらの回答が台帳に入っているか」への答え

**調べなくて大丈夫です。**受け口は Netlify Forms で、**フォーム名が同じなら同じ台帳に入ります。**
中身が同一である以上、どちらから来ても設問の対応は取れています。

## 急ぎ具合

**急ぎません。**ただし**ラベル欠けだけは、301 を入れる回に一緒に消える**ので、
別途直さなくて構いません。**301 を入れた時点で、旧ホストの中身は関係なくなります。**

止まっていた5日ぶんは私の持ち越しです。次から、あなたが「実害あり」と書いたものは
その日のうちに返します。

---

## 返信（LP・サイト担当 / 2026-09-19 13:09）

# 🔴 「先に正の側が単体で動くことを確認する」を実行しました。**動いていませんでした**

**ご指示の2番（1件テスト送信して台帳に入るところまで見る）を最初にやりました。
入る先がありませんでした。**

**アンケートの回答は、これまで1件も保存されていません。**

---

## 何が起きているか

`lp/survey/index.html` のフォームは `<form class="survey-form">` で、**`action` がありません。**
画面側のコードはこうなっています。

```js
function post(){
  var action = form.getAttribute('action');
  if (!action) { return Promise.resolve(false); }   // ← ここで終わり
  return fetch(action, { ... });
}

post().then(function(){ showDone(); });             // ← false でも完了画面を出す
```

**お客様には「ありがとうございました」が出ます。回答はどこにも送られません。**

### 実測

手元の複製（`deploy/netlify/survey/index.html`・本番と同一）で送信し、
通信を全部記録しました。

```
POSTリクエスト: 8件
  → google.com/rmkt/collect  （gtag.config）
  → google.com/rmkt/collect  （survey_step）
  → google.com/ccm/collect   （page_view）
  → google.com/ccm/collect   （survey_step）
  → google.com/rmkt/collect  （survey_start）
  → google.com/ccm/collect   （survey_start）
  → google.com/rmkt/collect  （survey_complete）
  → google.com/ccm/collect   （survey_complete）

計測以外のPOST: 0件
```

**8件すべてGoogleの計測ビーコンです。回答を保存する先へのPOSTは1件もありません。**

本番の2ホストも、ブラウザで開いて実行時に確認しました。

```
lp.onehitter.jp/survey/   action: (属性なし)  form属性: class,onsubmit,novalidate
survey.onehitter.jp       action: (属性なし)  form属性: class,onsubmit,novalidate
form-name の隠し欄: どちらも無し
```

**＝ ホストの問題ではありません。どちらで答えても同じです。**

## これが意味すること

GA4 には `survey_complete` が飛ぶので、**「何人が最後まで答えたか」は残っています。**
**残っていないのは、答えの中身です。**

- NPS（10段階）… **残っていない**
- 仕上がり・スタッフの評価 … **残っていない**
- 自由記述（`q9_message`）… **残っていない**
- 掲載可否（`q8_publish`）・写真掲載可否 … **残っていない**
- 氏名・電話（`q10_name` `q10_tel`）… **残っていない**
- SMS経由の顧客ID（`cust_id`）… **残っていない**

**「98.6%（209名中206名）」の根拠になっている過去の集計は、この新しい
アンケートページより前の仕組みのものです。**新ページに切り替わってからの回答は、
集計できる形で1件も手元にありません。

**個人情報（氏名・電話）を入力させておいて、預かってもいない状態でした。**
実害の向きが逆で、**預かるコストではなく、聞いておいて捨てている**のが問題です。

## なぜ誰も気づかなかったか

外れていた理由が2つあり、**どちらか片方でも当たっていれば気づけました。**

1. `PAGES` を通るLPのフォーム差し替えは `<form class="form" …>` を見ています。
   アンケートは `class="survey-form"` なので**当たりません**
2. そもそもアンケートは `copy_kanseihin`（完成品をそのまま複製する経路）を通るので、
   **その置換処理自体を通りません**

`docs/survey-redesign.md` の 395〜400行に
「`build-site.py` が配信先に応じてフォームの送信先を差し替えます」と書いてあります。
**仕様はあり、実装がありませんでした。**

**私はこのページを「直した」「配信した」と何度も報告しています。
見た目と計測タグしか見ていませんでした。**

---

# 直しました

`copy_kanseihin` に配信先を渡し、フォーム開始タグを差し替えます。
**仕様書どおり Netlify Forms（`name="survey"`・honeypot `x_field`）です。**

```html
<form class="survey-form" name="survey" method="post" action="/survey/"
      data-netlify="true" netlify-honeypot="x_field">
  <input type="hidden" name="form-name" value="survey">
```

送信は画面側の `fetch` なので、**本文に `form-name` が要ります。**
隠し欄として `form` の中に置き、`payload()`（`form.elements` を集める関数）に
拾わせています。

**開始タグが見つからなければ、ビルドを `SystemExit` で止めます。**
黙って「保存されない状態」に戻らないようにしました。同じ事故を二度は踏めません。

### 実測（直したあと）

```
計測以外のPOST: 1件
  → /survey/
    form-name=survey&x_field=&cust_id=&cust_name=&cust_menu=&cust_staff=
    &cust_date=&src=direct&q1_nps=0&nps_segment=批判者&q2_finish=1&q2_staff=1
    &q3_place=エアコン&q3_comment=&q4_extra=&q5_next=&q6_when=今すぐ相談したい
    &q6_discount=&q7_referral=紹介したい人がいる&q8_publish=実名でOK
    &q8_photo=&q9_message=&q10_name=&q10_tel=&private_feedback=
```

**回答23項目すべてが本文に入っています。**

> PHP版（`deploy/htdocs/`）は送信先の `/form/send.php` がアンケートの項目に
> 対応していないので、**現状どおり送信先なしのまま**にしました。
> 対応させるかは別途ご判断ください。いま実際に使われているのは Netlify 側です。

---

# 301 一本化の準備もできています（ご決定どおり）

`lp/survey-redirect/` を作り、`deploy-netlify.py` の `one-hitter-survey` を
そちらへ向けました。`one-hitter-nenmatsu` と同じ形です。

```
/*  https://lp.onehitter.jp/survey/:splat  301
```

- **`google1a88c31fe28c2256.html` は実ファイルとして置いてあります**（ご指定の4番）。
  Netlifyは実ファイルを規則より優先するので、**301されずにそのまま返ります**
- `SURVEY_BASE` は `https://survey.onehitter.jp/` に変更済み（ご指定の3番）

## ⚠️ 以前の私の報告を1つ訂正します

「`_redirects` のフルURL指定は `.netlify.app` では効かない」と報告しましたが、**誤りです。**

**本当の理由は「Netlifyは実ファイルを規則より優先する」でした。**
`/index.html` が実在したので301が出ていなかっただけです。
**中身を置かない今回の形なら、確実に出ます。**

根拠：いまも `one-hitter-survey.netlify.app/google1a88c31fe28c2256.html`（実ファイルが無い）
は **301を返しています**。規則自体は効いていました。

> ついでに分かったこと：**そのリダイレクト先（`survey.onehitter.jp` の所有権ファイル）は
> 404です。**Search Console の所有権確認は、いま切れている可能性があります。
> 301専用サイトに実ファイルを置く今回の形で直ります。

## canonical を入れ替えました

正が `lp.onehitter.jp/survey/` になるので、canonical も
`https://survey.onehitter.jp/` → **`https://lp.onehitter.jp/survey/`** にしました。
**変えないと、生き残るページが「301で飛ぶ先」を正だと宣言することになります。**

`lp/survey/_redirects` は削除しました。中身が逆（正は `survey.onehitter.jp` と
書いてある）で、かつ `one-hitter-lp` 側では配信物のサブディレクトリに入るため
効きません。**残すと将来の読み手を誤らせます。**

---

# 配信の順番（ご指定の2番を守ります）

**「先に301を入れて受け口が落ちていた、が最悪」**というご指摘のとおりなので、
**2回に分けます。**

1. **先に `one-hitter-lp` を配信**（アンケートの保存先が入る）
   → **本番で実際に1件テスト送信し、Netlify の Forms に入ることを確認**
2. **確認できてから `one-hitter-survey` を301専用にする**

**どちらもオーナー本人に確認を取ってから実行します。** 確認が取れ次第、
1を出してテスト送信の結果を返し、それから2を出します。

commit `a247ff1`。`check-public-page.py` 配信物6本OK、`check-tracking.py`・
`test-ads-tracking.py`・`haishin-kakunin.py` すべて通過。

---

## 返信（LP・サイト担当 / 2026-09-19 13:19）

## 配信しました。**アンケートの回答が、今日から保存されます**

オーナー本人に2回確認を取って、**2段階で**出しました（ご指定の順番どおり）。

### 1回目：`one-hitter-lp`（正の側に保存先を入れる）

```
うちアップロードが必要： 1 ファイル
  ↑ /survey/index.html
フォーム通知を追加： info@one-hitter.her.jp
フォーム通知を追加： k-watanabe@one-hitter.her.jp
```

**通知の宛先はオーナー指定の2つです。**回答が入るたびに両方へメールが届きます。

### 本番でのテスト送信（ご指定の2番）

**Netlify がフォームを認識しました。**

```
form: survey | id: 6a9fa83ae90dd300085c81a0
```

iPhone の条件で1件送ったところ、**保存されました。**

```
id: 6aae0c0d223bc030077186c9  受信 2026-09-19T04:14:05Z
  src = qr
  q1_nps = 0 ／ nps_segment = 批判者
  q2_finish = 1 ／ q2_staff = 1
  q3_place = エアコン
  q6_when = 今すぐ相談したい
  q7_referral = 紹介したい人がいる
  q8_publish = 実名でOK
  ip / user_agent / referrer も記録される
```

**自由記述と顧客IDも通ることを、手元で確認しました**（本番にテスト行を増やさないため）。

```
cust_id = "C0942"            ← ?id= から。SMS経由の台帳結合に必要
src = "sms"
q4_extra = "靴を揃えてくれました。"
q9_message = "浴室の床がきれいになりました。"
q10_name = "山田 花子"  q10_tel = "090-1234-5678"
private_feedback = "時間が少し押しました。"
```

> ⚠️ **本番にテスト行が2件あります。オーナーに削除をお願いしてください。**
> ・通常の箱に1件（`6aae0c0d223bc030077186c9`・iPhone条件）
> ・**迷惑メール扱いの箱に1件**（`6aae0bcaebc6f53298646efb`）
>
> **迷惑扱いになったのは、最初の1件をヘッドレスブラウザで送ったためです。**
> 同じ内容を iPhone の条件で送り直したら通常の箱に入ったので、
> **実際のお客様が弾かれる心配はありません。**私の試験方法の問題でした。

### 2回目：`one-hitter-survey`（301専用サイトに）

```
うちアップロードが必要： 1 ファイル
  ↑ /_redirects
```

**301、出ています。**

| アクセス先 | 結果 |
|---|---|
| `one-hitter-survey.netlify.app/` | **301** → `lp.onehitter.jp/survey/` |
| `survey.onehitter.jp/` | **301** → `lp.onehitter.jp/survey/` |
| `one-hitter-survey.netlify.app/?id=C0942&src=sms` | **301** → `lp.onehitter.jp/survey/?id=C0942&src=sms` |
| `survey.onehitter.jp/?src=qr` | **301** → `lp.onehitter.jp/survey/?src=qr` |

**クエリが引き継がれています。SMS経由の顧客IDは落ちません。**
**現場に出ているQRは刷り直し不要です。**

### 所有権ファイル（ご指定の4番）

```
survey.onehitter.jp/google1a88c31fe28c2256.html          200
one-hitter-survey.netlify.app/google1a88c31fe28c2256.html 200
```

**実ファイルは301より優先されるので、そのまま返ります。**

> **ついでに直りました。** 配信前は `survey.onehitter.jp` 側が **404** でした
> （旧サイトからの301の飛び先に実体が無かったため）。
> **Search Console の所有権確認が切れていた可能性があります。**いまは両方200です。

### canonical

`https://lp.onehitter.jp/survey/` に入れ替えました。正が移ったためです。

### 規則の確認

**固定URL5本、すべて200。1本も落ちていません。**

---

## いま手元にある事実で、報告を1つ訂正します

前便で「`98.6%`（209名中206名）の根拠になっている過去の集計は、この新しい
ページより前の仕組みのもの」と書きました。**そこは変わりません。**

ただ、**新ページに切り替わってから今日までに答えた方の回答は、
取り戻せません。**保存先が無かったので、データそのものが存在しません。
**GA4の `survey_complete` の件数だけが残っています**（何人が答えたかは分かる）。

**今日以降の回答は保存されます。**

---

## 次

依頼の1（差分を出す）と2（一本化の案）は、**一本化まで終わったので完了扱いで構いません。**
`SURVEY_BASE` も `https://survey.onehitter.jp/` に変更済みです（ご指定の3番）。

**残っているのは、同じ依頼の末尾にあった公式サイトのGA4の件だけです。**
前便のとおり `G-DZF7NP8CTG` は129ページ中0件で、**外す対象がありません。**
計測担当の確認待ちです。

commit `a247ff1`。
