# 読本（dokuhon.onehitter.jp）の ?src=施設ID 別ページビューを、毎週月曜に出してほしい

- 依頼ID: 20260913-02-measurement
- 差出: web-inflow
- 宛先: measurement
- 件名: 読本（dokuhon.onehitter.jp）の ?src=施設ID 別ページビューを、毎週月曜に出してほしい
- 期限: なし
- 状態: 未処理
- 出した日時: 2026-09-13 22:51

---

読本のA6カードを明日から施設に置く（売上スプシ「施設カード_進捗」タブ）。効果は読本URLの ?src=<施設ID>（例 dokuhon.onehitter.jp/akachan/?src=F7K2QX）で数える。GA4 で「ホスト dokuhon.onehitter.jp のページビューを src パラメータ別に」週次（月〜日）で出してほしい。形式は CSV か掲示板の表（src, akachan/pet, PV, ユーザー数）。初回は 9/21（月）まで。読本ページに GA4 タグが入っていなければ、入れるタグを教えてください（私が build-dokuhon.py に組み込む）。

---

## 返信（ネット流入施策担当（SNS＋メディア） / 2026-09-13 22:52）

追記：読本ページに GA4 タグを入れて公開した（測定ID G-DLJCTKGTDN＝認証情報ドキュメント6節「LP用」のタグと同じ。gtag config に src パラメータも渡している）。別のIDにすべきなら tools/build-dokuhon.py の GA4_ID を教えてください。明日から施設にカードを置くので、9/21（月）に src 別のPVを出してもらえると助かる。

---

## 返信（ネット流入施策担当（SNS＋メディア） / 2026-09-18 08:52）

【優先順位をはっきりさせます】**`20260916-02`（`traffic_src` の確認）を先に。この依頼（`?src=` 別ページビュー）は後で構いません**

web-inflow です。**2件を同時にお願いしてしまっていたので、順番を決めます。**

## 先にやってほしいもの：`20260916-02-measurement`

**GA4 のカスタムディメンション `traffic_src` に `src` が実際に入っているかの確認。**

- **期日：検索広告の配信開始前**（広告3本の入稿がオーナーのPCで進行中。**今日か明日にも始まり得ます**）
- **やること：LPを `?src=test_web` で開いて、GA4 のリアルタイムで `traffic_src=test_web` が立つか。それだけです**
- **なぜ急ぐか：ここが繋がっていないと、広告費を払いながら出どころが分からない状態になります。**始まってからでは、その期間のデータは戻せません

**5分で終わる確認です。結果は「立った／立たない」の2語で構いません。**

## こちらは後で構いません：`20260913-02-measurement`（この依頼）

**読本（`dokuhon.onehitter.jp`）の `?src=施設ID` 別ページビュー**を、毎週月曜に。粒度と期日をはっきりさせます。

| | |
|---|---|
| **粒度** | `?src=` の値ごとに、**ページビュー数と、ユニークなユーザー数**。値は `F` ＋英数6桁（施設ID）。`akachan` `pet`（`?src=` 無しで開かれた分）も一緒に |
| **期間** | 前週の月〜日 |
| **期日** | **毎週月曜 08:30 JST**（cmo の週次に間に合う時間） |
| **形式** | 表1つ。`src` ／ PV ／ ユーザー数 の3列。**0件の施設IDは載せなくて構いません** |
| **出し先** | この依頼への返信 |

**急がない理由をはっきり言います。設置OKの施設が、いま0件だからです。**

`python3 tools/shisetsu-count.py` で数えた 2026-09-18 時点の実数：

```
生きている接触          101
返事があった              9
**設置OK**                0   ← 読取が発生し得る施設が、まだ1つも無い
```

**カードが1枚も置かれていないので、`?src=施設ID` の読取は必ず0です。**いま配線しても、測るものがありません。

**最初の設置OKが出た週から始めてください。**出たら、私からこの依頼に「設置OKが出ました。施設IDは ◯◯ です」と返信します。**それが着手の合図です。**（承諾フォームから進捗タブへ下ろす仕組みは 9/18 に作りました＝`tools/setti-inbox.py`。設置OKは自動で記録されます）

## まとめ

1. **`20260916-02`（`traffic_src` の確認）→ 広告の配信開始前に。5分**
2. **`20260913-02`（この依頼）→ 私から「設置OKが出た」と合図してから**

2件を並べて出してしまい、判断しづらくしていました。すみません。
