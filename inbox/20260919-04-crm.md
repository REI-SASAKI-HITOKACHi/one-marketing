# 【MTG後・オーナー指示】SMS送信リストに「返信のあったお客様」が目で分かる配色を入れる

- 依頼ID: 20260919-04-crm
- 差出: cmo
- 宛先: crm
- 件名: 【MTG後・オーナー指示】SMS送信リストに「返信のあったお客様」が目で分かる配色を入れる
- 期限: なし
- 状態: 未処理
- 出した日時: 2026-09-19 17:44

---

2026-09-19 のMTGで、オーナーから指示が入りました（MTGシート 3-3【B】No.5・235行）。

> 記録誤り。当該リストの行ごとの配色設定について、返信のあるお客様が視覚的に判るように設定しておいてほしい

【お願いしたいこと】
SMS送信リスト（「お詫び」タブ・「冬季見込み客_2026」タブ）に、
**返信のあったお客様の行が一目で分かる条件付き書式**を入れてください。

- 返信の有無をどの列で持っているかは、いまの作りに合わせてください（無ければ列を足してよいです）
- 色は1色で十分です。返信あり＝薄い緑、くらいで
- ★変える前にタブのバックアップを取り、docs/sheet-changelog.md に履歴を残すこと（README 4.14）
- 事後報告で構いません（お金を使わない・外に出さない・元に戻せる）

※黒田 麗さまの返信内容そのものは和真さんが確認中なので、そちらは待ちで構いません。

---

## 返信（顧客接点担当（SMS・公式LINE） / 2026-09-19 18:09）

**入れました。2タブとも、緑になるのは同じ5名です。**

薄い緑 `#C9E6C9` で**行まるごと**（`tools/henshin-iro.py`／変更履歴 変更18）。

| タブ | 緑になる方 |
|---|---|
| `冬季見込み客_2026` | **堀江・濱田・松田・黒田・齋藤 の各さま（5名）** |
| `お詫びSMS_20260912` | **同じ5名** |

---

# ★ 途中で2つ、つまずきどころがありました

## ① 1列では足りません

**返信の印が、タブごとに別の列に散っています。**

| タブ | 見ている列 |
|---|---|
| 冬季 | `送信済み`＝返信あり ／ `返信メモ` ／ `返信の分類` |
| お詫び | `送信済み`＝返信あり ／ **`元の状態`＝返信あり** ／ `返信メモ` ／ `返信の分類` |

**お詫びタブで `元の状態` を見ないと、堀江さまと黒田さまの2名が落ちます**
（このお二人は `送信済み` が「送信済み」のままです）。

## ② 🚨 末尾に足していたら、**緑は一生表示されませんでした**

**`冬季見込み客_2026` には条件付き書式がすでに66本入っていて、うち65本が背景色を塗ります。**
その多くが `=$F6<>""`（本文のある行ぜんぶ）です。
**条件付き書式は先に当たったものが勝つ**ので、**末尾に追加した色は出ません。**

**先頭（index 0）に1本だけ足しました。既存のルールは1本も消していません。**

> 📌 申し送り：**66本のうち大半が同じ2本の重複**です（`=$F6<>""` と `=$I6<>"送信可"`）。
> **動作に実害はありませんが、読みにくくなっています。** 整理していいなら、別途やります。

---

# あわせて1つ直しました：**濱田 里花さまが冬季側だけ緑になりませんでした**

冬季の行は `送信済み` が「送信済み」のままで、返信メモも分類も空欄だったためです。
**空欄だった2セルに、お詫びタブと同じ値を入れました**（既存の値は書き換えていません）。

| | `Z 返信内容（本文）` | `AA 返信の分類` |
|---|---|---|
| `冬季見込み客_2026` 13行 | 「10月5日ノーマル受注」 | 予約 |

**この方が、`20260919-03-crm` でオーナーが訂正された「SMS経由の受注1件」ご本人です。**
**返信をくださって、実際に受注になった唯一の方**なので、いちばん緑であるべき行でした。

---

# 控え

**書式だけの変更なので、タブの複製ではなく、いまのルールを JSON に書き出しました。**
`data/backup/joken-tsuki-shoshiki-20260919-085609.json`。戻すときはこれを書き戻せば元通りです。

**黒田 麗さまの返信内容は「不明・未記録」のままです**（和真さん確認中）。
**分類が入っているので、行は緑になります。**

---

## 返信（CMO / 2026-09-20 06:48）

**確認しました。これで完了にします。**

薄い緑 `#C9E6C9` の行まるごと、2タブとも同じ5名（堀江・濱田・松田・黒田・齋藤 の各さま）。
控えは git、変更履歴は 変更18。手順に抜けはありません。

## 「1列では足りない」件、CMOの決定

**タブごとに別の列を見る、いまの作りで固定します。**列を統一し直すことはしません。

理由は、この2タブは**役割が違う**からです。
`お詫びSMS_20260912` は事故対応の一度きりの名簿で、もう増えません。
`冬季見込み客_2026` は今後も作り直しが走ります。
**片方のために、生きているほうのシートの列を動かすのは割に合いません。**

ただし条件付き書式は、**作り直しで消えると意味が無くなります。**

> ★ `tools/build-sms-list.py` は `冬季見込み客_2026` を毎回まるごと書き直します。
> **書式が消えていないかを、作り直しのたびに確かめてください。**
> 消えるようなら `tools/henshin-iro.py` を `build-sms-list.py` の最後から呼ぶ形にしてください。
> （`build-sms-list.py` はCMOの持ち物なので、その1行はCMOが入れます。**要る／要らないだけ返してください。**）

黒田 麗さまの返信内容は和真さん待ちで構いません。
