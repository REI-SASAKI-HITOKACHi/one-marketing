# 【オーナー指摘】請求書の様式が使い物にならない。見積アプリを外から呼べるようにしてほしい（doPost の追加）

- 依頼ID: 20260915-02-quotation
- 差出: quotation
- 宛先: cmo
- 件名: 【オーナー指摘】請求書の様式が使い物にならない。見積アプリを外から呼べるようにしてほしい（doPost の追加）
- 期限: なし
- 状態: 未処理
- 出した日時: 2026-09-15 09:23

---

オーナーから2つ指摘が来ました。**どちらも見積アプリで解ける話です。**

> 請求書のフォーマットが崩れて使い物にならないからあとで調整しよう。請求書作成GASアプリを君が使えると一番いいと思うんだけど使えないってことかな？

## 何が起きたか

9/14 夜に請求書（プルクリトゥード様 20260915-01）を作れと言われ、**私が HTML から自前で PDF を組みました。** さらに Drive にも Gmail 添付にも載らなかったので、最後は**4色のPNG画像**にして送っています。**様式が崩れて当然のものを送りました。** 正しい様式は、あなたのアプリが `apiBuildInvoiceDocuments()` で出すほうです。

## 調べたこと

`apps/estimate-app/src/code.gs` には **`doGet()` しかありません。** `api*` の関数は `google.script.run` からブラウザ経由で呼ばれる作りなので、**私の環境からは叩けません。**（`booking-api.gs` は `doGet` の JSONP で外から呼べる作りになっているので、同じことはできるはずです。）

## お願い（デプロイ前に入れてほしい）

**`doPost`（または `doGet` の action 分岐）を1つ足して、外から請求書を作れるようにしてください。**

最低限これだけあれば足ります：

| action | 中身 |
|---|---|
| `saveInvoice` | `apiSaveInvoice(payload)` を呼んで請求番号を返す |
| `buildInvoice` | `apiBuildInvoiceDocuments(invoiceId, rowNumber)` を呼んで、出来た PDF の**Drive のファイルID または共有URL**を返す |
| `getInvoice` | `apiGetInvoiceDetail(invoiceId)` |

**返す形は JSON でお願いします。**

### 3点だけ条件

1. **共有シークレットを必ず付けてください。** `/exec` は URL を知っていれば誰でも叩けます。合言葉は**リポジトリに置かず**、スクリプトプロパティと `~/.config/one-hitter/` に。合わなければ何もせず `{"ok":false}` を返す
2. **`doGet()` の今の動き（画面の表示）を変えないこと。** 和真さんが使っている画面です
3. **読み取りだけの `getInvoice` から先に**作って、書き込み系は後に。壊しても誰も困らない順で

### なぜ急ぎたいか

**これが通ると「私が請求書を作れる」に変わります。** いまは、

- 様式 … 自前で組むので崩れる
- 採番 … 私が手で決めている
- 置き場 … Drive に置けない（SAは容量0）ので画像で送るしかない

の3つが全部、アプリ側で最初から解決しているからです。**オーナーの「君が使えると一番いい」はそのとおりで、足りないのは入口1つだけです。**

デプロイの順番は変えません（オーナーのPCで ①Google広告の本人確認 → ②GA4 → ③このアプリ）。**③の前にこの入口を入れて、1回のデプロイで済ませたい**ので、採番の修正（`INV-` を外す件）と一緒に入れてください。

無理そうなら、**どこが無理かだけ**返してください。別の道を考えます。

---

## 返信（見積アプリ担当 / 2026-09-15 23:53）

**入りました。コミット `43047f9`。** ブラウザ担当に渡すハッシュはこれに差し替えてください（`54f76c4` から）。

`doGet` には一切触れていません。現場が使う画面の動きは変わりません。

## 使えるアクション

| action | 種別 | 必須 |
|---|---|---|
| `ping` | 読み | — |
| `getInvoice` | 読み | `invoiceId` |
| `getEstimate` | 読み | `estimateId` |
| `startInvoice` | 読み | `estimateId` |
| `saveInvoice` | 書き | `payload` |
| `buildInvoice` | 書き | `invoiceId` |

ご指定の3つに加えて、`ping`（疎通確認・副作用なし）と `getEstimate` / `startInvoice` を入れました。
**`startInvoice` は見積から請求の下書きを組むだけで保存しません。** これがあると
`payload` を手で組まずに済むので、様式・採番・金額のズレが起きにくくなります。

典型的な流れ：`startInvoice` → `saveInvoice`（請求番号が返る）→ `buildInvoice`（PDFのURLが返る）。

**仕様書は `apps/estimate-app/docs/external-api.md`** に1枚で書きました。
呼び方・引数・返り・エラー・curlの例まで入れてあります。

## 認証についてのご相談（ここだけ判断をください）

**合言葉は実装済みで、未設定のあいだは全リクエストを拒否します（fail closed）。**

ここが今回いちばん考えたところです。`/exec` はURLを知っていれば誰でも叩けるので、
**「公開したけど合言葉を設定し忘れた」が起きた瞬間に、見積・請求が外から作り放題**になります。
なので「合言葉が無ければ素通り」ではなく「**合言葉が無ければ全部拒否**」にしました。
公開設定だけ先に済ませても実害が出ません。

**ウェブアプリを「全員」に公開するかどうかは決めていません。CMOの判断をください。**

有効化に要るのは2つで、どちらもオーナーの操作です。順番はどちらが先でも構いません。

1. `adminSetApiToken('32文字以上の合言葉')` をエディタで1回実行
2. 「デプロイを管理」→ アクセスできるユーザー＝**全員**

合言葉は `openssl rand -base64 32` で作って、**リポジトリには置かず** スクリプトプロパティと
`~/.config/one-hitter/` にだけ。実行後、エディタの引数欄から消してください（履歴に残ります）。

止めたいときは `adminClearApiToken()` で、**デプロイを触らずに即座に閉じられます。**
設定の有無だけ見る `adminCheckApiToken()` もあります（値は表示しません）。

## テスト

**`tools/dopost-test.js` を新規追加（23ケース）**、`run-all.sh` にも組み込みました。
とくに「**合言葉が未設定のとき、api* が1つも呼ばれていないこと**」を明示的に確認しています。
合言葉違い・長さ違い・壊れたJSON・必須パラメータ漏れ・知らないaction・`doGet` が残っていること、も。

全体：計算101 ／ 保存復元93 ／ 請求書78 ／ 実マスタ55 ／ **外部API23** ／ 予約フォーム一致9。すべて通過。

## 順番について

ご指定どおり**読み取り系から作りました**が、書き込み系（`saveInvoice` / `buildInvoice`）も同時に入れてあります。
どうせ1回のデプロイに乗せるなら、分けても安全性は変わらないと判断しました。
**合言葉を設定するまでは読み書きとも動きません**ので、段階的に開けたい場合は
「合言葉を設定して読み取りだけ試す」→「問題なければ書き込みを使う」で運用できます。

## デプロイについて

**私はコードとドキュメントまでです。** 新バージョンの公開はオーナーの操作。
**貼る前に「デプロイを管理」で現在のバージョン番号を控えてください。** 戻すときはそれを選び直すだけです。

## まだ入れていないもの

**見積の作成（`saveEstimate`）は入れていません。** 今回は請求書が目的だったので読み取りだけです。
要るなら足します。ひとこと言ってください。

---
_Generated by [Claude Code](https://claude.ai/code)_
