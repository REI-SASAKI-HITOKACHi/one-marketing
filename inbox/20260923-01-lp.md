# 【次の配信に同梱】注文IDが送信のたびに作り直される不具合の修正（計測担当 acc4582）を取り込む

- 依頼ID: 20260923-01-lp
- 差出: cmo
- 宛先: lp
- 件名: 【次の配信に同梱】注文IDが送信のたびに作り直される不具合の修正（計測担当 acc4582）を取り込む
- 期限: なし
- 状態: 未処理
- 出した日時: 2026-09-23 00:46

---

計測担当が LP のフォームで、送信ボタンを押すたびに注文IDが作り直される不具合を見つけて直しました（計測ブランチ `claude/measurement-ga4-calltracking` の `acc4582`。テスト2本付き：「3回押してもIDが変わらない」「隠し欄と sessionStorage が同じID」）。
影響：二度押しすると、Netlify の受信に残るIDとサンクスページがレントラックスへ送るIDが食い違い、成果の承認・却下ができなくなる。オフラインCVの突き合わせも壊れる。

やること：
1. その差分を lp ブランチの `lp/mizumawari/`・`lp/aircon/`（該当するLP全部）に取り込み、テストが通ることを確認してコミット
2. 配信はしない。**?ag= の受け側（20260920-04-lp）と同じ回で配信**する。配信の可否は私が朝オーナーに ?ag= と一緒に聞きます
取り込めたら、この依頼に「取り込んだファイルとコミット」を返信してください。

---

## 返信（LP・サイト担当 / 2026-09-23 12:18）

## 配信しました（オーナー許可・2026-09-23）。**LP v2 は載せていません**

commit `8335e21`。オーナーから直接「98.6%を戻したうえで ?ag=・注文ID修正・
予約フォーム、LP v2 は完成後」と許可をもらって実行しました。

### 出したもの

| サイト | 内容 |
|---|---|
| `one-hitter-lp` | 76ファイル中10更新（LP4本＋サンクス4本＋survey＋tokushoho） |
| `one-hitter-booking` | `index.html` 1ファイル |

- **`?ag=`**（広告グループID）… aircon / mizumawari の隠し欄と別ホストへの引き継ぎ
- **注文IDの作り直しの修正**（`acc4582` の取り込み）… LP4本すべて
- **満足度 98.6%** … オーナー指示で戻しました（LP4本＋予約フォーム）

### 本番で実測した結果

```
aircon      ag=987654321  GA4 traffic_src=gads_aircon      3回押しても注文ID同一
mizumawari  ag=123        GA4 traffic_src=gads_mizumawari  同上
LP4本すべて 98.6%あり／v2の痕跡なし
予約フォーム 200・98.6%あり
固定URL5本  すべて200
```

**注文IDは3回押しても `OH-20260923-aircon-KRL4` のまま変わりません。**

---

## 取り込みの途中で、テストが3本落ちました（直してから配信）

`acc4582` に入っていた新しいテスト「GA4とフォームが同じ値になる」が落ちました。

```
?src=日本語         GA4='日本語'   フォーム='direct'
?src=（30文字）      GA4=30文字     フォーム=20文字
?src='gads mizu!'   GA4=そのまま    フォーム='gadsmizu'
```

**GA4は生の値、フォームの隠し欄は `clean()`（英数と `_-` のみ・20文字）を通していたためです。**
広告の値（`gads_*`）では一致していたので**実害は出ていません**が、
**GA4と台帳で流入元が食い違う**形でした。

**GA4側にも同じ整形をかけて揃えました。**
素通しにする方向ではなく、両方を整形する方向で直しています
（素性の分からない値を台帳にもGA4にも入れない、という考えは変えたくないので）。

これで `test-ads-tracking.py` は**全PASS**です。

## 取り込みで持ち込まなかったもの

`acc4582` には計測担当の道具も入っていましたが、**このブランチには持ち込みませんでした。**

```
tools/ads-offline-cv.py
tools/test-ads-offline-cv.py
docs/広告-オフラインコンバージョンの運用.md
```

LPの取り込みではないのと、そちらのブランチで育てているものなので、
**二重管理にしないほうがよい**と判断しました。必要なら言ってください。

`tools/test-ads-tracking.py` は衝突しましたが、**HEAD側が空だったので取り込む側を
そのまま採用**しています（テスト9本と注文ID用の2本が入りました）。

---

## ⚠️ ひとつ、引き継ぎをお願いします

**作業ツリーには LP v2 が入っています。配信物（`deploy/`）は v2 前の状態のままにしてあります。**

配信の前に、v2 の影響を受けた6ファイルを v2 直前（`82f9003`）へ一時的に戻し、
ビルド・点検・配信してから作業ツリーへ戻しました。
**配信物に v2 が混ざっていないことを機械で確認してから配信しています**（温白0・ピル0・区名0／4本とも）。

**＝ いまこのブランチでビルドすると、`deploy/` に v2 が入ります。**
**v2 の承認が出るまでは、ビルドしたまま配信しないでください。**

私は v2 の続き（§5-3 Googleクチコミ → §5-5 カート化 → §5-8 → §5-7）に戻ります。
**9/24 のプレビューは、本番とは別のデプロイで出します。**

---

## 返信（CMO / 2026-09-23 12:45）

配信と実測、ありがとうございます。GA4とフォームの流入元を両方整形で揃えた判断もそのままで良いです。完了にします。LP v2 は 9/24 のプレビューを待ちます。
