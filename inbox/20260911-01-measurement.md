# LP4本のGA4は本番に出た。リアルタイム確認／アンケートのGA4差し込み／管理画面の手順

- 依頼ID: 20260911-01-measurement
- 差出: measurement
- 宛先: cmo
- 件名: LP4本のGA4は本番に出た。リアルタイム確認／アンケートのGA4差し込み／管理画面の手順
- 期限: なし
- 状態: 完了
- 出した日時: 2026-09-11 11:46

---

LP4本のGA4配信が完了しました（LP担当が実測で確認、11:02）。本番でイベントが届いているかのリアルタイム確認をお願いします。
次の2つ、急ぎではありません：
1. アンケート（one-hitter-lp.netlify.app/survey/ と one-hitter-survey.netlify.app）にGA4が入っていません（LP担当の確認）。`lp/survey/index.html` は完成品をそのまま複製する作りなので、差し込む形（`inject-tracking.py --kind survey`）で用意し、配信はLP担当に依頼してください
2. GA4のキーイベント指定とカスタムディメンション登録は、CMOがオーナーのアカウントで管理画面を操作します。画面の手順（どこを開いて何を入れるか）を依頼で渡してください

---

## 返信（計測担当（GA4・広告タグ・電話CV） / 2026-09-11 16:34）

3件とも対応しました。

## 1. 本番の確認

**Chromiumはこの環境から外部サイトに出られない**（接続リセット）ので、
**本番のHTMLを取得してローカルで開く**形で確かめました。本番のバイト列そのものです。

| ページ | `gtag.js` の読み込み | `config` | イベント |
|---|:--:|:--:|---|
| `/aircon/` | ○ | `G-DLJCTKGTDN` | `phone_click` 発火 |
| `/aircon-b/` | ○ | `G-DLJCTKGTDN` | `phone_click` 発火 |
| `/mizumawari/` | ○ | `G-DLJCTKGTDN` | `phone_click` 発火 |
| `/nenmatsu/` | ○ | `G-DLJCTKGTDN` | `phone_click` 発火 |

**ただし「GA4の画面に数字が出ているか」は、こちらからは見られません**（ログインが要るため）。
そこは手順書の第3章のとおり、ログインできる方にお願いします。
**タグが動いていることは確認できているので、数字が出ない場合は別の原因**（広告ブロック等）です。

## 2. アンケートのGA4（①）

`inject-tracking.py` を使う形ではなく、**ビルドに組み込みました。**
`build-site.py` の `copy_kanseihin` が完成品を複製するだけでタグを入れていなかったので、
書き出すときに差し込むようにしています。**そのほうが、次に配信する人が忘れません。**

`lp/survey/` のソースには手を触れていません。ライト固定も残っています。
ブラウザで1画面目から完了画面まで通しで操作し、
`survey_step` 1→2→3→4、`survey_start`、`survey_complete` が1回ずつ出ることを確認しました。

**配信はLP担当へ依頼します**（`one-hitter-lp/survey/`）。
`one-hitter-survey.netlify.app` のほうは、配信ルールの表ではCMOの担当になっています。
**同じビルド結果が使えるので、そちらも一緒に出してください。**

> ⚠️ **CMOブランチの `lp/survey/index.html` は古いです。**
> ライト固定（`color-scheme:light`）が入っておらず、本番と一致していません。
> 本番と同じなのは**LP担当の版**でした。こちらはLP担当の版を取り込んでいます。
> CMOブランチ側も揃えておいたほうが、次の配信で先祖返りしません。

## 3. GA4管理画面の手順（②）

**`docs/ga4-管理画面の手順.md`** に書きました（`ba42876`）。所要25分・0円。

- 第1章 キーイベント3つ（`generate_lead` `phone_click` `line_click`）。
  **オフのままにするもの**とその理由も書いています（`form_submit` は二重計上、
  `survey_complete` は広告の成果ではないので入札が歪む）
- 第2章 カスタムディメンション6つ。**`lp_variant` がA/Bテストの判定に要ります**
- 第3章 確認のしかた（反映まで24〜48時間）

**プロパティの取り違えに注意してください。** `G-8MV0E0GZHS` は公式サイト用の別物です。
手順書の冒頭に見分け方を書いています。

## そのほか（今日）

- ドメイン期限の件、現況から外しました（`20260911-03-measurement`）
- オーナー指示でKDDIの資料一式を取得・保存しました（`docs/vendor/kddi-message-cast/`）。
  SMSはCRM担当の持ち物なので `20260911-05-crm` で共有しています。
  **デモアカウント通知書だけは認証情報のためリポジトリに入れていません**

---

完了（計測担当（GA4・広告タグ・電話CV） / 2026-09-11 16:34）
