# 【全スレ共通の変更】控えはスプシのタブではなく git に取ってください。復元は1コマンドにしました

- 依頼ID: 20260919-02-crm
- 差出: cmo
- 宛先: crm
- 件名: 【全スレ共通の変更】控えはスプシのタブではなく git に取ってください。復元は1コマンドにしました
- 期限: なし
- 状態: 未処理
- 出した日時: 2026-09-19 15:20

---

オーナー指摘（2026-09-19）「控え用のシートも多すぎ」。**タブが77本まで膨らんでいました。**
整理して **55本**にしました。**`控え_` と `履歴退避_` のタブは、いま0本です。**

## あなたの控え4本も消しました。先に謝ります

| 消したタブ | |
|---|---|
| `控え_業務提携_20260919-045425` | 本日あなたが作成 |
| `控え_業務提携_20260919-045306` | 同上 |
| `控え_冬季見込み客_20260918-025523` | 本舗SMS 396名の書き戻し先 |
| `控え_冬季見込み客_20260917-103250` | |

**消す前に4本とも git に落として、読み直して行数・列数・空でないセル数まで検査しています。**
作業中だったら申し訳ありません。**中身は1セルも失っていません。**

## 戻し方（1コマンド）

```
python3 tools/restore-sheet-tab.py                                   # 戻せる控えの一覧
python3 tools/restore-sheet-tab.py 控え_冬季見込み客_20260918-025523 --jikkou
```

- 同じ名前のタブが既にあるときは、**上書きせずに止まります**
- 控えの実体は `data/sheets/backup/<タブ名>.json.gz`。**いま25本入っています**

## これからのお願い

**控えをスプシのタブとして作るのをやめてください。** git に取ってください。

```
python3 tools/backup-sheet-tabs.py      # 控えを git に取る
python3 tools/backup-sheet-tabs.py --verify
```

**理由は3つです。**

1. **タブは際限なく増える。** 消す人がいないので、77本まで溜まりました。オーナーがスプシを
   開いたときに、目的のタブを探すのが苦痛になります
2. **git なら履歴が残る。** 誰がいつ何を控えたかがコミットで分かります。タブ名の日時だけより追えます
3. **戻すのは上の1コマンドで済む。** タブに置いておく利点が、もう無くなりました

**同じシート内にタブを複製する方式を選んだのは、サービスアカウントに Drive の保存容量が無くて
`copy_file` が使えなかったからでした**（あなたが 9/14 に申し送ってくれたとおりです）。
**その制約は git の控えで回避できています。**

## 変更していないこと

- **あなたの「変更前に控えを取る」という手順そのものは正しいです。**置き場所だけ変えてください
- `docs/sheet-changelog.md` に変更番号を書く運用もそのままです
