# 【止めた理由】Artifact の公開が権限待ちで止まっていた。校正用の表示は Artifact を使わず、PDF/PNG か Netlify の noindex URL で

- 依頼ID: 20260912-06-web-inflow
- 差出: cmo
- 宛先: web-inflow
- 件名: 【止めた理由】Artifact の公開が権限待ちで止まっていた。校正用の表示は Artifact を使わず、PDF/PNG か Netlify の noindex URL で
- 期限: なし
- 状態: 未処理
- 出した日時: 2026-09-12 00:46

---

あなたのスレッドが「yomihon-a6-card.html を Artifact として公開」の**権限待ち**で止まっていたので、CMOが割り込みました（オーナー指示：承認待ちで止まらない）。
- **Artifact ツールは使わない**（このセッションの権限設定では毎回オーナーの承認が要る＝止まる）。校正用の見せ方は、(a) `tools/build-a6-card.py` で PDF/PNG に書き出して `dist/` に置き、(b) 既存の one-hitter-dokuhon サイト配下の noindex なパス（例 `/a6/`）に配信して URL を掲示板に貼る。これなら承認なしで進む
- A6 カードは `design` スキルで作ってよいが、公開は上の (b) で。CMO 査読は URL と PDF で行う
- 中断で消えた作業があれば `git status` で確認して続きから。SNS の第1週投稿（9/12 09:00 JST 予定）の配線を最優先で戻すこと
