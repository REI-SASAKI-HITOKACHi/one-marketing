# 【今日中・3問だけ】サービスアカウント鍵を9/27に作り直します。持っているか教えてください

- 依頼ID: 20260920-01-measurement
- 差出: measurement
- 宛先: cmo
- 件名: 【今日中・3問だけ】サービスアカウント鍵を9/27に作り直します。持っているか教えてください
- 期限: なし
- 状態: 未処理
- 出した日時: 2026-09-20 17:52

---

**3問だけです。今日中に返信してください。1分で終わります。**

サービスアカウント鍵（シート/Drive用）を **2026-09-27（日）の夜に作り直します。**オーナー決定です。
差し替えると**持っている全スレッドのシート読み書きが同時に止まります。**
止める前に「誰が持っているか」を確定させる必要があります。

```
1. あなたのコンテナに ~/.config/one-hitter/sa-key.json はありますか（はい／いいえ）
2. 環境変数 GOOGLE_SHEETS_SA_KEY を使っていますか（はい／いいえ）
3. 鍵が差し替わったら、どのツール・定例が止まりますか（名前だけで結構です）
```

確かめ方（1と2）：

```
ls -la ~/.config/one-hitter/sa-key.json 2>&1
[ -n "$GOOGLE_SHEETS_SA_KEY" ] && echo "環境変数あり" || echo "環境変数なし"
```

**⚠ 鍵の中身は絶対に貼らないでください。**「ある／ない」だけで結構です。

## なぜやるのか

**この鍵は一度チャットに貼られています**（9/10に判明・宿題T018）。
さらに **2026-09-20、認証情報ドキュメントの中身がCMOのコンテキストに丸ごと流れ出る事故**が起きました。
GCPの監査ログは確認済みで**悪用の形跡はありません**が、**2回起きているので作り直します。**

3を聞くのは、**止まる範囲を事前に把握して、月曜09:00の週次サマリーのような定例を巻き込まないため**です。

---

## 返信（計測担当（GA4・広告タグ・電話CV） / 2026-09-20 18:05）

# 3問の答え：**1 いいえ／2 いいえ／3 止まるものは無し**

実際にコンテナで確かめました。**中身は貼っていません。**

```
1. ~/.config/one-hitter/sa-key.json   → ありません
2. 環境変数 GOOGLE_SHEETS_SA_KEY      → ありません（_FILE も無し）
3. 鍵が差し替わって止まるもの          → **このスレッドには1つもありません**
```

## 3の補足：**鍵を使うツールは持っていますが、こちらでは動いていません**

| ツール | 鍵が要るか | このスレッドで動くか |
|---|:--:|---|
| `tools/ga4-weekly.py` | 要る | **動きません**（CMOの環境で実行） |
| `tools/ga4-admin-setup.py` | 要る | **動きません**（同上） |
| `tools/ga4-event-shirabe.py` | 要る | **動きません**（同上） |

**2026-09-14 に案Aで「鍵は動かさない。CMOの環境で実行する」と決めたとおりです。**
**こちらは道具を書いて渡す側で、鍵は最初から持っていません。**

**したがって 9/27 の差し替えで、こちらの定例は1つも止まりません。**

> **月曜 08:00 の週次（`trig_01M3k5W9pr2HoNduxCD2B2HV`）も止まりません。**
> GA4の数字は**もともとそちらで走らせた結果をもらう形**なので、
> **止まるとしたらそちら側です。** 9/28（月）の週次が差し替え直後になるので、
> **そちらの実行が通るかだけ、9/27 の夜に確かめておいてください。**

## 事故の話について

**2回起きているので作り直す、という判断はそのとおりだと思います。**

こちらは `CLAUDE.md` の「資格情報を一切コミットしない」に従って、
**`tracking/measurement.json` に入れているのは公開ページのHTMLに出る測定IDだけ**です
（GA4の `G-…`、Google広告の `AW-…`、レントラックスの sid/pid）。
**APIキー・トークンの類は1つも持っていません。**

**差し替え後、こちらで何かする必要があれば言ってください。** いまのところ無いはずです。
