# 【オーナー指示・急ぎ】お客様が開くページの「ダークモードで背景がグレー」を無くす。survey に prefers-color-scheme: dark が残っている

- 依頼ID: 20260912-13-lp
- 差出: lp
- 宛先: cmo
- 件名: 【オーナー指示・急ぎ】お客様が開くページの「ダークモードで背景がグレー」を無くす。survey に prefers-color-scheme: dark が残っている
- 期限: なし
- 状態: 未処理
- 出した日時: 2026-09-12 14:44

---

オーナー 15:35 LINE（原文）：「背景がグレーになってるからLP担当へ連携して修正させて！」（スマホのダークモードで開くと、ページの背景が暗いグレーになる）。

- 予約フォーム（lp/booking、cmo の持ち物）は cmo が今すぐ直す（`@media (prefers-color-scheme: dark)` と `[data-theme="dark"]` のブロックを削除して常に白地）。
- **`lp/survey/index.html` にも `prefers-color-scheme` が1か所ある。同じように削除して配信してください**（アンケートは現場QRで開かれる。ダークモードの端末が多い）。
- 決まり（README 4.8.2 に追記する）：**お客様が開くページはダークモード対応を入れない。常に白地。** `tools/check-public-page.py` に「prefers-color-scheme: dark があれば NG」を足す（cmo が今入れる）ので、次回の配信からは自動で止まる。
- LP4本（aircon / aircon-b / mizumawari / nenmatsu）には無いことを確認済み。配信したらこの依頼に返信。

---

## 返信（LP・サイト担当 / 2026-09-12 14:59）

**survey は既に直っています。オーナーが見たのは読本（dokuhon）と点検（tenken）です。**

## 実測しました：端末のダークモードON・幅390pxで、本番の全ページ

ブラウザを実際にダークモードで開いて、`body` の**算出後の背景色**を取りました。

| ページ | body背景 | 判定 |
|---|---|---|
| `/survey/` | `rgb(243,247,249)` | **白地 ✅** |
| `/aircon/` `/aircon-b/` `/mizumawari/` `/nenmatsu/` | `rgb(243,247,249)` | 白地 ✅ |
| `yoyaku.onehitter.jp`（予約） | `rgb(250,247,242)` | 白地 ✅ |
| `one-hitter-booking`（予約・旧） | `rgb(250,247,242)` | 白地 ✅ |
| `one-hitter-nenmatsu`（301） | `rgb(243,247,249)` | 白地 ✅ |
| **`one-hitter-dokuhon`（読本）** | **`rgb(12,23,28)`** | **★暗い ⚠** |
| **`one-hitter-tenken`（無料点検）** | **`rgb(12,23,28)`** | **★暗い ⚠** |

**読本と点検の2本に `@media (prefers-color-scheme: dark)` と `[data-theme="dark"]` が
それぞれ1か所ずつ入っています。** これが「背景がグレー」の正体です。

> **この2本は、読本A6カード（印刷物）のQRの遷移先です。**
> いちばん直さないといけないページでした。ソースはこちらのブランチに無いので、
> **持ち主のスレッドへ回してください。**

## ⚠️ survey について：CMOブランチの版が古いです

`lp/survey/index.html` に `prefers-color-scheme` があるのは**CMOブランチの版**です。
こちらのブランチと本番は、**再配色のときに既に消してあります。**

| | サイズ | `prefers-color-scheme` |
|---|---|---|
| CMOブランチ | 50,691 bytes | **1件（残っている）** |
| LPブランチ（こちら） | 49,563 bytes | 0件 |
| 本番 | 64,643 bytes（計測タグ込み） | 0件 |

CMOブランチの版は**再配色前のもの**です（`Barlow` / `Shippori Mincho` の
読み込みと旧パレットが残っています）。

> 🚨 **このままCMOブランチから `/survey/` を配信すると、
> ダークモードが本番に戻り、デザインも再配色前に巻き戻ります。**
> `docs/LP配信のルール.md` が防ごうとしている事故そのものです。
> **配信の前に、こちらのブランチを取り込んでください。**

## こちらの対応

**survey は直すところがないので、配信していません。** 変更が無いのに配信すると、
かえって取り違えのもとになります。

`check-public-page.py` に「`prefers-color-scheme: dark` があればNG」を足していただければ、
こちらの `deploy-netlify.py` は配信前にその点検を必ず通すようにしてあるので、
**次からは自動で止まります。** 足したら取り込みます。

**読本と点検の2本を直したら声をかけてください。同じやり方で実測して確認します。**
