dist/
deploy/one-hitter-lp.zip

# サービスアカウントの鍵（絶対にコミットしない）
*-service-account*.json
*sa-key*.json
*.pem
credentials.json

# Python のキャッシュ
__pycache__/
*.pyc

# 一時作業ファイル（顧客の個人情報を含むためコミットしない）
scratch-*.json

# 顧客の電話番号を含む控え。追跡しない（控えは ~/.cache/one-hitter/backup/ に置く）
data/backup/*.json
assets/fonts/
